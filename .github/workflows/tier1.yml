name: Tier 1 — Linux CI
run-name: Tier 1 — Linux CI${{ inputs.source_ref && format(' — {0}', inputs.source_ref) || '' }}

on:
  push:
    branches: ["main"]
    # Docs/tooling-only pushes to main don't affect the build — skip the full
    # tier matrix. This is a fail-safe DENYLIST (paths-ignore): a push is skipped
    # only when EVERY changed file is provably non-build (docs, git config
    # .gitignore/.gitattributes, and .claude/** Claude tooling that CI never
    # reads). Anything not listed — code, CMakeLists/CMakePresets, .github CI,
    # dictionaries/, tools/codegen — still triggers a build. Applied to push: ONLY, never
    # pull_request: a path-skipped REQUIRED check never reports a conclusion,
    # which would block every PR forever (PR-side cost is already controlled by
    # the gate-precheck skip below).
    paths-ignore:
      - "**/*.md"
      # brain/ (the SecondBrain bundle) is documentation. It is ALL Markdown today,
      # so "**/*.md" above already covers it -- but only by accident of CONTENT.
      # The first non-.md file added there (a diagram, a JSON index, a helper)
      # would silently re-arm the whole matrix on a docs-only change. Listing the
      # directory records the INTENT, so the skip stops depending on file
      # extensions. Freshness is still gated -- brain-freshness.yml has no
      # paths filter and must keep running on exactly these commits.
      - "brain/**"
      - "docs/**"
      - "specs/**"
      - ".specify/**"
      - "LICENSE"
      - ".gitignore"
      - ".gitattributes"
      - ".claude/**"
  # PRs run in pull_request context so the tier1-required check counts toward
  # branch protection (a workflow_dispatch run does NOT satisfy the PR merge gate
  # — only pull_request-context check-runs do). The heavy matrix stays on-demand
  # via the gate-precheck below: it SKIPS until BOTH gate-a and gate-b are
  # satisfied, so a pre-gate PR spends no CI. All three tiers gate on the SAME
  # (Gate A ∧ Gate B) condition, so the single gate-label event that satisfies
  # both co-fires all three tiers to green-latest (the sequential run-tier* label
  # scheme could not — each labeled event re-fired every tier workflow into a
  # skip, leaving the others' latest run red). push:main remains the post-merge
  # gate + README badge.
  pull_request:
    # `labeled`/`unlabeled` are load-bearing: adding gate-a-done / gate-b-done
    # after /gate-a / /gate-b must re-evaluate the gate so the matrix runs.
    # `edited` re-evaluates when the PR body changes (waiver-rationale detection).
    types: [opened, reopened, synchronize, labeled, unlabeled, edited]
    branches: ["**"]
  workflow_dispatch:
    inputs:
      source_ref:
        description: Exact upstream commit to build for rolling validation
        required: true
        type: string
  # 056 REL-1: on a published GitHub release, the python-wheel-release job below
  # attaches the single cp312-abi3 wheel as a release asset (no PyPI — REL-2).
  # The wheel build + full install-test matrix run first on the release commit so
  # a broken artifact cannot be attached; the C++ build/sanitizer/coverage/bench
  # lanes are SKIPPED on release (they already gated the merge commit this release
  # descends from) via `github.event_name != 'release'` on those jobs.
  release:
    types: [published]

env:
  CONAN_HOME: ~/.conan2

# Concurrency: a CONDITIONAL workflow-level group + per-heavy-job groups below.
#
# The workflow-level group is EVENT-DEPENDENT to avoid the classic footgun (a
# cheap skip-run cancelling a genuine in-flight matrix) while still preventing
# overlapping Tier-1 runs on `main`:
#   * push (main only — always proceed=true via the non-PR early-exit in
#     gate-precheck, so a push run is NEVER a skip-run): a SHARED group
#     `tier1-push-<ref>` with cancel-in-progress, so a newer main push (e.g. a
#     squash-merge immediately followed by a post-merge close-out) cancels the
#     older WHOLE run at run START — no overlapping Tier-1 runs, no duplicate
#     clang matrix left running while the newer run spins up. Cancelling the
#     older main run is always desirable: main HEAD is the newer commit, which
#     supersedes it.
#   * pull_request: a UNIQUE per-run group (`tier1-run-<run_id>`), so the
#     workflow-level group NEVER cancels across PR runs. This preserves the
#     property that a PR skip-run (gate-precheck proceed=false, heavy jobs
#     `if`-skipped) cannot cancel a real in-flight matrix — Tier 1 heavy jobs
#     are SKIPPED until both gates clear, so every pre-gate push / gate-{a,b}
#     label event spawns such a skip-run.
#
# PR anti-pileup is handled by the PER-JOB groups below (keyed on
# `<pr#||ref>-<preset>/<sanitizer>/…`): a newer push mid-matrix cancels the
# prior matrix per job. The linux matrix keys its group on
# `matrix.preset` so the six preset legs don't cancel each other. gate-precheck
# is intentionally left ungrouped at the job level: it is cheap and must never
# cancel anything.
concurrency:
  group: >-
    ${{ github.event_name == 'push'
        && format('tier1-push-{0}', github.ref)
        || format('tier1-run-{0}', github.run_id) }}
  cancel-in-progress: true

jobs:
  # ── Gate precheck — Tier 1 does not spend CI until gates are satisfied ────
  # The user's pipeline is local-first: /speckit-verify mirrors Tier 1
  # locally, /gate-a (design) and /gate-b (PR review) converge via Codex and
  # apply gate-{a,b}-{done,waived}. Only THEN is the CI matrix worth running.
  # gate-a.yml / gate-b.yml already enforce the labels (red on a PR without
  # them); this job ADDS a cheap gate so the expensive matrix below is simply
  # SKIPPED (not run) until both gates clear — it never fails red itself
  # (the two gate workflows own the red signal; no duplicate).
  #
  # Non-PR events (post-merge push:main, manual workflow_dispatch) are trusted
  # and always proceed — there is no PR/labels to inspect post-merge.
  #
  # The label/auto-waive logic below is a faithful mirror of gate-a.yml +
  # gate-b.yml §6 (whitespace-only, pure-doc, Gate-A trigger paths). Keep the
  # three copies in sync if the gate rules change.
  gate-precheck:
    name: gate-precheck (Gate A + Gate B)
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    outputs:
      proceed: ${{ steps.decide.outputs.proceed }}
      python_touched: ${{ steps.decide.outputs.python_touched }}
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ inputs.source_ref || github.sha }}
          fetch-depth: 0

      - name: Decide whether the gates permit Tier 1
        id: decide
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -o pipefail

          # ── Python-relevance path-filter (computed for ALL events) ──
          # ⚠️ AFTER #254 THIS FLAG GATES THE WHEEL JOBS ALONE. It used to gate
          # `python-bindings` too; that job was folded into the six `linux`
          # matrix legs and deleted, and `linux` has NO python_touched gate —
          # only `proceed`. So the bindings are now built and pytest-ed on every
          # non-release run, including pure-C++ PRs. That is a deliberate trade:
          # the binding signal goes from CONDITIONALLY required to
          # UNCONDITIONALLY required, and it is what pays for the merge.
          # This collapse is the split #253 asks for, arrived at by deletion.
          #
          # python-wheel-{build,test} gate on `proceed` (both gates satisfied)
          # AND this relevance flag: on a PR they run only once the gates clear,
          # and then only when the diff touches the binding/C-ABI surface — so a
          # pure-C++ PR or a pre-gate PR skips the container wheel build.
          # (⚠️ This said "~7-min" and was wrong by ~10x — measured 53-74 min
          # uncached, 68m46s on PR #269's run, where it was Tier 1's critical
          # path. Corrected with its sibling at the job itself in the same
          # commit; see there for the measurement and #259.)
          # A merge to main (push), workflow_dispatch, and release build them
          # UNCONDITIONALLY (full build on main; PRs stay path-gated).
          # `.github/workflows/tier1.yml` is in the set because the wheel jobs'
          # BUILD RECIPE lives here. A PR editing only this file used to leave
          # every python job SKIPPED, so a change to how they build shipped with
          # zero CI signal and was first exercised post-merge on push:main. The
          # added cost is bounded: these jobs also require `proceed` (both gate
          # labels), which in this repo's flow lands once, immediately before
          # merge.
          #
          # ⚠️ `conan/profiles/linux-clang-{debug,asan,tsan,ubsan}` and
          # `conanfile.py` are KEPT IN THE LITERAL, but their #243/#247
          # justification no longer applies: they were here because each
          # python-bindings leg named a DIFFERENT profile, so
          # `conan/profiles/linux-clang-tsan` decided what the tsan leg linked.
          # Those legs are gone, and the `linux` matrix that replaced them does
          # not read this flag at all. Narrowing the literal is #253's, not
          # #254's (NG-4) — recorded here so the stale warrant is visible rather
          # than silently inherited.
          #
          # ANCHORED to exactly those four profiles, not the bare
          # `conan/profiles/` prefix: the four ARE the python legs' build
          # recipe, and a bare-prefix match launches the whole ~387-runner-
          # minute python fleet (4 binding legs + wheel build + 4 wheel
          # install-tests) on an edit to any OTHER profile under that
          # directory — `windows-msvc-release`, `linux-clang-libc++`,
          # `linux-clang-coverage`, `README.md`, `linux-clang-tsan.backup` —
          # none of which these legs consume. Two caveats: (a) if a python
          # leg ever starts consuming a fifth profile, the alternation needs
          # one more term — a visible, one-line fix-forward, the same
          # maintenance the `linux` matrix's own profile list already
          # carries; (b) none of the four profiles uses Conan `include()`
          # today, so there is no shared base file this anchor would miss —
          # if one is added under `conan/profiles/`, the filter must widen
          # to cover it.
          # ⚠️ #255 ADDED THREE TERMS, and the warrant is the tier1.yml one above
          # applied consistently: "the wheel jobs' BUILD RECIPE lives here".
          #
          #   ci/check-wheel-payload.sh          part of the recipe MOVED OUT of
          #   ci/expected-shipped-doc-files.txt  this file into ci/ by #255. Left
          #                                      unlisted, an edit to the payload
          #                                      gate itself would ship with zero
          #                                      pre-merge signal — the exact
          #                                      failure the tier1.yml term exists
          #                                      to prevent.
          #   CMakeLists.txt (ROOT only)         THE WHEEL'S CONTENTS ARE ITS
          #                                      install() RULES. scikit-build-core
          #                                      stages the install tree verbatim,
          #                                      so a rule added here changes what
          #                                      the wheel ships. Failing scenario
          #                                      this closes: a PR adds
          #                                      install(... DESTINATION bin) and
          #                                      touches no bindings/python/ file
          #                                      -> wheel job skipped -> the wheel
          #                                      silently gains bin/ -> merges green.
          #
          # ROOT-ANCHORED deliberately: `^CMakeLists\.txt$` and not
          # `.*CMakeLists\.txt$`, which would launch the ~387-runner-minute python
          # fleet on nearly every C++ change. ⚠️ THE RESIDUAL IS REAL AND STATED
          # RATHER THAN PAPERED OVER: a subdirectory CMakeLists can also add an
          # install() rule and will NOT match here. Those install into the export
          # set's `lib/`, which `wheel.exclude` drops wholesale, so the plausible
          # blast radius is small — but it is not zero, and the backstop for it is
          # push:main, which builds everything unconditionally. Pre-merge coverage
          # of the wheel payload is therefore GOOD, not TOTAL; do not write
          # "cannot reach main" anywhere on the strength of this line.
          PY_RE='^(bindings/python/|include/fix/c_api|\.github/workflows/tier1\.yml$|ci/check-wheel-payload\.sh$|ci/expected-shipped-doc-files\.txt$|CMakeLists\.txt$|conan/profiles/linux-clang-(debug|asan|tsan|ubsan)$|conanfile\.py$|.*\.i$)'
          PYTHON_TOUCHED=false
          EV="${{ github.event_name }}"
          if [ "$EV" = "pull_request" ]; then
            git diff --name-only "${{ github.event.pull_request.base.sha }}" "${{ github.event.pull_request.head.sha }}" > /tmp/py_changed.txt || true
          else
            # push:main (merge), workflow_dispatch, release, other trusted events:
            # build EVERYTHING including the python wheel + binding sanitizer legs,
            # regardless of what the merge touched. Only PRs are path-gated (above).
            # (user 2026-07-03 — full build on every main push.)
            PYTHON_TOUCHED=true
          fi
          if [ "$PYTHON_TOUCHED" != true ] && [ -s /tmp/py_changed.txt ] && grep -qE "$PY_RE" /tmp/py_changed.txt; then
            PYTHON_TOUCHED=true
          fi
          echo "python jobs relevant (python_touched)=$PYTHON_TOUCHED"
          echo "python_touched=$PYTHON_TOUCHED" >> "$GITHUB_OUTPUT"

          # ── Non-PR events: trusted, always run ──────────────────────────
          if [ "${{ github.event_name }}" != "pull_request" ]; then
            echo "Event '${{ github.event_name }}' is not a pull_request — Tier 1 runs unconditionally."
            echo "proceed=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          BASE_SHA="${{ github.event.pull_request.base.sha }}"
          HEAD_SHA="${{ github.event.pull_request.head.sha }}"
          PR_NUMBER="${{ github.event.pull_request.number }}"
          git diff --name-only "$BASE_SHA" "$HEAD_SHA" > /tmp/changed.txt
          echo "Changed files:"; cat /tmp/changed.txt

          # ── Shared auto-waive (mirror gate-a.yml / gate-b.yml §6) ───────
          WS_EMPTY=false
          if [ -z "$(git diff -w "$BASE_SHA" "$HEAD_SHA")" ]; then
            WS_EMPTY=true
          fi
          NON_DOC=$(grep -Ev '^[^/]*\.md$|^docs/|^book/|^README' /tmp/changed.txt || true)
          SPECIFY_DOCS=$(grep -E '^\.specify/[^/]+\.md$' /tmp/changed.txt || true)
          PURE_DOC=false
          if [ -z "$NON_DOC" ] && [ -z "$SPECIFY_DOCS" ]; then
            PURE_DOC=true
          fi

          LABELS=$(gh pr view "$PR_NUMBER" --json labels --jq '.labels[].name' || true)
          BODY=$(gh pr view "$PR_NUMBER" --json body --jq '.body' || true)

          # ── Gate A (mirror gate-a.yml) ──────────────────────────────────
          GATE_A_OK=false
          if [ "$WS_EMPTY" = true ] || [ "$PURE_DOC" = true ]; then
            echo "Gate A: auto-waived (whitespace-only or pure-doc)."
            GATE_A_OK=true
          else
            TRIGGER_RE='^(include/|src/|codegen/|\.specify/[^/]+\.md$)'
            if [ -z "$(grep -E "$TRIGGER_RE" /tmp/changed.txt || true)" ]; then
              echo "Gate A: no trigger paths touched — does not apply."
              GATE_A_OK=true
            elif echo "$LABELS" | grep -qx 'gate-a-done'; then
              echo "Gate A: gate-a-done present."
              GATE_A_OK=true
            elif echo "$LABELS" | grep -qx 'gate-a-waived' && echo "$BODY" | awk '
                BEGIN { found = 0; rationale = 0 }
                /^##+[[:space:]]*[Gg]ate[[:space:]]*[Aa]/ { found = 1; next }
                found && /[^[:space:]]/ { rationale = 1 }
                END { exit (rationale ? 0 : 1) }'; then
              echo "Gate A: waived with rationale in PR body."
              GATE_A_OK=true
            else
              echo "Gate A: NOT satisfied (trigger paths touched, no gate-a-done/waived)."
            fi
          fi

          # ── Gate B (mirror gate-b.yml) ──────────────────────────────────
          GATE_B_OK=false
          if [ "$WS_EMPTY" = true ] || [ "$PURE_DOC" = true ]; then
            echo "Gate B: auto-waived (whitespace-only or pure-doc)."
            GATE_B_OK=true
          elif echo "$LABELS" | grep -qx 'gate-b-done'; then
            echo "Gate B: gate-b-done present."
            GATE_B_OK=true
          elif echo "$LABELS" | grep -qx 'gate-b-waived' && echo "$BODY" | awk '
              BEGIN { found = 0; rationale = 0 }
              /^##+[[:space:]]*[Gg]ate[[:space:]]*[Bb]/ { found = 1; next }
              found && /[^[:space:]]/ { rationale = 1 }
              END { exit (rationale ? 0 : 1) }'; then
            echo "Gate B: waived with rationale in PR body."
            GATE_B_OK=true
          else
            echo "Gate B: NOT satisfied (no gate-b-done/waived)."
          fi

          # ── Verdict — UNIFORM gate: proceed iff BOTH gates clear (this job
          # stays green either way). All three tiers use this SAME condition, so
          # the single gate-label event that satisfies both gates co-fires all
          # three matrices to green-latest (no per-tier run-tier* label — its
          # sequential labeled events left the other tiers' latest run a skip). ─
          if [ "$GATE_A_OK" = true ] && [ "$GATE_B_OK" = true ]; then
            echo "Gates satisfied — Tier 1 matrix will run."
            echo "proceed=true" >> "$GITHUB_OUTPUT"
          else
            echo "::notice::Gates not yet satisfied (A=$GATE_A_OK B=$GATE_B_OK) — Tier 1 heavy jobs SKIPPED to save CI. Run /gate-a + /gate-b and add the labels. Red enforcement (if any) comes from the gate-a / gate-b workflows."
            echo "proceed=false" >> "$GITHUB_OUTPUT"
          fi
          exit 0

  # ── Linux build / test matrix ────────────────────────────────────────────
  linux:
    name: "${{ matrix.preset }}"
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: write  # auto-push-on-miss (push:main / dispatch on main) — save rebuilt Conan cache to GHCR
      id-token: write  # BoringCache authenticates this trusted validation run through GitHub OIDC
    # Backstop against a hung test wedging the runner. A clean sanitizer leg is
    # ~24 min (build ~15 + test ~6 + setup); the debug leg hit ~45 min cold (whole
    # tree recompile on a new branch). 60->90 (2026-07-09): the tsan leg hit the
    # 60-min cap on a cold run and was cancelled — 90 gives the cold sanitizer
    # legs headroom while still capping a genuine deadlock.
    # 180->240 (2026-08-02, 083): 083 changes widely-included wire/dictionary
    # headers, so its first CI run recompiles ~1500 objects against a pre-083
    # ccache. The local tsan mirror measured 168 min against the 180 cap — under
    # it, but with no headroom for a slower runner. A job that hits the cap is
    # killed BEFORE its ccache-save step, so the retry restores the same pre-083
    # cache and recompiles the same ~1500 objects: a timeout costs two full
    # cycles, not one, and a re-run cannot fix it. 240 buys the one-time churn.
    timeout-minutes: 240
    needs: gate-precheck
    # `&& github.event_name != 'release'`: a published release only needs the
    # wheel build+install-test lanes to attach the asset — the C++/sanitizer
    # matrix already gated the merge commit this release descends from.
    if: needs.gate-precheck.outputs.proceed == 'true' && github.event_name != 'release'
    concurrency:
      group: tier1-${{ github.event.pull_request.number || github.ref }}-${{ matrix.preset }}
      cancel-in-progress: true

    # ccache across CI runs: cache compiler output so unchanged TUs (the bulk of
    # the tree on an incremental PR / a rebuilt main) are served from cache
    # instead of recompiled. CMake reads CMAKE_<LANG>_COMPILER_LAUNCHER from the
    # env when a fresh build tree is first configured (CI always builds fresh).
    # CCACHE_COMPILERCHECK=content rehashes the compiler BINARY each run (not
    # mtime) so a re-installed clang-22/gcc-13 can never serve a stale object —
    # a fail-closed guard against the stale-object false-green class
    # (feedback_stale_build_objects_false_green_masks_pins).
    #
    # ⚠️ #241 — "Coverage is a separate job and is deliberately left UNCACHED to
    # protect coverage-mapping integrity" STOOD HERE for the life of this file,
    # uncited. It was a recorded DECISION, not an omission, so removing it took
    # an equivalence proof rather than a two-line step addition. It is now
    # REFUTED BY MEASUREMENT and the coverage job carries its own ccache.
    #
    # THE PROOF (do not delete without reading .specify/ci241-coverage-ccache.md,
    # which records the accept/reject bands fixed BEFORE the runs reported):
    # sha256 every .o after a cold build and again after a warm one, and diff.
    # Identical bytes cannot carry a different __llvm_covmap / __llvm_covfun /
    # __llvm_prf_* section, so byte identity IS coverage-mapping integrity.
    #
    #   cold  run 31519511922 — 1486 objects, ccache 0.94% hits, 132.8 min
    #   warm  run 31568303088 — 1486 objects, ccache 99.87% hits,  33.4 min
    #   both manifests hash to dcf17d78…21408f — ZERO differing objects
    #
    # The hit rate is not decoration: an object that MISSED was freshly compiled
    # in both runs, so its manifest line is a cold-vs-cold self-comparison
    # proving nothing. Only 2 of 1489 calls missed and 3 produce no object at
    # all, so >=1484 of the 1486 objects were genuinely cache-served (every .o
    # is a CMake target object and the launcher is job-level env, so the
    # manifest is a subset of cacheable_calls; cold cache was empty — restore
    # logged "No cache found." — so cold hits are intra-run duplicates).
    #
    # ⚠️ NOT proven, and deliberately not claimed: that ccache is safe for every
    # FUTURE coverage configuration. The proof is against this compiler, this
    # profile, this flag set. A change to any of them re-opens the question —
    # the standing `ccache statistics` step in that job is what would make a
    # silent regression visible.
    #
    # ⚠️ The FIRST post-merge coverage run on main is COLD (~133 min): `save:` is
    # push-only, so main holds no coverage entry until that run creates one.
    # That is the change working, not failing.
    env:
      CMAKE_CXX_COMPILER_LAUNCHER: ccache
      CMAKE_C_COMPILER_LAUNCHER: ccache
      CCACHE_COMPILERCHECK: content
      # Keep the upstream compression level so stored-byte and transfer
      # measurements remain comparable with the issue's existing evidence.
      CCACHE_COMPRESSLEVEL: "5"
      # Keep ccache outside the checkout so source-cleanliness tests do not see
      # the action's default .ccache directory as an untracked repo file.
      CCACHE_DIR: /tmp/fixpp-ccache-${{ matrix.preset }}

    strategy:
      fail-fast: false
      matrix:
        preset:
          - linux-clang-debug
          - linux-clang-release
          - linux-clang-asan
          - linux-clang-ubsan
          - linux-clang-tsan
          - linux-gcc-release

    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ inputs.source_ref || github.sha }}

      # The Debug+sanitizer legs (asan/ubsan/tsan) are the disk-heaviest: 3627
      # ninja edges (1460 compiles + 1460 C++20 module scans + 330 links), and a
      # 33 GiB tree of which bin/'s 330 instrumented executables are 18 G.
      #
      # ⚠️ Two numbers here were wrong and are corrected together (#254). The old
      # text said "~1748 instrumented executables": 1748 was the ninja EDGE count
      # of the day, not an executable count — run 28184395001 died at edge
      # 1651/1748 with `ld: final link failed: No space left on device`, which is
      # why UBSan (slightly larger binaries from its type-check metadata) tipped
      # over first. A duplicate of the same comment said ~1987. Neither was ever
      # a count of executables; there are 330.
      #
      # ⚠️ The "~14 GB free-disk ceiling" was ACCURATE WHEN WRITTEN and is kept as
      # history, not deleted as an error. Measured 2026-08-08 on plain
      # ubuntu-24.04: 145 G disk, 87 G free BEFORE this step, 114 G after. So the
      # ceiling moved; the reclaim is what moves it. KEEP THIS STEP — the margin
      # IS the reclaim, and #254 folds the Python bindings onto these same legs
      # on top of that tree. ⚠️ The only bindings figure quoted here is the
      # directly measured `lib/_fixpp.so` = 120.83 MiB. An earlier draft added a
      # "+~126 MiB" TOTAL; that came from a reconfigure which rebuilt 3053 edges,
      # so it cannot be attributed to this change's 5 extra edges and is not a
      # controlled delta (Appendix D P3(1); Gate B round 1 F2). The job-end
      # `Measure disk` step below is the standing instrument — use it, not a
      # remembered number.
      - name: Free up disk space
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: true
          android: true
          dotnet: true
          haskell: true
          large-packages: false
          docker-images: true
          swap-storage: false

      - name: Install Clang 22 (apt.llvm.org) + GCC 13 + Ninja
        run: |
          # Clang 22 matches local dev toolchain (constitution Article II §2).
          # llvm.sh handles GPG key + apt repo + clang-22/clang++-22/lld-22/llvm-22.
          wget -O /tmp/llvm.sh https://apt.llvm.org/llvm.sh
          chmod +x /tmp/llvm.sh
          ci/apt-guard.sh llvm-toolchain -- sudo /tmp/llvm.sh 22 all
          # apt.llvm.org's llvm.sh installs versioned binaries (e.g. clang-22)
          # but does NOT update the unversioned `clang`/`clang++` paths. CMake
          # presets use unversioned `clang++`, which otherwise resolves to the
          # runner image's pre-installed clang-18 (real binary at /usr/bin/clang++,
          # missing C++23 std::expected in libstdc++ headers visible to it).
          # `update-alternatives --install` does NOT overwrite that pre-existing
          # regular file — force a symlink directly. Detect the actually installed
          # major (llvm.sh silently falls back to an earlier major when the
          # requested one isn't yet published for the runner's distro).
          CLANG_BIN=$(ls /usr/bin/clang++-* 2>/dev/null | grep -oE '/usr/bin/clang\+\+-[0-9]+$' | sort -V | tail -1)
          CLANG_VER=$(echo "$CLANG_BIN" | grep -oE '[0-9]+$')
          test -n "$CLANG_VER" || { echo "::error::No clang-* installed by llvm.sh"; exit 1; }
          echo "Force-symlinking clang-$CLANG_VER as default unversioned clang/clang++"
          sudo ln -sf /usr/bin/clang-$CLANG_VER   /usr/bin/clang
          sudo ln -sf /usr/bin/clang++-$CLANG_VER /usr/bin/clang++
          clang++ --version
          # swig + python3-dev (#254): every leg now builds the Python bindings,
          # so `find_package(SWIG 4.2 REQUIRED)` (bindings/python/CMakeLists.txt,
          # #296 cap now LIFTED) must resolve here — only the >=4.2 floor
          # applies, so the distro's unbounded `swig` package satisfies it.
          # ⚠️ Note this is apt's swig (4.2.0 on ubuntu-24.04), NOT the wheel's:
          # the wheel build takes swig from PyPI via pyproject's build-system
          # requires. Lifting the cap therefore moves the WHEEL lane's SWIG and
          # leaves these six legs where they were — which is the point, since
          # #296 was only ever observed on the wheel lane.
          # Both packages are carried over VERBATIM from the deleted
          # `python-bindings` job's apt line — that is the configuration CI has
          # proven green, and python3-dev additionally decides which
          # Python3_SITEARCH branch fires (bindings/python/CMakeLists.txt).
          # Dropping python3-dev on the theory that actions/setup-python's headers
          # suffice is an UNTESTED narrowing: no CI run and no local build can
          # discriminate it, because all of them had the system headers present.
          ci/apt-guard.sh apt-install -- sudo apt-get install -y --no-install-recommends \
            gcc-13 g++-13 \
            ninja-build \
            swig \
            python3-dev \
            python3-pip \
            pipx

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install Conan + pytest via pipx
        run: |
          pipx install conan
          conan --version
          # pytest is required by the decimal_compare_oracle CTest entry
          # (tests/oracle/decimal_compare_oracle_test.py) which is registered
          # in every preset. ⚠️ This used to read "not just the python-bindings
          # build" — #254 folded that job into these six legs, so the contrast
          # it drew no longer exists and the sentence inverted. pytest is now
          # needed here for TWO independent reasons: that CTest entry, and the
          # bindings' own test suite (bindings/python/tests/).
          # pyyaml is required by the interop_cell_results_schema_check CTest
          # entry (tests/interop/cell_results_schema_check_test.py — 016 T028).
          pip install pytest pyyaml

      - name: Initialize Conan default profile
        run: conan profile detect --force

      - name: Teach Conan about Clang 22
        run: |
          # Conan 2.x's stock settings.yml does not enumerate Clang 22 yet.
          # Append via settings_user.yml (additive, non-destructive).
          mkdir -p ~/.conan2
          cat > ~/.conan2/settings_user.yml <<'EOF'
          compiler:
            clang:
              version: ["19", "20", "21", "22", "23"]
          EOF

      # Conan deps come from a pre-seeded GHCR OCI artifact (per matrix.preset)
      # instead of the repo-wide 10 GB actions/cache pool. Keeping the heavy
      # from-source chain (opentelemetry-cpp + protobuf/abseil/prometheus) OFF
      # that pool stops it LRU-evicting ccache — the real cause of the ~1-3%
      # ccache hit rate and cold ~100-min builds. See
      # research/G19-fix-fpml-iso20022/remaining-work/ci-conan-ghcr-pilot/.
      # MISS (unseeded / conanfile bumped) → restore is a no-op and the
      # `conan install --build=missing` below rebuilds; re-seed locally after.
      - name: Set up oras
        uses: oras-project/setup-oras@v2
      - name: Restore Conan cache from GHCR
        id: conan_restore
        run: ci/restore-conan-cache.sh ${{ matrix.preset }}

      # ccache MUST be installed BEFORE "Conan install" on this lane. When a
      # dependency has no prebuilt Conan Center binary for the resolved config,
      # `conan install --build=missing` compiles it from source using the
      # job-level CMAKE_{C,CXX}_COMPILER_LAUNCHER=ccache. If ccache is not yet on
      # PATH the dependency's compiler probe fails with exit 127 ("ccache" not
      # found) and the whole lane goes red. The earlier assumption that Tier 1
      # (libstdc++) always resolves to prebuilt deps was NOT durable: abseil/
      # 20260107.1 shipped with no linux-gcc-release binary and forced a
      # from-source build that hit exit 127. Installing ccache and its storage
      # helper before starting the BoringCache proxy keeps those dependency
      # builds on the same compiler-cache path. Same fix landed on Tier 3 in
      # #177 — ref
      # project_tier3_libcxx_crc32c_ccache127_infra.
      - name: Install ccache and its HTTP storage helper
        env:
          CCACHE_VERSION: "4.14"
          CCACHE_SHA256: c64760b0b85ba86068f4cd162dc42e2dc39c6f46b0cb8c1990dfccbec7a1fed0
          CCACHE_STORAGE_HTTP_VERSION: "0.9"
          CCACHE_STORAGE_HTTP_SHA256: 875dbf6d575d06e4c4492f1ba639beb68530bc23382031a6bacc767cded9f463
        run: |
          ccache_archive="ccache-${CCACHE_VERSION}-linux-x86_64-glibc.tar.gz"
          curl --fail --location --show-error --silent --retry 5 --retry-all-errors --retry-delay 5 \
            "https://github.com/ccache/ccache/releases/download/v${CCACHE_VERSION}/${ccache_archive}" \
            --output "${RUNNER_TEMP}/${ccache_archive}"
          echo "${CCACHE_SHA256}  ${RUNNER_TEMP}/${ccache_archive}" | sha256sum --check
          tar xzf "${RUNNER_TEMP}/${ccache_archive}" -C "${RUNNER_TEMP}"
          sudo install -m 0755 \
            "${RUNNER_TEMP}/ccache-${CCACHE_VERSION}-linux-x86_64-glibc/ccache" \
            /usr/local/bin/ccache

          helper_archive="ccache-storage-http-go-${CCACHE_STORAGE_HTTP_VERSION}-linux-amd64.tar.gz"
          curl --fail --location --show-error --silent --retry 5 --retry-all-errors --retry-delay 5 \
            "https://github.com/ccache/ccache-storage-http-go/releases/download/v${CCACHE_STORAGE_HTTP_VERSION}/${helper_archive}" \
            --output "${RUNNER_TEMP}/${helper_archive}"
          echo "${CCACHE_STORAGE_HTTP_SHA256}  ${RUNNER_TEMP}/${helper_archive}" | sha256sum --check
          tar xzf "${RUNNER_TEMP}/${helper_archive}" -C "${RUNNER_TEMP}"
          sudo install -m 0755 \
            "${RUNNER_TEMP}/ccache-storage-http-go-${CCACHE_STORAGE_HTTP_VERSION}-linux-amd64/ccache-storage-http" \
            /usr/local/bin/ccache-storage-http

      - name: Checkout the BoringCache validation plan
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.0.2
        with:
          ref: boringcache-validation
          path: .boringcache-plan
          sparse-checkout: .boringcache.toml
          sparse-checkout-cone-mode: false
          persist-credentials: false

      - name: Exclude the validation plan from source status
        run: printf '%s\n' '/.boringcache-plan/' >> .git/info/exclude

      - name: Use BoringCache for the shared Tier 1 compiler cache
        uses: boringcache/one@1039999c65011be670f5655e0e48ad556188ab12 # v1.30.4
        with:
          working-directory: .boringcache-plan
          trust-policy: auto
          mode: ccache
          diagnostics: summary
          fail-on-cache-error: true
          save-always: true

      - name: Conan install
        run: |
          # ── #278: locally-patched civetweb, exported BEFORE the install ─────
          #
          # civetweb 1.16 races on ctx->stop_flag: STOP_FLAG_ASSIGN seeds its
          # compare-and-swap with a PLAIN `*f` read while the worker threads read
          # the flag atomically. Upstream fixed it in master with
          # `__atomic_load_n(f, __ATOMIC_SEQ_CST)`, but v1.16 (2023-04-10) is
          # still the newest release, so there is nothing to upgrade TO. See
          # conan/recipes/civetweb/README.md for the full trail and the
          # retirement condition.
          #
          # ⚠️ THIS LINE IS INSIDE THE EXISTING STEP ON PURPOSE. A separate step
          # would trip ci/test-tier1-python-policy.sh's deliberately brittle
          # count of the `linux` job's steps — a pin that exists so adding a step
          # is a deliberate act. Nothing here needs to reach the python steps, so
          # folding it in is the honest option rather than re-pinning a guard.
          #
          # Export is idempotent and costs ~0.1 s (it copies two files). It must
          # precede `conan install` so the patched recipe revision is the one the
          # graph resolves. `conan install` is run WITHOUT `--update`, so the
          # cache revision wins and no remote lookup can override it.
          conan export conan/recipes/civetweb --version 1.16
          conan install . \
            -pr conan/profiles/${{ matrix.preset }} \
            --build=missing \
            -of build/${{ matrix.preset }}

      # ── #252: the dependency closure really carries the sanitizer, or none ──
      #
      # The sanitizer profiles set only CXXFLAGS/tools.build:cxxflags, so every
      # PURE-C dependency (openssl, zlib, curl, civetweb) compiled UNINSTRUMENTED
      # while its package id still moved — `tools.build:cflags` is listed in
      # `tools.info.package_id:confs` and never set. Measured under
      # `linux-clang-tsan`: libcrypto.a, 50,830 symbols, ZERO `__tsan_*`.
      #
      # ⚠️ UNCONDITIONAL, AND TWO-SIDED. Every preset in this matrix runs it: the
      # sanitizer legs assert NON-ZERO references, the others assert ZERO. An
      # `if: contains(preset, 'san')` guard would be a second spelling of the
      # polarity table (the trap ci/derive-python-sanitizer.sh's header names),
      # and dropping the negative half would leave the "grep proven only on the
      # fixed tree" shape #252's own scope note rules out.
      #
      # Placed here — after `conan install`, before configure/build — so a lane
      # whose closure is wrong fails in seconds rather than after a full build.
      # ci/test-sanitized-deps.sh pins the checker itself in `ci-script-pins`.
      #
      # ⚠️ IT ALSO SITS BEFORE THE GHCR SAVE STEPS, and that is deliberate rather
      # than incidental: a RED here means the closure this run produced is not the
      # one the profile asks for, and seeding it into the shared cache would hand
      # every later run the same wrong binaries with a `HIT` to make them look
      # earned. The cost is that a failing gate on `push:main` also forgoes that
      # run's cache seeding — accepted, because the alternative is publishing a
      # closure the gate has just rejected.
      - name: Assert the dependency closure is instrumented (#252)
        run: ci/assert-sanitized-deps.sh "${{ matrix.preset }}" "build/${{ matrix.preset }}"

      # Self-populate: on push:main, if the restore MISSED (conanfile bumped),
      # push the just-rebuilt cache to GHCR so the next run hits. seed packs ONLY
      # the current graph's package_ids (conan cache save --list) → never stale
      # packages. Best-effort (continue-on-error); never on PRs (untrusted write).
      #
      # `|| (workflow_dispatch on main)` (#222): tier2.yml has always accepted a
      # manual dispatch as the reseed lever and documents it as a deliberate
      # design choice; tier1 and tier3 gated on `push` alone, with no stated
      # reason for the asymmetry. A dispatch here therefore restored, missed,
      # rebuilt — and published NOTHING, so the only reseed lever on these tiers
      # was a push to main. restore-conan-cache.sh's MISS annotation is shared
      # by all seven call sites and now tells the reader to "dispatch this
      # workflow on main to reseed"; making the gate uniform (and ref-restricted
      # to main, so only main's content can ever publish into main's namespace)
      # is what makes that sentence true here, rather than teaching one
      # diagnostic to describe a per-tier special case.
      - name: Save Conan cache to GHCR (push:main / dispatch on main + miss only)
        if: (github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.conan_restore.outputs.hit == 'false'
        continue-on-error: true
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}  # for prune's `gh api` (delete stale tags)
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | oras login ghcr.io -u "${{ github.actor }}" --password-stdin
          ci/seed-conan-cache.sh ${{ matrix.preset }}

      # ── #254 — the Python sanitizer identity, derived ONCE from the preset ───
      # The deleted `python-bindings` job spelled the sanitizer five times per
      # leg (preset name, FIXPP_ENABLE_<SAN>, build-dir suffix,
      # FIXPP_PYTHON_SANITIZER, san_opts). Two collapse for free — the preset
      # already carries FIXPP_ENABLE_<SAN> and BUILD_SHARED_LIBS
      # (CMakePresets.json), which is why neither is passed below. The other
      # three come from here, and are consumed by expression only.
      #
      # ⚠️ Do NOT replace this with `contains(matrix.preset, 'san')` in the `if:`
      # conditions below. That is the same mapping spelled a second time under
      # another name, and it also matches `asan`/`ubsan` inside a future preset
      # name. The script is fail-closed on an unknown preset (::error:: + exit 1)
      # precisely so a new sanitizer leg cannot silently default to `none` and
      # build an UNINSTRUMENTED _fixpp.so that reports green — the #251 class.
      # Pinned by ci/test-tier1-python-policy.sh over this job's exact preset set.
      - name: Derive the python sanitizer identity from the preset
        id: pysan
        run: ci/derive-python-sanitizer.sh "${{ matrix.preset }}" >> "$GITHUB_OUTPUT"

      - name: Configure
        # FIXPP_ARTIFACT_DIR is a CACHE variable baked into the fixpp-package
        # target at CONFIGURE time — it is NOT read from the environment, so it
        # must be set here rather than on the build step. Its in-repo default is
        # a local WSL mount; on a runner it has to be inside the workspace for
        # upload-artifact to reach it. Harmless on the non-packaging lanes.
        #
        # ⚠️ THE INSTALL POLARITY IS NOW PER-LEG, AND DERIVED — #257 answered
        # the question #254 deferred. `linux-{clang,gcc}-release` build
        # `fixpp-package`, so those two now ship the Python payload
        # DELIBERATELY, at a pinned prefix-relative destination
        # (FIXPP_PY_INSTALL_LAYOUT=package). The other four still pass OFF: the
        # consumer install-witness stage-installs on ALL SIX, and a payload in
        # an artifact nobody decided to put it in is the #254 accident.
        #
        # ⚠️ The polarity comes from ci/derive-python-sanitizer.sh, NOT from a
        # `contains(matrix.preset, 'release')` guard here. That guard would be
        # the preset->packaging mapping spelled a second time under another
        # name, and it would also match a future non-packaging preset with
        # 'release' in it. The script fails closed on an unknown preset, and
        # ci/test-tier1-python-policy.sh drives it over this job's exact preset
        # set — so a new leg cannot silently default into shipping a payload.
        #
        # The option still defaults ON so 056's LAY-1 in-tree `cmake --install`
        # keeps working everywhere outside CI.
        #
        # No -B and no CMAKE_TOOLCHAIN_FILE override: the preset's binaryDir is
        # build/<preset> and its CMAKE_TOOLCHAIN_FILE already points at
        # build/<preset>/conan_toolchain.cmake, which is where `conan install
        # -of build/<preset>` above puts it. Both overrides existed only to
        # service the deleted job's `-py` build dirs.
        run: >
          cmake --preset ${{ matrix.preset }}
          -DFIXPP_ARTIFACT_DIR=${{ github.workspace }}/_artifacts
          -DFIXPP_BUILD_PYTHON=ON
          -DFIXPP_INSTALL_PYTHON=${{ steps.pysan.outputs.install_python }}
          -DFIXPP_PY_INSTALL_LAYOUT=${{ steps.pysan.outputs.py_layout }}
          -DFIXPP_PYTHON_SANITIZER=${{ steps.pysan.outputs.sanitizer }}

      # ── #254 / L-056-4 — the OFF side, asserted at Configure time ────────────
      # A cheap STRUCTURAL pin on the generated install script, run before
      # anything is built. It is deliberately paired with, not a substitute for,
      # the `fixpp::python::install-absent-witness` ctest below: this one reads
      # the generated rules, that one performs a real DESTDIR install and
      # inspects what landed. Gate A round 2 (R2-P2-1) was right that the grep
      # alone certifies less than its contract — it would pass if the payload
      # moved to install(CODE) or to another directory's install script.
      #
      # The instrument is proven on BOTH sides (design doc E-32/E-34): 4
      # directives with the rules unguarded, 0 with FIXPP_INSTALL_PYTHON=OFF.
      # The existence check comes FIRST so a 0 can never come from the file
      # having silently stopped being generated.
      - name: Assert the Python install rules match the leg's polarity (#257 / L-056-4)
        env:
          WANT: ${{ steps.pysan.outputs.install_python }}
        run: |
          set -euo pipefail
          f="build/${{ matrix.preset }}/bindings/python/cmake_install.cmake"
          if [ ! -f "$f" ]; then
            echo "::error::$f absent — FIXPP_BUILD_PYTHON=ON should have generated it."
            exit 1
          fi
          n=$(grep -c '^[[:space:]]*file(INSTALL' "$f" || true)
          echo "python install directives in $f: $n (FIXPP_INSTALL_PYTHON=$WANT)"
          # ⚠️ TWO-SIDED, and that is the whole point. The OFF legs assert ZERO;
          # the two packaging legs assert NON-ZERO. A one-sided check would pass
          # on a release leg whose payload silently stopped being installed —
          # i.e. it would certify the #257 decision as delivered while shipping
          # nothing. The instrument is proven on both sides by construction:
          # the same tree yields 0 with OFF and >0 with ON.
          if [ "$WANT" = "OFF" ]; then
            if [ "$n" != "0" ]; then
              echo "::error::FIXPP_INSTALL_PYTHON=OFF but $n install directive(s) remain."
              echo "::error::These would enter fixpp-package and the packages-linux-* artifacts on a leg"
              echo "::error::that did NOT decide to ship them — the #254 accident."
              exit 1
            fi
          else
            if [ "$n" = "0" ]; then
              echo "::error::FIXPP_INSTALL_PYTHON=ON but ZERO install directives were generated."
              echo "::error::packages-linux-*-release would ship no Python payload at all, while L-056-4"
              echo "::error::and #257 say it does. Refusing to report this lane green on an empty install."
              exit 1
            fi
          fi
          df -h "$GITHUB_WORKSPACE" | tail -1

      # 069-v44-all-families T017 (FR-008): this Tier-1 matrix is the CI
      # invariant that keeps >=1 preset continuously verifying the FULL v44
      # builder set (83, via `ctest -L roundtrip|family_golden|mode_count`
      # below with no -L filter). No preset here currently overrides
      # FIXPP_CODEGEN_V44_FAMILIES (see cmake/Codegen.cmake), so every leg
      # defaults to `all` — assert that durably so a future preset addition
      # can't silently flip a verifying job to `official` (33) without CI
      # noticing.
      - name: Verify full v44 coverage preset (FR-008)
        run: |
          value=$(grep '^FIXPP_CODEGEN_V44_FAMILIES:' "build/${{ matrix.preset }}/CMakeCache.txt" | cut -d= -f2)
          if [ "$value" != "all" ]; then
            echo "::error::Tier-1 preset ${{ matrix.preset }} configured FIXPP_CODEGEN_V44_FAMILIES='$value', not 'all'. FR-008 requires at least one CI preset to continuously verify the full 83-builder v44 coverage set — do not let a verifying preset silently opt down to 'official'."
            exit 1
          fi
          echo "OK: FIXPP_CODEGEN_V44_FAMILIES=all (full v44 coverage verified) on ${{ matrix.preset }}"

      - name: Build
        run: |
          cmake --build --preset ${{ matrix.preset }}
          # #254 disk trend point 3 of 4. One line, no new step; the operative
          # high-water mark is the job-end pair, because the consumer witness,
          # the packaging tier and `fixpp-package` all land AFTER this.
          df -h "$GITHUB_WORKSPACE" | tail -1

      # 087 T024 / FR-014 / SC-008 — the LAST GATE-CLOSABLE VACUITY PATH.
      #
      # ⚠️ ASSERT THE COUNT, NOT JUST THE EXIT CODE. Same shape as the packaging
      # assertion below, for the same reason: `ctest -L` exits 0 when the filter
      # matches NOTHING. fixpp::consumer::install-witness — which carries the
      # whole 087 system-include gate AND 086's reachability matrix — is
      # registered only inside `if(FIXPP_BUILD_CODEGEN_TOOL)` nested in
      # `if(FIXPP_BUILD_TESTS)`. If that option ever goes
      # OFF on a lane, the gate stops existing and the lane reports GREEN.
      #
      # UNCONDITIONAL — no `if:` guard, deliberately. The two test steps below
      # are mutually exclusive by `if:` (`minimal_tier_test` runs on all but linux-gcc-release,
      # `full_tier_test` only on it) and jointly cover every lane, so ONE unguarded step
      # covers both without six conditional copies. Contract §6b.
      #
      # Placed after Build and before the first test step: `ctest -N` reads
      # CTestTestfile.cmake and therefore needs a configured tree. This is
      # EARLIER than the packaging assertion it is modelled on, which sits
      # between the two test steps because only one lane needs it.
      - name: Assert the consumer witness is registered (087 FR-014 gate)
        run: |
          set -euo pipefail
          expected=1
          n=$(ctest --preset ${{ matrix.preset }} -L consumer -N \
                | sed -n 's/^Total Tests: //p')
          echo "registered consumer witnesses: ${n:-0} (expected ${expected})"
          if [ "${n:-0}" != "${expected}" ]; then
            echo "::error::consumer label registered ${n:-0} tests, expected ${expected}."
            echo "::error::The 086/087 installed-interface gate is not registered on this lane."
            echo "::error::Refusing to report this lane green on an unrun gate."
            exit 1
          fi

      # ── #254 / R2-P2-1 — the same vacuity gate, for the install witness ──────
      #
      # `fixpp::python::install-absent-witness` is the SEMANTIC half of the
      # L-056-4 guard: it performs a real DESTDIR `cmake --install` and rejects
      # the Python payload anywhere in the staged tree. It registers only when
      # FIXPP_INSTALL_PYTHON is OFF (bindings/python/CMakeLists.txt) — which is
      # to say, only because the Configure step above passes that flag.
      #
      # ⚠️ THAT IS EXACTLY WHY THIS STEP EXISTS. Drop the flag and the ON-side
      # `install-present-witness` registers instead: ctest stays green, the
      # payload silently enters packages-linux-*-release, and the only survivor
      # is the Configure-time grep that R2-P2-1 judged INSUFFICIENT. Asserting
      # the test RAN — not merely that ctest was green — is what closes that.
      # Modelled on the consumer assertion above; same `ctest -L` vacuity trap.
      #
      # The name is asserted, not just the count: `-L python` matches exactly
      # one test in either configuration, so a count alone cannot tell the OFF
      # witness from the ON one.
      - name: Assert the Python install witness is the leg's own (#257 / L-056-4)
        env:
          WANT: fixpp::python::install-${{ steps.pysan.outputs.py_witness }}-witness
        run: |
          set -euo pipefail
          names=$(ctest --preset ${{ matrix.preset }} -L python -N \
                    | sed -n 's/^ *Test *#[0-9]*: //p')
          echo "registered python install witnesses: ${names:-<none>} (expected $WANT)"
          # The NAME is asserted, not the count: `-L python` matches exactly one
          # test in EVERY configuration, so a count cannot tell the three modes
          # apart. Getting the wrong name here means the Configure flags and the
          # witness disagree — and the witness is what actually opens the tree.
          if [ "$names" != "$WANT" ]; then
            echo "::error::expected exactly '$WANT', got '${names:-<none>}'."
            echo "::error::Either the Configure flags did not reach CMake (so the wrong witness registered"
            echo "::error::and the payload disposition is not the one this leg decided), or the witness"
            echo "::error::stopped being registered at all. Refusing to report this lane green either way."
            exit 1
          fi

      # 084 T067 / FR-026a — the packaging tier is GATED ON ONE LANE.
      #
      # `ctest --preset` carries no label filter, so the six packaging witnesses
      # would otherwise run on EVERY lane, each stage-installing and then
      # configuring and compiling a sub-project — including a 1700-line TU in
      # the real-client tier — on the sanitizer legs that already sit at the
      # runner's disk ceiling.
      #
      # linux-gcc-release is the gating lane because it builds ZERO third-party
      # dependencies from source (M1 / Assumption 9), so gating the heaviest
      # tier is bounded rather than all-or-nothing.
      #
      # NOTE the exclusion is by LABEL, not by test name, and it is `packaging`
      # only: consumer::install-witness carries the `consumer` label and keeps
      # running on all six lanes. That is the "minimal tier" FR-026a intends —
      # every lane still proves an external consumer compiles and links against
      # the installed package.
      # ⚠️ THE ctest INVOCATION IS UNCHANGED. It is WRAPPED, not modified: the
      # peak-RSS instrument (#266) spawns it, samples /proc while it runs, tees
      # its output, and exits with ITS status. So this step's red/green signal is
      # still ctest's and nothing else's — see ci/measure-peak-rss.py's fail-loud
      # contract, and T5 of ci/test-peak-rss.sh, which pins that a non-zero ctest
      # status is propagated rather than swallowed.
      #
      # The `id` was `tsan_test` while the peak probe read only the tsan lane; it
      # now measures every lane in this matrix, and a name that says otherwise is
      # the kind of stale anchor this repo has been bitten by. Renamed with its
      # only two consumers, both below.
      - name: Test (minimal tier — packaging gated on linux-gcc-release)
        id: minimal_tier_test
        if: matrix.preset != 'linux-gcc-release'
      # ⚠️ `--timeout 1800` IS A WEDGE GUARD. Without it there is NO per-test
      # timeout at all: this project calls `enable_testing()` and never
      # `include(CTest)`, so no DartConfiguration.tcl is written and ctest's
      # documented 1500 s default never applies. A test that deadlocks then runs
      # until the JOB budget expires -- `session_tc_logout` wedged this lane for
      # 86 minutes on 2026-08-31 with no Timeout verdict, and would have burned
      # all 240 minutes had the run not been cancelled by hand.
      #
      # It is NOT a performance assertion and must never be tightened into one.
      # The CONDITION it must satisfy: stay clear of the slowest test that does
      # NOT carry its own TIMEOUT property, by enough margin that runner variance
      # and ordinary growth never reach it. `codegen_determinism_test` is slower
      # than this and carries its own property (tests/codegen/CMakeLists.txt); an
      # explicit property wins over `--timeout`, so it is unaffected.
      #
      # The measurement behind 1800, as a dated observation and not a standing
      # claim: across all 14 test lanes of the 2026-08-31 runs (tier1 33398306794
      # plus the tier2/tier3 runs of the same push), the slowest property-less
      # test was dictionary_pure_tests at 839 s on windows-msvc-asan.
      # That is a snapshot; it rots. Re-derive before trusting or changing it:
      #   # every lane, not one job -- the tier2/tier3 runs are SEPARATE runs
      #   for wf in tier1.yml tier2.yml tier3-libcxx.yml; do
      #     rid=$(gh run list --workflow $wf --limit 1 --json databaseId -q '.[0].databaseId')
      #     for jid in $(gh api /repos/$O/$R/actions/runs/$rid/jobs?per_page=100 -q '.jobs[].id'); do
      #       gh api /repos/$O/$R/actions/jobs/$jid/logs \
      #         | grep -oP 'Test\s+#\d+: \S+ .*Passed +[0-9.]+ sec'
      #     done
      #   done
        run: |
          python3 ci/measure-peak-rss.py \
            --out "/tmp/peak-rss-${{ matrix.preset }}.env" \
            --log "/tmp/ctest-${{ matrix.preset }}.log" \
            --label "${{ matrix.preset }}" \
            -- ctest --preset ${{ matrix.preset }} --output-on-failure -LE packaging --no-tests=error --timeout 1800

      # ── #266 / #229 criterion 4: the peak-memory measurement ────────────────
      #
      # `ctest --parallel` widening (#267) is gated on a MEASURED peak-memory
      # figure. #245 installed a probe that read cgroup v2 `memory.peak`; it
      # produced a reading on 0 of 8 post-merge runs, because a GitHub-hosted job
      # sits in the ROOT cgroup and `memory.peak` exists only on non-root nodes.
      # The source is now ci/measure-peak-rss.py, which samples /proc and sums
      # the RSS of ctest's whole process tree — the same quantity #229's local
      # sweep measured, so CI and local figures are comparable by construction.
      #
      # THREE THINGS CHANGED BESIDES THE SOURCE, each narrowing what can be
      # claimed. All the detail lives in ci/peak-memory-report.sh; the reason for
      # each is there, not duplicated here, because a comment copy of a volatile
      # rationale goes stale silently.
      #
      #  1. IT MEASURES THE TEST PHASE, NOT THE JOB. `memory.peak` was a job-wide
      #     high-water mark that also covered Build, and was reported as a
      #     deliberate CEILING because the runner user cannot reset it. Sampling
      #     around the ctest invocation needs no such concession.
      #  2. IT RUNS ON EVERY LANE IN THIS MATRIX, not just linux-clang-tsan.
      #     #267 widens `--parallel` lane by lane and its acceptance requires a
      #     peak reading PER LANE; a tsan-only probe cannot discharge that.
      #  3. THE PIN IS PER-LANE AND LIVES IN ci/expected-eligible-tests.txt.
      #     The counts genuinely differ per lane, so #245's single
      #     `expected_eligible=350` was wrong for five of the six rather than
      #     merely stale.
      #
      # NEVER FAILS THE LANE — it is a measurement, not a gate. But a measurement
      # that cannot be taken must SAY SO: every path in the report script emits
      # an attributed disposition, so "no number" can never be read as "captured,
      # and it was fine"
      # (feedback_silent_empty_recurred_three_times_including_inside_its_own_fix).
      # ⚠️ QUOTED. An unquoted `#` after a space starts a YAML comment, so the
      # previous name `Peak memory (ctest --parallel evidence, #266 / #229)`
      # actually reached GitHub as `Peak memory (ctest --parallel evidence,`.
      # Harmless while the step was advisory; not harmless now that it can be
      # the red one, since the name is what tells a reader WHICH gate failed.
      - name: "Peak memory + unexplained-sanitizer gate (#266 / #229 / #267)"
        if: always() && matrix.preset != 'linux-gcc-release'
        # ⚠️ `continue-on-error: true` DELIBERATELY REMOVED (#267 acceptance item 5).
        # It was here because this step is a MEASUREMENT and a lane must not go
        # red over a /proc sampling hiccup — that reasoning still holds and is
        # now enforced inside the script instead: `ci/peak-memory-report.sh`
        # exits 0 on every instrument-failure path, and exits 1 for exactly one
        # reason, an UNEXPLAINED sanitizer report. Leaving this line would have
        # made that `exit 1` inert: GitHub records `outcome: failure` but sets
        # `conclusion: success`, the job continues, and the lane reports green —
        # a gate that observes and never asserts. Restoring it silently reverts
        # the script's fail-closed contract, so do not, without also reverting
        # the script.
        env:
          # `|| 'unavailable'`: GitHub's steps context is documented to report
          # `skipped` for a step that never ran, but default rather than trust
          # an empty interpolation — a blank cell would defeat the "Test
          # outcome is on the page regardless of which branch rendered it"
          # requirement this step exists to satisfy.
          TEST_OUTCOME: ${{ steps.minimal_tier_test.outcome || 'unavailable' }}
        # The Test step above (id: minimal_tier_test) has no `always()`, so on a
        # Build failure it is SKIPPED, not failed — and this step still runs.
        # A number here would then be read as acceptance evidence for a run that
        # measured a workload strictly smaller than "the suite ran". The report
        # script labels accordingly rather than suppressing the number.
        run: |
          ci/peak-memory-report.sh \
            "${{ matrix.preset }}" \
            "/tmp/peak-rss-${{ matrix.preset }}.env" \
            "/tmp/ctest-${{ matrix.preset }}.log" \
            "${TEST_OUTCOME}"

      # rpmbuild is NOT on ubuntu-24.04 and packaging::contents drives the
      # default generator list (DEB;RPM;TGZ), so without this the lane fails on
      # a missing tool rather than on anything about the package.
      - name: Install rpmbuild (packaging lane only)
        if: matrix.preset == 'linux-gcc-release' || matrix.preset == 'linux-clang-release'
        run: ci/apt-guard.sh apt-update -- sudo apt-get update && ci/apt-guard.sh apt-install -- sudo apt-get install -y rpm

      # ⚠️ ASSERT THE COUNT, NOT JUST THE EXIT CODE. `ctest -R`/`-L` exits 0 when
      # the filter matches NOTHING (verified), so an exit-code-only check reports
      # this lane green having run zero witnesses — the
      # feedback_ci_gate_observes_not_asserts_witness_skips_into_green shape.
      # Registration is asserted BEFORE the run, so a lane that silently stops
      # registering them fails here instead of passing quietly.
      - name: Assert the packaging tier is registered (SC-011/SC-012 gate)
        if: matrix.preset == 'linux-gcc-release'
        run: |
          set -euo pipefail
          expected=8
          n=$(ctest --preset ${{ matrix.preset }} -L packaging -N \
                | sed -n 's/^Total Tests: //p')
          echo "registered packaging witnesses: ${n:-0} (expected ${expected})"
          if [ "${n:-0}" != "${expected}" ]; then
            echo "::error::packaging tier registered ${n:-0} tests, expected ${expected}."
            echo "::error::Refusing to report this lane green on an unrun gate."
            exit 1
          fi

      # The `ci/` regression pins used to live here, on the linux-gcc-release
      # leg. They now have their own job — see `ci-script-pins` below for why.

      # Wrapped by the #266 instrument for the same reason as the minimal-tier
      # step above, and it is NOT an afterthought: this is the ONLY lane that
      # runs the packaging tier, and the packaging witnesses stage-install a
      # prefix and drive a nested `cmake` + `ninja` build. That is the shape
      # behind the largest single-process transient in #229's local sweep — 0.87
      # GiB at t=9.3 s on `linux-clang-debug`, twelve processes, against a run
      # body that never again exceeded 0.39 GiB. So the lane most likely to hold
      # a memory surprise is the one a `-LE packaging` view of the matrix cannot
      # see at all.
      #
      # ⚠️ It is measured, NOT a widening candidate. #267's phases name asan,
      # ubsan, debug, coverage, the four libc++ lanes and msvc; this lane is in
      # none of them and gains no `execution.jobs`. Nested builds inside ctest
      # are the case where naive test-level parallelism multiplies compile jobs,
      # and nothing here measures that.
      - name: Test (full tier incl. packaging — the SC-011/SC-012 gate)
        id: full_tier_test
        if: matrix.preset == 'linux-gcc-release'
      # `--timeout 1800`: wedge guard, see the `linux` job's first Test step for
      # why it exists and why it must not be tightened.
        run: |
          python3 ci/measure-peak-rss.py \
            --out "/tmp/peak-rss-${{ matrix.preset }}.env" \
            --log "/tmp/ctest-${{ matrix.preset }}.log" \
            --label "${{ matrix.preset }}" \
            -- ctest --preset ${{ matrix.preset }} --output-on-failure --no-tests=error --timeout 1800

      # A second report step rather than widening the one above's `if:`. The two
      # ctest steps are 100 lines apart with the rpmbuild install and the
      # packaging-registration assertion between them, so one combined step would
      # sit nowhere near one of its two subjects. (Tier 3 combines its two
      # because they are adjacent.)
      # Quoted — see the `linux` job's first Peak memory step for why.
      - name: "Peak memory + unexplained-sanitizer gate (packaging tier, #266 / #229 / #267)"
        if: always() && matrix.preset == 'linux-gcc-release'
        # ⚠️ `continue-on-error: true` DELIBERATELY REMOVED (#267 acceptance item 5).
        # It was here because this step is a MEASUREMENT and a lane must not go
        # red over a /proc sampling hiccup — that reasoning still holds and is
        # now enforced inside the script instead: `ci/peak-memory-report.sh`
        # exits 0 on every instrument-failure path, and exits 1 for exactly one
        # reason, an UNEXPLAINED sanitizer report. Leaving this line would have
        # made that `exit 1` inert: GitHub records `outcome: failure` but sets
        # `conclusion: success`, the job continues, and the lane reports green —
        # a gate that observes and never asserts. Restoring it silently reverts
        # the script's fail-closed contract, so do not, without also reverting
        # the script.
        env:
          TEST_OUTCOME: ${{ steps.full_tier_test.outcome || 'unavailable' }}
        run: |
          ci/peak-memory-report.sh \
            "${{ matrix.preset }}" \
            "/tmp/peak-rss-${{ matrix.preset }}.env" \
            "/tmp/ctest-${{ matrix.preset }}.log" \
            "${TEST_OUTCOME}"

      # ── #254 — the Python binding tests, folded in from `python-bindings` ────
      #
      # PLACEMENT is deliberate: after both ctest steps and before the packaging
      # block. (a) the C++ signal is produced before any python step can stop the
      # job, so a python failure never preempts it; (b) a red python leg then
      # uploads no packages, which matches the existing intent below — "runs only
      # after a green test step, so a red lane uploads nothing".
      #
      # TWO STEPS, NOT ONE, and `continue-on-error` off on both. Merging the jobs
      # costs signal granularity (R2): a pytest flake now reds a required C++ leg
      # on a PR that touched no Python. Separately-named steps do not fix the
      # blast radius — they buy ATTRIBUTABILITY, so the failing step is
      # identifiable from the checks UI without opening the job log. That trade
      # was accepted knowingly.
      #
      # The two `if:` guards are mutually exclusive and jointly exhaustive over
      # the six legs by construction of ci/derive-python-sanitizer.sh's table.
      - name: Run Python tests
        if: steps.pysan.outputs.sanitizer == 'none'
        env:
          PYTHONPATH: "${{ github.workspace }}/build/${{ matrix.preset }}/lib"
        run: pytest bindings/python/tests/ -v

      # asan / ubsan / tsan legs: the trampoline/worker path under the sanitizer
      # (FR-013/FR-014 UAF, FR-007 GIL race). Runs the round-trip PLUS the
      # PY-002/PY-003 tests that exercise the worker path — the GIL-release GREEN
      # leg (test_gil_release_canary.py, FR-013/SC-003) and the raising-callback
      # watchdog (test_callback_raise_watchdog.py, FR-011/SC-004) — so the
      # trampoline stays sanitized every run. The canary RED leg
      # (FIXPP_PY_GIL_RELEASE_CANARY) is NOT built here; the deliberate-hang build
      # is local-only.
      #
      # CPython is not instrumented, so the runtime is LD_PRELOADed: subprocess
      # children of the watchdog/canary tests inherit LD_PRELOAD and the *SAN
      # options through the environment. Both clang naming schemes are tried, and
      # ubsan's standalone runtime basename comes from the derive script — it is
      # `ubsan_standalone`, not `ubsan`, the one non-identity row in the table.
      #
      # ⚠️ THE SANITIZER OPTIONS STAY PER-INVOCATION. Do NOT lift san_opts to the
      # job's `env:` block during a future tidy-up: this job also runs the C++
      # ctest suite, and a job-level *SAN_OPTIONS would silently change THAT run
      # too. Nothing set for pytest reaches ctest today, and that is load-bearing.
      #
      # ⚠️ The resolved runtime is asserted to EXIST before pytest. A missing
      # LD_PRELOAD path is silently ignored by the loader on some configurations,
      # and the suite would then run UNINSTRUMENTED and report green — the #251
      # class again (R2-P2-3).
      - name: Run Python tests under sanitizer
        if: steps.pysan.outputs.sanitizer != 'none'
        env:
          PYTHONPATH: "${{ github.workspace }}/build/${{ matrix.preset }}/lib"
        run: |
          set -euo pipefail
          RT_BASE="${{ steps.pysan.outputs.rt_base }}"
          RT=$(clang -print-file-name=libclang_rt.${RT_BASE}-x86_64.so)
          [ -f "$RT" ] || RT="$(clang -print-runtime-dir)/libclang_rt.${RT_BASE}.so"
          echo "${{ steps.pysan.outputs.sanitizer }} runtime: $RT"
          if [ ! -f "$RT" ]; then
            echo "::error::libclang_rt.${RT_BASE} not found under either naming scheme."
            echo "::error::LD_PRELOAD of a missing path is silently ignored — the suite would run"
            echo "::error::UNINSTRUMENTED and report green. Refusing to run it."
            exit 1
          fi
          env ${{ steps.pysan.outputs.san_opts }} LD_PRELOAD="$RT" \
            pytest bindings/python/tests/ -v

      # ── 084 T065 / FR-026: attach the package artifacts ────────────────────
      # Runs only after a green test step, so a red lane uploads nothing.
      #
      # RELEASE-ONLY IN CI (user decision 2026-08-03). Artifacts are attached on
      # the Release lanes only: linux-gcc-release and linux-clang-release here,
      # windows-msvc-release in tier2. No -Debug configuration uploads packages.
      #
      # FR-026 covers every in-scope lane, not just the one that runs the
      # packaging ctest witnesses — that is decision D3, a separate obligation,
      # left untouched above. So this IS a narrowing of FR-026, recorded as such
      # in spec.md's FR-026 banner and tasks.md T065/T066 rather than left
      # implicit.
      #
      # What this does NOT change: which tests run. The packaging ctest tier was
      # already Release-only on both tiers under D3, so no witness stops
      # executing — the Debug legs of FR-019 (.pdb) and T058 were never
      # CI-exercised on a Debug lane. Debug packaging still works locally and was
      # proven there across the six-configuration matrix.
      #
      # An earlier version of this comment justified excluding Debug on disk
      # cost, citing ~3 GB of artifacts per Linux Debug configuration. That
      # figure was wrong by ~30x (measured: 99 MB gcc-debug, 72 MB clang-debug)
      # and is NOT the reason. The reason is the user's decision to keep CI
      # artifacts Release-only.
      - name: Build packages
        if: matrix.preset == 'linux-gcc-release' || matrix.preset == 'linux-clang-release'
        run: cmake --build --preset ${{ matrix.preset }} --target fixpp-package

      # T068 / SC-010: the artifact NAME must identify the matrix cell, or a
      # second lane silently overwrites the first. `matrix.preset` is unique by
      # construction, and the files inside already carry all six FR-017
      # dimensions.
      - name: Upload packages
        if: matrix.preset == 'linux-gcc-release' || matrix.preset == 'linux-clang-release'
        uses: actions/upload-artifact@v7
        with:
          name: packages-${{ matrix.preset }}
          path: ${{ github.workspace }}/_artifacts/*
          if-no-files-found: error
          retention-days: 14

      # ── #254 — disk at JOB END, points 1-4 complete ──────────────────────────
      # Points 1-3 are the `Free up disk space` step's own before/after and the
      # one-liners on Configure and Build. THIS is the operative high-water mark:
      # the consumer install-witness, the packaging tier's sub-project builds and
      # `fixpp-package`'s CPack staging all land after the Build step.
      #
      # ⚠️ THE SPLIT IS THE POINT. The MEASUREMENT asserts; the REPORTING does
      # not. An earlier draft put continue-on-error on the whole thing and then
      # wrote an acceptance criterion saying "a run that reports NOT MEASURED does
      # not discharge this" — with nothing that fails. That criterion was
      # self-waiving. A df/du that cannot run now reds the leg; only the cosmetic
      # summary write is non-fatal.
      #
      # NO FLOOR IS ASSERTED, deliberately. Disk exhaustion is already
      # self-asserting — `ld: final link failed: No space left on device` reds the
      # Build step, which is how it was found in June. This is a TREND instrument,
      # not the detector, and there is no measured floor to assert against: any
      # threshold named here would be invented, and an invented threshold either
      # never fires or fires spuriously. Re-open if job-end free space ever drops
      # below ~2x the tree on any leg.
      - name: Measure disk at job end (#254)
        if: always()
        run: |
          set -euo pipefail
          P="${{ matrix.preset }}"
          if [ ! -d "build/$P" ]; then
            echo "build/$P absent — the job did not reach a configured tree; nothing to measure."
            exit 0
          fi
          df -h "$GITHUB_WORKSPACE" > /tmp/disk.txt || { echo "::error::df failed — instrument unavailable."; exit 1; }
          du -sh "build/$P" >> /tmp/disk.txt || { echo "::error::du on build/$P failed — instrument unavailable."; exit 1; }
          du -h "build/$P/lib/_fixpp.so" >> /tmp/disk.txt 2>/dev/null \
            || echo "_fixpp.so absent (expected only if the build did not reach it)" >> /tmp/disk.txt
          cat /tmp/disk.txt

      - name: Report disk to the job summary (#254)
        if: always()
        continue-on-error: true
        run: cat /tmp/disk.txt >> "$GITHUB_STEP_SUMMARY"

  # ── Coverage job (SOFT — does not fail on threshold; Phase 3) ────────────
  # SOFT GATE: coverage threshold enforcement is disabled until Phase 4 module 1
  # closes per [const §IX.1] and the deliverables note in Step 4.
  coverage:
    name: linux-clang-coverage
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: write  # auto-push-on-miss (push:main / dispatch on main) — coverage owns its tag
      id-token: write  # BoringCache authenticates this trusted validation run through GitHub OIDC
    # Backstop against a hung coverage build; matches the `linux` job's 90 for
    # cold-ccache headroom (previously inherited GitHub's 360-min default).
    # 180->240 (2026-08-02, 083): tracks the `linux` job for the same reason —
    # this job rebuilds the same tree, so 083's header churn busts its ccache by
    # construction, not by coincidence. Measured 130 min on the last green main
    # (run 30483617307) vs tsan's 168, so it is not the tightest leg — but a
    # ~1500-object cold recompile eats most of its 50-min margin, and a job
    # killed at the cap loses its ccache-save step, making a re-run cost a
    # second full cycle rather than fixing anything.
    timeout-minutes: 240
    needs: gate-precheck  # runs in parallel with matrix, after the gate check
    # Skipped on release (see the `linux` job note) — coverage doesn't gate the asset.
    if: needs.gate-precheck.outputs.proceed == 'true' && github.event_name != 'release'
    concurrency:
      group: tier1-${{ github.event.pull_request.number || github.ref }}-coverage
      cancel-in-progress: true

    # #241 — ccache on the coverage lane. Mirrors the `linux` job's env exactly;
    # see that block for the rationale behind each variable. Two coverage-specific
    # notes:
    #
    #  * CCACHE_DIR remains separate from the matrix debug leg for local ccache
    #    state and statistics. BoringCache sets remote-only storage and uses one
    #    shared remote namespace; ccache's command hash keeps coverage objects
    #    distinct because the coverage flags and dependency paths differ.
    #
    #  * hash_dir is left at its default. The preset is Debug, so -g is present,
    #    so ccache hashes the CWD — which is what keeps the absolute
    #    build/linux-clang-coverage/_codegen paths and the coverage mapping's
    #    embedded source paths honest. Do NOT set hash_dir=false here: unlike the
    #    build-dir-rename case in #244, there is no second lane to align with, so
    #    it would buy nothing and put debug-info/coverage path attribution at risk.
    env:
      CMAKE_CXX_COMPILER_LAUNCHER: ccache
      CMAKE_C_COMPILER_LAUNCHER: ccache
      CCACHE_COMPILERCHECK: content
      CCACHE_COMPRESSLEVEL: "5"
      CCACHE_DIR: /tmp/fixpp-ccache-linux-clang-coverage

    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ inputs.source_ref || github.sha }}

      # Heavy build on the ~14 GB free-disk ceiling; reclaim the preinstalled
      # SDKs we don't use (~30 GB). Mirrors the `linux` job's identical step.
      - name: Free up disk space
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: true
          android: true
          dotnet: true
          haskell: true
          large-packages: false
          docker-images: true
          swap-storage: false

      - name: Install Clang 22 (apt.llvm.org) + Ninja
        run: |
          wget -O /tmp/llvm.sh https://apt.llvm.org/llvm.sh
          chmod +x /tmp/llvm.sh
          ci/apt-guard.sh llvm-toolchain -- sudo /tmp/llvm.sh 22 all
          # apt.llvm.org's llvm.sh installs versioned binaries (e.g. clang-22)
          # but does NOT update the unversioned `clang`/`clang++` paths. CMake
          # presets use unversioned `clang++`, which otherwise resolves to the
          # runner image's pre-installed clang-18 (real binary at /usr/bin/clang++,
          # missing C++23 std::expected in libstdc++ headers visible to it).
          # `update-alternatives --install` does NOT overwrite that pre-existing
          # regular file — force a symlink directly. Detect the actually installed
          # major (llvm.sh silently falls back to an earlier major when the
          # requested one isn't yet published for the runner's distro).
          CLANG_BIN=$(ls /usr/bin/clang++-* 2>/dev/null | grep -oE '/usr/bin/clang\+\+-[0-9]+$' | sort -V | tail -1)
          CLANG_VER=$(echo "$CLANG_BIN" | grep -oE '[0-9]+$')
          test -n "$CLANG_VER" || { echo "::error::No clang-* installed by llvm.sh"; exit 1; }
          echo "Force-symlinking clang-$CLANG_VER as default unversioned clang/clang++"
          sudo ln -sf /usr/bin/clang-$CLANG_VER   /usr/bin/clang
          sudo ln -sf /usr/bin/clang++-$CLANG_VER /usr/bin/clang++
          clang++ --version
          ci/apt-guard.sh apt-install -- sudo apt-get install -y --no-install-recommends \
            ninja-build \
            python3-pip \
            pipx

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install Conan + pytest via pipx
        run: |
          pipx install conan
          conan --version
          # pytest is required by the decimal_compare_oracle CTest entry
          # (tests/oracle/decimal_compare_oracle_test.py) which is registered
          # in every preset. ⚠️ This used to read "not just the python-bindings
          # build" — #254 folded that job into these six legs, so the contrast
          # it drew no longer exists and the sentence inverted. pytest is now
          # needed here for TWO independent reasons: that CTest entry, and the
          # bindings' own test suite (bindings/python/tests/).
          # pyyaml is required by the interop_cell_results_schema_check CTest
          # entry (tests/interop/cell_results_schema_check_test.py — 016 T028).
          pip install pytest pyyaml

      - name: Initialize Conan default profile
        run: conan profile detect --force

      - name: Teach Conan about Clang 22
        run: |
          mkdir -p ~/.conan2
          cat > ~/.conan2/settings_user.yml <<'EOF'
          compiler:
            clang:
              version: ["19", "20", "21", "22", "23"]
          EOF

      # #241 — MUST run before "Conan install". The job-level
      # CMAKE_CXX_COMPILER_LAUNCHER=ccache above applies to Conan's own
      # from-source builds too, and `conan install --build=missing` below takes
      # that path whenever the GHCR restore misses. With ccache not yet on PATH
      # the dependency's compiler probe dies with exit 127 — the identical trap
      # recorded at the matrix job's step and on Tier 3 (#177). BoringCache does
      # not install these prerequisites, so the workflow installs the pinned
      # upstream binaries before it starts the native ccache adapter.
      - name: Install ccache and its HTTP storage helper
        env:
          CCACHE_VERSION: "4.14"
          CCACHE_SHA256: c64760b0b85ba86068f4cd162dc42e2dc39c6f46b0cb8c1990dfccbec7a1fed0
          CCACHE_STORAGE_HTTP_VERSION: "0.9"
          CCACHE_STORAGE_HTTP_SHA256: 875dbf6d575d06e4c4492f1ba639beb68530bc23382031a6bacc767cded9f463
        run: |
          ccache_archive="ccache-${CCACHE_VERSION}-linux-x86_64-glibc.tar.gz"
          curl --fail --location --show-error --silent --retry 5 --retry-all-errors --retry-delay 5 \
            "https://github.com/ccache/ccache/releases/download/v${CCACHE_VERSION}/${ccache_archive}" \
            --output "${RUNNER_TEMP}/${ccache_archive}"
          echo "${CCACHE_SHA256}  ${RUNNER_TEMP}/${ccache_archive}" | sha256sum --check
          tar xzf "${RUNNER_TEMP}/${ccache_archive}" -C "${RUNNER_TEMP}"
          sudo install -m 0755 \
            "${RUNNER_TEMP}/ccache-${CCACHE_VERSION}-linux-x86_64-glibc/ccache" \
            /usr/local/bin/ccache

          helper_archive="ccache-storage-http-go-${CCACHE_STORAGE_HTTP_VERSION}-linux-amd64.tar.gz"
          curl --fail --location --show-error --silent --retry 5 --retry-all-errors --retry-delay 5 \
            "https://github.com/ccache/ccache-storage-http-go/releases/download/v${CCACHE_STORAGE_HTTP_VERSION}/${helper_archive}" \
            --output "${RUNNER_TEMP}/${helper_archive}"
          echo "${CCACHE_STORAGE_HTTP_SHA256}  ${RUNNER_TEMP}/${helper_archive}" | sha256sum --check
          tar xzf "${RUNNER_TEMP}/${helper_archive}" -C "${RUNNER_TEMP}"
          sudo install -m 0755 \
            "${RUNNER_TEMP}/ccache-storage-http-go-${CCACHE_STORAGE_HTTP_VERSION}-linux-amd64/ccache-storage-http" \
            /usr/local/bin/ccache-storage-http

      - name: Checkout the BoringCache validation plan
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.0.2
        with:
          ref: boringcache-validation
          path: .boringcache-plan
          sparse-checkout: .boringcache.toml
          sparse-checkout-cone-mode: false
          persist-credentials: false

      - name: Exclude the validation plan from source status
        run: printf '%s\n' '/.boringcache-plan/' >> .git/info/exclude

      - name: Use BoringCache for the shared Tier 1 compiler cache
        uses: boringcache/one@1039999c65011be670f5655e0e48ad556188ab12 # v1.30.4
        with:
          working-directory: .boringcache-plan
          trust-policy: auto
          mode: ccache
          diagnostics: summary
          fail-on-cache-error: true
          save-always: true

      # Conan from pre-seeded GHCR (off the 10 GB pool) — see the matrix-job note.
      - name: Set up oras
        uses: oras-project/setup-oras@v2
      - name: Restore Conan cache from GHCR
        id: conan_restore
        run: ci/restore-conan-cache.sh linux-clang-coverage

      - name: Conan install
        run: |
          conan install . \
            -pr conan/profiles/linux-clang-coverage \
            --build=missing \
            -of build/linux-clang-coverage

      # `|| (workflow_dispatch on main)` — see the matrix job's save step for
      # why (#222).
      - name: Save Conan cache to GHCR (push:main / dispatch on main + miss only)
        if: (github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.conan_restore.outputs.hit == 'false'
        continue-on-error: true
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | oras login ghcr.io -u "${{ github.actor }}" --password-stdin
          ci/seed-conan-cache.sh linux-clang-coverage

      - name: Configure
        run: cmake --preset linux-clang-coverage

      # 069-v44-all-families T017 (FR-008): see the "linux" job's identical
      # step for the durability rationale.
      - name: Verify full v44 coverage preset (FR-008)
        run: |
          value=$(grep '^FIXPP_CODEGEN_V44_FAMILIES:' build/linux-clang-coverage/CMakeCache.txt | cut -d= -f2)
          if [ "$value" != "all" ]; then
            echo "::error::linux-clang-coverage configured FIXPP_CODEGEN_V44_FAMILIES='$value', not 'all'. FR-008 requires at least one CI preset to continuously verify the full 83-builder v44 coverage set."
            exit 1
          fi
          echo "OK: FIXPP_CODEGEN_V44_FAMILIES=all (full v44 coverage verified) on linux-clang-coverage"

      - name: Build
        id: build
        run: cmake --build --preset linux-clang-coverage

      # ⚠️ NEW SURFACE IN #241 — this is the FIRST lane in tier1 to call
      # ci/ccache-stats.sh; the matrix legs do not (yet). It is kept, not
      # reverted with the rest of the #241 probe scaffolding, for two reasons:
      # it is the standing regression REPORTER for the equivalence proof
      # recorded above — it prints the hit rate to the log and Job Summary so
      # a silent fall to ~0% is *visible* rather than merely slow — and it is
      # the direction #248 wants tier1 to move. Reuses the TESTED instrument
      # (ci/test-ccache-scripts.sh — 30 assertions, 10 killed mutants) rather
      # than an inline `run:` block. ⚠️ It does NOT gate on the rate:
      # ci/ccache-stats.sh asserts liveness only, imposes no hit-rate floor,
      # and the "restore HIT with a near-zero rate" warning is gated on a restore
      # disposition this lane cannot supply (argument 2 is '', see below). A
      # warm run at 0% hits is therefore GREEN and silent — deliberately, so
      # the necessarily-cold PR run and first post-merge `main` run (§8 of the
      # record) are not red by design.
      #
      # Argument 2 remains empty because this project-specific reporter expects
      # the output contract of Fixpp's archive restore script. BoringCache's
      # action summary separately reports the preflight disposition and native
      # ccache counters for this run.
      - name: ccache statistics
        run: ci/ccache-stats.sh linux-clang-coverage '' '${{ steps.build.outcome }}'

      # Wrapped by the #266 peak-RSS instrument for the same reason as the matrix
      # legs — see the `Peak memory` step in the `linux` job. The coverage lane
      # is measured but NOT widened in this change: it runs the FULL suite (no
      # `-LE packaging`), so its test count is not comparable to the five lanes
      # above, and #267's acceptance item 4 requires the merged coverage to be
      # shown identical before and after widening rather than assumed from `%p`.
      # Measuring it first is what makes that comparison possible later.
      - name: Test (collect profiles)
        id: coverage_test
        env:
          # %p embeds the PID so each test binary writes its own .profraw file
          # instead of overwriting a shared one (ctest runs multiple binaries).
          LLVM_PROFILE_FILE: "${{ github.workspace }}/build/linux-clang-coverage/profiles/default-%p.profraw"
        run: |
          mkdir -p build/linux-clang-coverage/profiles
          python3 ci/measure-peak-rss.py \
            --out /tmp/peak-rss-linux-clang-coverage.env \
            --log /tmp/ctest-linux-clang-coverage.log \
            --label linux-clang-coverage \
            -- ctest --preset linux-clang-coverage --output-on-failure --timeout 1800

      # Quoted — see the `linux` job's first Peak memory step for why.
      - name: "Peak memory + unexplained-sanitizer gate (#266 / #229 / #267)"
        if: always()
        # ⚠️ `continue-on-error: true` DELIBERATELY REMOVED (#267 acceptance item 5).
        # It was here because this step is a MEASUREMENT and a lane must not go
        # red over a /proc sampling hiccup — that reasoning still holds and is
        # now enforced inside the script instead: `ci/peak-memory-report.sh`
        # exits 0 on every instrument-failure path, and exits 1 for exactly one
        # reason, an UNEXPLAINED sanitizer report. Leaving this line would have
        # made that `exit 1` inert: GitHub records `outcome: failure` but sets
        # `conclusion: success`, the job continues, and the lane reports green —
        # a gate that observes and never asserts. Restoring it silently reverts
        # the script's fail-closed contract, so do not, without also reverting
        # the script.
        env:
          TEST_OUTCOME: ${{ steps.coverage_test.outcome || 'unavailable' }}
        run: |
          ci/peak-memory-report.sh \
            linux-clang-coverage \
            /tmp/peak-rss-linux-clang-coverage.env \
            /tmp/ctest-linux-clang-coverage.log \
            "${TEST_OUTCOME}"

      - name: Merge coverage profiles
        run: |
          llvm-profdata-22 merge -sparse \
            build/linux-clang-coverage/profiles/*.profraw \
            -o build/linux-clang-coverage/coverage.profdata
        # SOFT: profile may be empty at this phase; do not fail
        continue-on-error: true

      - name: Generate LCOV report
        run: |
          # The merged profdata holds counts from ALL test binaries ctest ran.
          # llvm-cov export only attributes coverage to the objects it is given,
          # so enumerate every instrumented binary in bin/ (first one positional,
          # the rest via -object) — not just core+capi, which silently dropped
          # all dictionary/codegen/wire coverage.
          OBJECTS=""
          for b in build/linux-clang-coverage/bin/*; do
            [ -f "$b" ] && [ -x "$b" ] || continue
            [ "$(basename "$b")" = fixpp_core_tests ] && continue
            OBJECTS="$OBJECTS -object $b"
          done
          # Trailing source paths restrict the report to library sources only
          # (include/ + src/) — excludes tests/, bench/, tools/, bindings/,
          # _deps/ and other non-library trees from the Codecov totals.
          llvm-cov-22 export \
            --format=lcov \
            --instr-profile=build/linux-clang-coverage/coverage.profdata \
            build/linux-clang-coverage/bin/fixpp_core_tests \
            $OBJECTS \
            include src \
            > build/linux-clang-coverage/coverage.lcov
        # SOFT: no tests yet produce meaningful coverage; do not fail
        continue-on-error: true

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v7
        with:
          name: coverage-lcov
          path: build/linux-clang-coverage/coverage.lcov
          if-no-files-found: warn  # SOFT: artifact may not exist yet

      - name: Upload to Codecov
        uses: codecov/codecov-action@v6
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: build/linux-clang-coverage/coverage.lcov
          flags: unittests
          disable_search: true   # prevent auto-discovery of .ddi/.modmap CMake artefacts
          fail_ci_if_error: false  # SOFT: keep consistent with other coverage steps

  # ── Python wheel gate (PY-005 / 056) ─────────────────────────────────────
  # ADDITIVE (CI-8): this does NOT touch the python tests on the `linux` matrix
  # legs or the local-only GIL canary — it builds + install-tests the SHIPPED
  # cp312-abi3 wheel. Build ONCE in manylinux_2_28 (python-wheel-build), then
  # matrix-install-test the single artifact on 3.12/3.13/3.14 (python-wheel-test).
  # All cibuildwheel config is in bindings/python/pyproject.toml [tool.cibuildwheel]
  # — the single source shared with the local bindings/python/build-wheel.sh.
  #
  # ⚠️ THIS WHEEL DOES NOT WRAP THE BINARIES BUILT BY THE `linux` MATRIX. It
  # compiles the engine from source inside the manylinux_2_28 container. The
  # bindings built by the four NON-packaging `linux` legs are a TEST VEHICLE
  # that ships nothing (`FIXPP_INSTALL_PYTHON=OFF`).
  #
  # ⚠️ #257: `linux-{clang,gcc}-release` DO ship a Python payload now, into
  # packages-linux-*-release. That payload is a DIFFERENT artifact from this
  # wheel — same source, different compiler, different glibc, no manylinux tag —
  # and mixing them is not supported. This job still owns the only artifact a
  # `pip install` user ever sees.
  # (#254: the vehicle used to be a separate `python-bindings` job; folding it
  # into the matrix changed WHERE the bindings are built. #257 then decided
  # WHETHER two of those legs ship them — see the Configure step.)
  # This wheel and the test-suite legs differ in three dimensions at once:
  #
  #            shipped wheel                 | legs running the python tests
  #   compiler gcc-toolset-14                | clang 22
  #   build    Release                       | Debug (+ asan/ubsan/tsan)
  #   OTel     with_otel=False               | with_otel default True
  #
  # So the artifact users run is exercised only by python-wheel-test's install +
  # import + functional subset. Do not read a green sanitizer leg as coverage of
  # the shipped wheel, and do not read the #257 package payload as coverage of
  # it either — that payload is never install-tested across interpreters.
  # See L-056-4 in spec/behaviors-and-limitations.md.
  python-wheel-build:
    name: python-wheel (build cp312-abi3)
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: write  # #259: publish this lane's ccache to GHCR on push:main / dispatch on main
      id-token: write  # BoringCache authenticates this trusted validation run through GitHub OIDC
    # CI-6: cibuildwheel builds the whole statically-linked engine + static
    # OpenSSL in-container; generous backstop against a hung build wedging the runner.
    timeout-minutes: 180
    needs: gate-precheck       # CI-1
    # Gate-then-relevance: the container wheel build runs on a release
    # (the asset is built there), OR — on a PR — once both gates are satisfied
    # (`proceed`) AND the binding/C-ABI/packaging surface actually changed
    # (`python_touched`). A pure-C++ PR or a PR without gate labels skips it;
    # a merge to main (push) builds it unconditionally.
    #
    # ⚠️ THIS SAID "~7-min" AND WAS WRONG BY ~10x (#259). Measured UNCACHED:
    # 53-74 min across five push:main runs, and 68m46s on PR #269's run — where
    # it was the LAST job in Tier 1 to finish, ~24 min after the next-longest
    # lane, i.e. the critical path. The sibling copy of this claim lived at the
    # `python_touched` filter below and is corrected in the same commit; fixing
    # one and leaving the other is the half-restructure this repo keeps
    # re-paying for (#247's G2). With ccache warm the figure should fall
    # sharply — deliberately not quoted here until a warm run has reported one.
    if: >-
      github.event_name == 'release' ||
      (needs.gate-precheck.outputs.proceed == 'true' &&
       needs.gate-precheck.outputs.python_touched == 'true')
    concurrency:               # CI-7
      group: tier1-${{ github.event.pull_request.number || github.ref }}-python-wheel-build
      cancel-in-progress: true

    # ── ccache for the in-container wheel compile (#259) ──────────────────────
    #
    # These govern the HOST side only — restore, stats and seed. The COMPILE
    # happens inside the manylinux container, which gets its own copies through
    # CIBW_ENVIRONMENT at the build step (a container does not inherit the
    # runner's environment).
    #
    # ⚠️ CORRECTION (#270 Gate B r1, F1 doc): a drifted mount between this
    # $CCACHE_DIR and the container's CIBW_ENVIRONMENT CCACHE_DIR is LOUD, not
    # silent. The host compiles nothing on this lane — every compile edge runs
    # inside the container and reaches the host cache dir only through the
    # bind mount — so if the two spellings disagree, the host's `ccache
    # --print-stats` reads an empty/unshared directory and reports ZERO
    # cacheable calls after a successful build. ci/ccache-stats.sh's liveness
    # assert (the "recorded ZERO cacheable calls … after a SUCCESSFUL build …
    # fatal" branch) fails the step on exactly that shape. (An earlier commit
    # message, fc7a4ae3, described this as "silent and permanent" — that
    # overstated it; the dedup of four spellings into one CCACHE_DIR read is
    # still correct on its own merits, just not for that reason.)
    #
    # ⚠️ This lane CANNOT share a namespace with any matrix leg, and that is a
    # measured fact rather than a precaution. The wheel is built by
    # gcc-toolset-14 (gcc 14.2.1, Red Hat 14.2.1-11) inside manylinux_2_28
    # against EL8 headers, statically linked, with OTel off; `linux-gcc-release`
    # is Ubuntu gcc-13 against Ubuntu headers, shared, with OTel on. Under
    # CCACHE_COMPILERCHECK=content those cannot serve each other's objects even
    # if pointed at one directory — so this lane owns its own cache, seeded from
    # cold once. Unlike #244 part 1 there is no existing lane to borrow from.
    env:
      CCACHE_DIR: /tmp/fixpp-ccache-wheel
      # Matches the Tier 3 legs. ⚠️ A GUESS UNTIL THE FIRST SEED REPORTS: the
      # demand measurement is the archive size seed-ccache.sh prints, and
      # whether the cap binds is `cleanups_this_run` in the stats step — NOT the
      # hit rate. The two Release matrix lanes store 96 and 120 MiB, so 2G is
      # expected to be generous here; that expectation is not evidence.
      CCACHE_MAXSIZE: 2G
      CCACHE_COMPILERCHECK: content
      CCACHE_COMPRESSLEVEL: "5"

    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ inputs.source_ref || github.sha }}

      # cibuildwheel builds the whole engine + static OpenSSL in-container onto the
      # host disk; reclaim the preinstalled SDKs we don't use (~30 GB) for margin.
      - name: Free up disk space
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: true
          android: true
          dotnet: true
          haskell: true
          large-packages: false
          docker-images: true
          swap-storage: false

      # NBC-1 (T017): the C-ABI surface is byte-frozen (0->1 GA freeze). Pure-text,
      # so it runs first and fails fast — no point building a wheel if the frozen
      # C ABI drifted. Plus the existing occupancy gate (mirrors check-layers).
      - name: C-ABI byte-freeze gate (NBC-1)
        run: bash tools/check_capi_freeze.sh
      - name: C-ABI occupancy gate
        run: bash tools/check_capi_occupancy.sh

      # REL-2 / SC-005: assert NO PyPI/index-publish step exists anywhere in the
      # workflows. `gh release upload` (REL-1 below) attaches a release ASSET — it
      # does NOT publish to an index. Patterns are ANCHORED to real step syntax (a
      # line-leading pypi-action ref, or a twine-based publish command) so this
      # assertion never matches its OWN source line — the pgrep-self-match class
      # of false positive (worded here to avoid the matched token sequences).
      - name: Assert no PyPI publish step (REL-2)
        run: |
          if grep -rnE '^[[:space:]]*uses:[[:space:]]+\S*pypi|twine[[:space:]]+upload|python[[:space:]]+-m[[:space:]]+twine' .github/workflows/; then
            echo "::error::a PyPI/index publish step exists — REL-2/SC-005 forbids publishing to a package index"
            exit 1
          fi
          echo "REL-2: no PyPI/index publish step present."

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install cibuildwheel + audit tooling
        run: |
          pipx install cibuildwheel
          pipx install auditwheel
          pipx install abi3audit
          # Diagnostic: print installed tool versions. (cibuildwheel 4.x argparse
          # has no --version flag — `pipx list` is the version-agnostic check.)
          pipx list --short

      # ── ccache identity, resolved ONCE (#259) ─────────────────────────────
      #
      # Three consumers need the same two strings — restore, seed, and the
      # pinned-image assertion. Spelling them out three times is the drift
      # ci/ccache-cache-key.sh's opening note is about: a lane string the pull
      # side and the publish side compute DIFFERENTLY is a permanent MISS, and a
      # permanent miss is indistinguishable from "ccache didn't help", which is
      # the conclusion this whole change exists to overturn. One step, one read
      # of the tracked pin in bindings/python/pyproject.toml.
      - name: Resolve wheel ccache identity
        id: wheel_ident
        run: ci/wheel-ccache-ident.sh >> "$GITHUB_OUTPUT"

      # The SAME pinned ccache the container gets (ci/install-ccache.sh). Host
      # and container share one bind-mounted CCACHE_DIR, so running different
      # versions across it risks a host-side read that cannot parse a
      # container-written cache — which would report zero cacheable calls and
      # read exactly like "the cache didn't help". Equal by construction.
      - name: Install ccache (host side)
        run: sudo ci/install-ccache.sh /usr/local/bin

      - name: Checkout the BoringCache validation plan
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.0.2
        with:
          ref: boringcache-validation
          path: .boringcache-plan
          sparse-checkout: .boringcache.toml
          sparse-checkout-cone-mode: false
          persist-credentials: false

      - name: Exclude the validation plan from source status
        run: printf '%s\n' '/.boringcache-plan/' >> .git/info/exclude

      - name: Restore the wheel ccache from BoringCache
        id: ccache_restore
        uses: boringcache/one@1039999c65011be670f5655e0e48ad556188ab12 # v1.30.4
        with:
          working-directory: .boringcache-plan
          trust-policy: auto
          mode: archive
          cache-profiles: wheel-ccache
          diagnostics: summary
          fail-on-cache-error: true
          save-always: true

      # CI-3: persist the IN-CONTAINER Conan cache. cibw-before-all.sh runs
      # `conan install` inside the manylinux container, whose home is separate
      # from the host. Mount a host dir as the container's CONAN_HOME and cache
      # it across runs. Non-load-bearing: a cache miss just means conan
      # --build=missing rebuilds (slower) — the build still succeeds.
      - name: Cache Conan packages (CI-3)
        uses: actions/cache@v5
        with:
          path: /tmp/wheel-conan2
          key: conan-wheel-manylinux228-${{ hashFiles('conanfile.py', 'bindings/python/cibw-before-all.sh', 'bindings/python/pyproject.toml') }}
          restore-keys: |
            conan-wheel-manylinux228-

      - name: Build the single cp312-abi3 wheel (CI-2)
        id: wheel_build
        env:
          # CI-3: route the in-container Conan home to the host-mounted, cached dir.
          # #259 adds a SECOND mount for the ccache directory, the same mechanism
          # the Conan home already proved.
          CIBW_CONTAINER_ENGINE: "docker; create_args: -v /tmp/wheel-conan2:/host-conan2 -v ${{ env.CCACHE_DIR }}:/host-ccache"
          # ⚠️ ONE KEY, SPACE-SEPARATED — do NOT add a second `CIBW_ENVIRONMENT:`.
          # YAML keeps only the last duplicate key, so a second one would
          # silently drop CONAN_HOME and send the dependency closure back to a
          # from-source rebuild on every run, while looking like an addition.
          #
          # A container does not inherit the runner's environment, so the ccache
          # settings from the job-level `env:` block have to be restated here for
          # the side that actually compiles. CCACHE_DIR points at the MOUNT
          # POINT, not the host path.
          CIBW_ENVIRONMENT: "CONAN_HOME=/host-conan2 CCACHE_DIR=/host-ccache CCACHE_MAXSIZE=2G CCACHE_COMPILERCHECK=content CCACHE_COMPRESSLEVEL=5"
        run: |
          mkdir -p /tmp/wheel-conan2
          # The bind-mount source must exist before the container is created, or
          # docker creates it root-owned and the restore's contents are not
          # there. restore-ccache.sh already mkdir's it; this is belt-and-braces
          # for the case where that step was skipped.
          mkdir -p "$CCACHE_DIR"
          # A fresh CI checkout has NO build/ tree, so the local worktree wrapper
          # (build-wheel.sh — which exists only to dodge cibuildwheel's blind
          # `tar -c .` of cwd dragging in the multi-GB local build/) is
          # unnecessary here. Run cibuildwheel directly from the repo root
          # (pyproject's cmake.source-dir=../.. requires cwd=root).
          #
          # Output is teed so the next step can assert WHICH image was used.
          # `pipefail` is on (Actions runs `bash -e {0}`… but NOT with pipefail),
          # so set it explicitly — without it a cibuildwheel failure would be
          # masked by tee's exit 0 and the lane would go green on a failed build.
          set -o pipefail
          cibuildwheel --platform linux --output-dir wheelhouse bindings/python 2>&1 | tee /tmp/cibuildwheel.log
          ls -l wheelhouse/

      # ── ASSERT THE PIN TOOK EFFECT (#259) ─────────────────────────────────
      #
      # A pin that is silently ignored is worse than no pin here: the ccache tag
      # is derived from the pinned digest, so an ignored pin keys this lane's
      # cache to a toolchain that is NOT the one compiling — a permanent,
      # invisible mismatch that still reports hits.
      #
      # ⚠️ THE LOGIC IS IN ci/assert-wheel-image.sh, NOT INLINE HERE. Workflow-
      # embedded shell has no harness (#248), and this repo has paid repeatedly
      # for verification greps that could never go RED. In a script it is driven
      # by ci/test-ccache-scripts.sh through BOTH outcomes — a real match and a
      # deliberately wrong pin — so a green tick here means something.
      - name: Assert the pinned manylinux image is the one used
        if: always() && steps.wheel_build.outcome != 'skipped'
        run: |
          ci/assert-wheel-image.sh /tmp/cibuildwheel.log \
            '${{ steps.wheel_ident.outputs.image_ref }}' \
            '${{ steps.wheel_build.outcome }}'

      # ── Reclaim ownership of the bind-mounted cache dir (#259) ────────────
      #
      # The container runs as ROOT (manylinux needs it for dnf), so everything
      # it wrote through the mount is root-owned. The two steps that follow run
      # as the runner user: `ccache --print-stats` reads the directory, and the
      # seed tars it.
      #
      # ⚠️ WITHOUT THIS THE FAILURE IS EFFECTIVELY SILENT. seed-ccache.sh does
      # report a tar failure — but the Save step is `continue-on-error: true`
      # (correctly: a cache that cannot publish must not redden a lane whose
      # wheel built and tested fine), so the job would stay GREEN while nothing
      # was ever published. The cache would appear installed and do nothing,
      # every run, which is the exact shape #259 exists to end.
      - name: Reclaim ownership of the ccache dir
        if: always()
        run: sudo chown -R "$(id -u):$(id -g)" "$CCACHE_DIR" || true

      # ── ccache statistics + liveness (#259) ───────────────────────────────
      #
      # ── HIT FLOOR NOW ON (#299) ───────────────────────────────────────────
      #
      # This is the follow-up the previous comment here asked for. That comment
      # withheld the floor because turning it on is a claim that the lane HAS a
      # warm baseline, and at the time this lane had never had a warm run. It
      # has since, so #259's acceptance criterion 1 is now machine-checked
      # instead of being prose nobody enforces.
      #
      # ⚠️ THE FLOOR IS SET BELOW THE OBSERVED WARM RATE WITH MARGIN, NOT AT IT.
      # A floor level with the baseline is a flake generator: a large refactor
      # or a codegen change legitimately depresses a warm rate without anything
      # being wrong. This budget is sized to catch the PATHOLOGICAL signature
      # #299 names — a cache that RESTORES but no longer matches, from a
      # compiler bump inside an image, a flag that moved the command line, or a
      # namespace key that stopped colliding — which lands near 0 %, nowhere
      # near this bound. It is a BUDGET, not a measurement; re-deriving the
      # current warm rate and tightening toward it would convert a bound into a
      # flake.
      #
      # ⚠️ THE EXEMPTION IS WHAT MAKES THE FLOOR SAFE, and it lives in
      # ci/ccache-stats.sh, gated on `RESTORE == 'true'`. A seeding run (restore
      # MISS) is legitimately 0 % and stays green by construction — it cannot
      # reach the fatal branch. That is the original correct reasoning
      # preserved, not discarded.
      #
      # What IS asserted today is liveness: zero cacheable calls after a
      # SUCCESSFUL build is fatal, because a silently unwired launcher reports
      # the lane green while measuring nothing.
      - name: ccache statistics
        id: ccache_stats
        if: always()
        run: |
          ci/ccache-stats.sh \
            "${{ steps.wheel_ident.outputs.lane }}" \
            "${{ steps.ccache_restore.outputs.cache-hit }}" \
            "${{ steps.wheel_build.outcome }}" \
            70

      # T011 / SC-004 / LAY-3: exactly ONE wheel, tagged cp312-abi3 + manylinux,
      # external-library list EMPTY (static everything).
      - name: Verify tag + self-containment (SC-004, LAY-3)
        run: |
          set -euo pipefail
          test "$(ls wheelhouse/fixpp-*.whl | wc -l)" -eq 1
          test "$(ls wheelhouse/fixpp-*-cp312-abi3-manylinux_2_28_x86_64.whl | wc -l)" -eq 1
          # RC#4 regression guard: repair must emit a SINGLE 2_28 tag, not a compressed
          # manylinux_2_XX_x86_64.manylinux_2_28_x86_64 tag.  If --only-plat ever stops
          # taking effect the exact-match count above goes RED immediately; this guard
          # makes the regression class explicit (recorded shape: manylinux_2_26_x86_64.manylinux_2_28_x86_64).
          if ls wheelhouse/fixpp-*-cp312-abi3-manylinux_*_x86_64.manylinux_2_28_x86_64.whl >/dev/null 2>&1; then
            echo "FAIL SC-004: compressed platform tag emitted; expected single manylinux_2_28_x86_64"; exit 1
          fi
          WHL=$(ls wheelhouse/fixpp-*-cp312-abi3-manylinux_2_28_x86_64.whl)
          echo "wheel: $WHL"
          auditwheel show "$WHL"
          # FR-002: the external-library list must be EMPTY (static everything).
          # Inspect the extension module(s) directly — format-independent.
          WHL_TMP=$(mktemp -d)
          unzip -q "$WHL" -d "$WHL_TMP"
          SOS=$(find "$WHL_TMP" -name '_fixpp*.so')
          test -n "$SOS" || { echo "FAIL FR-002: no _fixpp*.so found in wheel"; exit 1; }
          NEEDED=$(for so in $SOS; do readelf -d "$so"; done \
            | awk '/\(NEEDED\)/{gsub(/[][]/,"",$NF);print $NF}' | sort -u)
          echo "extension NEEDED entries:"; echo "${NEEDED:-(none)}"
          # Baseline: libc, libm, libdl, libpthread, librt, ld-linux-x86-64.
          # libgcc_s, libstdc++, libssl, libcrypto, libfixpp*, OTel libs are banned.
          BAD=$(echo "$NEEDED" | grep -v '^$' | grep -Ev \
            '^(libc|libm|libdl|libpthread|librt|ld-linux-x86-64)\.so' || true)
          if [ -n "$BAD" ]; then
            echo "FAIL FR-002: wheel has non-baseline external dependencies:"
            echo "$BAD"
            exit 1
          fi
          rm -rf "$WHL_TMP"
          abi3audit --strict "$WHL"

      # #255: the step above verifies properties of the EXTENSION (tag, NEEDED
      # set, abi3 conformance). None of them reads the wheel's file list, which
      # is how the entire C++ install tree — archives, loose objects, the CMake
      # package config, a second unreachable copy of every dictionary — shipped
      # inside the wheel while all of the above stayed green. This asserts the
      # payload itself, in both directions; see the script header for the two
      # mutations that force each half RED.
      - name: Verify wheel payload (#255)
        run: |
          set -euo pipefail
          WHL=$(ls wheelhouse/fixpp-*-cp312-abi3-manylinux_2_28_x86_64.whl)
          ci/check-wheel-payload.sh "$WHL"

      - uses: actions/upload-artifact@v7
        with:
          name: fixpp-wheel
          path: wheelhouse/fixpp-*-cp312-abi3-manylinux_2_28_x86_64.whl
          if-no-files-found: error

  # CI-4/CI-5: install-test the ONE built wheel on each targeted interpreter —
  # the cross-version import is the abi3 feasibility witness. No rebuild per
  # interpreter (abi3: one wheel serves all). Red on any install/test failure.
  python-wheel-test:
    name: python-wheel (install-test ${{ matrix.python }})
    runs-on: ubuntu-24.04
    timeout-minutes: 20        # CI-6
    needs: python-wheel-build
    strategy:
      fail-fast: false
      matrix:
        python: ["3.12", "3.13", "3.14"]
    concurrency:               # CI-7
      group: tier1-${{ github.event.pull_request.number || github.ref }}-python-wheel-test-${{ matrix.python }}
      cancel-in-progress: true

    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ inputs.source_ref || github.sha }}

      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python }}

      - uses: actions/download-artifact@v8
        with:
          name: fixpp-wheel
          path: wheelhouse

      - name: Install the abi3 wheel + run the functional subset (CI-4)
        run: |
          set -euo pipefail
          WHL=$(ls wheelhouse/fixpp-*-cp312-abi3-manylinux_2_28_x86_64.whl)
          python -m venv /tmp/wt
          /tmp/wt/bin/pip install -q "$WHL" pytest
          # Out-of-repo, PYTHONPATH scrubbed (quickstart §4): the dedicated
          # tests/wheel/ suite imports only installed modules and resolves
          # dictionaries through fixpp.dictionary_path — assert fixpp loads from
          # the venv site-packages so no source tree shadows the locator.
          WORK=$(mktemp -d)
          cp -r bindings/python/tests/wheel "$WORK/wheel_tests"
          cd "$WORK"
          env -u PYTHONPATH /tmp/wt/bin/python -c \
            "import fixpp, os, sys; assert os.path.realpath(fixpp.__file__).startswith(sys.prefix), fixpp.__file__"
          env -u PYTHONPATH /tmp/wt/bin/python -m pytest -q "$WORK/wheel_tests"

      # SC-006 / FR-009 (T019): a deliberately broken wheel (FIX44.xml stripped)
      # must flip the gate red. Self-discriminating (proves the intact wheel
      # round-trip passes first). CI-5: no continue-on-error.
      # Runs on the first matrix leg only: the assertion is interpreter-independent
      # (strip the bundled dict -> locator fails), so one leg fully exercises SC-006;
      # `job-index == 0` (not a pinned version) keeps it firing if the list changes.
      - name: Broken-wheel negative gate (SC-006)
        if: ${{ strategy.job-index == 0 }}
        run: |
          WHL=$(ls wheelhouse/fixpp-*-cp312-abi3-manylinux_2_28_x86_64.whl)
          PYTHON=python3 bash bindings/python/tests/wheel/test_broken_wheel_gate.sh "$WHL"

  # REL-1/3 (T020): on a published release, attach the single cp312-abi3 wheel as
  # a release ASSET (NOT a PyPI upload — REL-2/SC-005). Only after the build +
  # full install-test matrix are green on the release commit.
  python-wheel-release:
    name: python-wheel (release attach)
    runs-on: ubuntu-24.04
    needs: [python-wheel-build, python-wheel-test]
    if: github.event_name == 'release'
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          name: fixpp-wheel
          path: wheelhouse

      - name: Attach the cp312-abi3 wheel to the GitHub release (REL-1/3)
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          WHL=$(ls wheelhouse/fixpp-*-cp312-abi3-manylinux_2_28_x86_64.whl)
          echo "attaching $WHL to release ${{ github.event.release.tag_name }}"
          gh release upload "${{ github.event.release.tag_name }}" "$WHL" --clobber \
            --repo "${{ github.repository }}"

  # ── Architecture layer check ─────────────────────────────────────────────
  # ── ci/ shell regression pins — THE ONLY TIER 1 JOB WITH PRE-MERGE SIGNAL ───
  #
  # `ci/test-restore-conan-cache.sh` (#245) and `ci/test-ccache-scripts.sh`
  # (#240) drive the real restore/seed/prune/stats scripts through every
  # disposition against shimmed `oras`, `conan`, `ccache`, `gh` and compiler
  # binaries. Their true dependencies are bash, jq and coreutils: no build, no
  # network, no toolchain. Measured at ~1.5 s and ~1 s.
  #
  # `ci/test-python-install-witness.sh` (#254) is the third, and it is the one
  # that widens the job's dependency set: it needs `cmake` on PATH (preinstalled
  # on ubuntu-24.04) to configure eight throwaway `project(NONE)` fixtures and
  # drive the real install witness over each. Still no compiler, no Conan, no
  # network, no Python — ~1 s for all eight cells.
  #
  # ⚠️ ITS OWN JOB, AND DELIBERATELY *NOT* GATED ON `gate-precheck`.
  #
  # Both pins previously ran as steps on the `linux-gcc-release` matrix leg,
  # which was wrong twice over. First, a shell-only harness then executed only
  # after a full Conan install + configure + C++ build + test of that leg
  # SUCCEEDED — so any unrelated red above it (a compile error, a flaky test, an
  # apt hiccup) meant the pin never ran, with no signal that it had not. Second,
  # the matrix is gated on `proceed`, i.e. it skips until BOTH gate labels land,
  # so both pins carried a comment conceding they "yield zero pre-merge signal"
  # — an accepted limitation that was never actually necessary.
  #
  # Ungated and buildless, they run on every pull_request from the first push.
  # That converts the conceded limitation into real pre-merge signal for the
  # `ci/` scripts, which is the one part of this repo's CI that no other job
  # exercises before merge.
  #
  # It IS in tier1-required's needs. A gate that runs but cannot block is the
  # observe-don't-assert shape this repo has a recorded lesson about
  # (feedback_ci_gate_observes_not_asserts_witness_skips_into_green, cited at
  # that job). Safe to make blocking BECAUSE it is fast and near-hermetic —
  # but "near": the two original pins (#245, #240) are bash+jq and hermetic,
  # nothing here can flake for THEM. The #251 pin below additionally requires
  # `actions/setup-python` and may bootstrap PyYAML from PyPI — a live
  # network dependency, gate-b/r2 finding 3. It fails closed (a failed
  # install fails this step, hence `ci-script-pins`, hence
  # `tier1-required`) — never a silent skip — but it is not "nothing here can
  # flake" for the job as a whole any more.
  ci-script-pins:
    name: ci-script-pins
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ inputs.source_ref || github.sha }}

      - name: Regression pin — conan-cache restore dispositions (#245)
        run: bash ci/test-restore-conan-cache.sh

      - name: Regression pin — ccache GHCR script dispositions (#240)
        run: bash ci/test-ccache-scripts.sh

      # The ctest cells in bindings/python/CMakeLists.txt only ever exercise the
      # layout the local tree produces; they cannot show which BROKEN layouts the
      # witness would certify. Gate B round 2 measured three it did, and this
      # harness measured a fourth (`_fixpp_data` installed outside the module's
      # directory). Buildless — see the job comment above for the `cmake`
      # dependency this step adds.
      - name: Regression pin — python install witness escapes (#254)
        run: bash ci/test-python-install-witness.sh

      # ci/check-wheel-payload.sh gates what the shipped wheel CONTAINS, and it
      # runs only inside python-wheel-build — behind `proceed`, behind the PY_RE
      # path filter, and after a manylinux container build. Its own header could
      # therefore offer only one way to prove it can report non-zero: "rebuild
      # the wheel", tens of minutes, which nobody will ever run. That is the
      # instrument-with-no-affordable-proof shape, so the arms live here instead
      # as zip fixtures: bash + python3, no build, no network, ~1 s.
      #
      # ⚠️ Every RED cell asserts its OWN diagnostic, not merely a non-zero exit.
      # That distinction was not cosmetic here: two cells initially reddened via
      # a DIFFERENT check than the one they were written for, and stayed green
      # under a mutation that reverted the rule they were supposed to pin.
      - name: Regression pin — wheel payload witness escapes (#255)
        run: bash ci/test-check-wheel-payload.sh

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      # Parses tier1.yml with PyYAML, so the #243/#251 sanitizer->profile
      # policy and the anchored PY_RE relevance filter get pre-merge signal
      # the same way the two pins above do (Article VII §§3-4; #247->#248's
      # location waiver does not transfer here — this is tracked YAML data
      # plus a regex, not shell embedded in a `run:` block with no harness to
      # host it). The pin itself reads tracked files only and executes no
      # build, no Conan and no workflow — but this step also bootstraps
      # PyYAML from PyPI below when the interpreter on PATH lacks it, which
      # is NOT hermetic and its frequency is unmeasured (gate-b/r2 finding 3;
      # see the job-level comment above).
      - name: Regression pin — tier1.yml python policy (#251, re-based #254)
        run: |
          python3 -c "import yaml" 2>/dev/null || pip install --quiet pyyaml
          bash ci/test-tier1-python-policy.sh

      # #266 replaced a peak-memory probe that ran 8 times and measured nothing
      # while exiting 0 every time. Its replacement gets the pre-merge signal the
      # original never had: 54 cells plus 18 mutants, each mutant proven to redden
      # the ONE cell that names its defect. Synthetic fixtures only — no build
      # tree, no ctest.
      #
      # ⚠️ THIS JOB IS NOW LOAD-BEARING FOR A GATE, not just for a measurement.
      # Since #267 acceptance item 5, ci/peak-memory-report.sh EXITS 1 on an
      # unexplained sanitizer report and the calling steps no longer carry
      # `continue-on-error`. T19a-g here are the only pins on that exit code —
      # every other cell asserts on TEXT, which is what a human reads, while the
      # exit code is what GitHub reads. Weakening this job weakens that gate.
      #
      # ⚠️ IT IS NOT FREE: measured 117 s locally, against the ~2.5 s this whole
      # job used to take. The cost is the mutants — each re-runs the full cell
      # suite against a broken copy, and the cells that matter need REAL resident
      # memory (a few hundred MiB touched, held for 1-2 s) because RSS is what is
      # being measured and a mocked workload would pin nothing. Stated rather
      # than buried: this job used to be the cheap one and is not any more.
      #
      # It does NOT move the critical path: `ci-script-pins` is in
      # `tier1-required`'s `needs:` but runs concurrently with the linux matrix
      # (40+ min) and coverage (33 min warm), so ~2 min is absorbed. Recorded as
      # the reason the cost was accepted, not as a claim that it is free.
      - name: Regression pin — peak-RSS instrument dispositions (#266)
        run: bash ci/test-peak-rss.sh

      # #300 — the install bound. Every Linux lane in every tier installs its
      # toolchain through ci/apt-guard.sh; a wedged mirror used to burn the
      # 180-240 min JOB timeout with nothing red and nothing naming apt.
      #
      # ⚠️ A BOUND THAT HAS NEVER BEEN SEEN TO FIRE IS NOT KNOWN TO BE ABLE TO.
      # The cells here drive synthetic hang/flake/failure fixtures, and the
      # three mutants re-run the whole suite against a copy of apt-guard.sh with
      # exactly one property broken, each proven to redden the ONE cell that
      # names its defect. Two of those cells are CLOCK assertions — the bound is
      # a time claim and cannot be read off an exit code.
      #
      # No apt, no network, no sudo: every fixture is synthetic.
      #
      # ⚠️ COST, STATED RATHER THAN BURIED (same convention as the peak-RSS step
      # above): measured ~47 s locally. It is dominated by the clock cells, whose
      # fixtures must really sleep — a mocked hang would pin nothing, because the
      # property under test IS elapsed time.
      #
      # It was ~120 s before the cell filter: each mutant re-ran the WHOLE suite,
      # and two of the three disable the bound, so unrelated cells fell through to
      # their fixtures' full sleep. A mutant now runs only the cell it claims to
      # pin — which is also the more honest scoring. Re-derive with
      # `time bash ci/test-apt-guard.sh` rather than trusting this number.
      - name: Regression pin — apt/llvm install bound (#300)
        run: bash ci/test-apt-guard.sh

      # #298 — the tests/wheel <-> in-tree parity contract. conftest.py and
      # tests/wheel/README.md both describe tests/wheel/ as a byte-faithful fork
      # with an enumerated membership, and nothing enforced any of it: the
      # convention was held by hand and had already drifted in two directions.
      #
      # The ALLOWLIST IS THE README, not a second list maintained beside it —
      # otherwise two artifacts would each claim to be the contract.
      #
      # ⚠️ #298's own warning is why the harness exists rather than just the
      # check: "a parity gate that silently passes is worse than none, because
      # it converts an unenforced convention into a falsely enforced one." Each
      # of the gate's directions is broken against a copy of the real suite and
      # asserted to produce ITS OWN message — a cell that fails for the wrong
      # reason is not a passing cell.
      - name: Regression pin — wheel/in-tree test parity (#298)
        run: bash ci/test-wheel-parity.sh

      # The CALLER-side invariants for #300 and #213 — the half neither
      # mechanism's own harness can see.
      #
      # ci/test-apt-guard.sh tests the WRAPPER; a bare `sudo apt-get install`
      # added to any workflow leaves every one of its cells green while "every
      # apt-backed install is bounded" quietly becomes false. And every fuzz
      # replay lives under `if(FIXPP_BUILD_FUZZ)`, so flipping that flag OFF does
      # not trip the zero-registration guard — it stops the guard being evaluated
      # at all, returning the lane to replaying zero seeds with every script gate
      # still green. A guard cannot guard its own enabling flag.
      #
      # Both invariants were true when written and pinned by nothing; a hostile
      # review named that as the defect. Synthetic copies only, no build.
      # ⚠️ INSTALLS ITS OWN PyYAML, and that line is not decoration. The checker
      # now EXITS 2 rather than warning when PyYAML is missing — a `::warning::`
      # does not fail a job, so the old behaviour printed "all invariants hold"
      # over a tree whose campaign workflow could have been push:-triggered.
      # Making it fail closed is only safe if the dependency is this step's own:
      # PyYAML reached this job as a SIDE EFFECT of an unrelated earlier pin
      # (#251's, which pip-installs it), so reordering or retiring that step
      # would have reddened this one for a reason naming neither. Same idiom as
      # the two steps that already do it; `pip install` is a no-op when present.
      - name: Regression pin — CI lane policy (#300 callers, #213 fuzz lane, #267 campaign trigger)
        run: |
          python3 -c "import yaml" 2>/dev/null || pip install --quiet pyyaml
          bash ci/test-ci-lane-policy.sh

      # #267 — the A-B-A parallelism verdict's own gates, over a SYNTHETIC
      # sample: every way the verdict can reject one is proven to fire, and each
      # cell requires its NAMED diagnostic, not merely a non-zero exit. The
      # harness asserts its OWN cell count against a declared constant and
      # refuses to report a result on a mismatch, so a cell lost to an editing
      # slip cannot pass as a smaller green run. That is why no number is
      # written here: the count re-derives itself, and a copy of it would not. It exists because `ci/ctest-parallelism-probe.md` records three
      # `execution.jobs` conclusions that were published and then WITHDRAWN —
      # this is the question the repo has already got wrong three times, by
      # reading numbers no checker had been proven able to reject.
      #
      # ⚠️ The cell worth knowing about is T4. If `--parallel N` ever stops
      # taking effect, all three passes run identically, the two serial passes
      # agree PERFECTLY, and the apparatus reports a clean sample saying "no
      # gain on this lane" having tested one configuration three times. The
      # A-vs-A' agreement check is a forced-MISS arm and cannot catch it; the
      # in-flight oracle T4 pins is the other half.
      #
      # Buildless and hermetic — python3 + coreutils, no compiler, no network.
      # Its cost grows with the cell count; re-derive rather than assume:
      # `time ci/test-parallelism-verdict.sh`.
      - name: Regression pin — parallelism verdict gates (#267)
        run: bash ci/test-parallelism-verdict.sh

      # #267 — the seam the harness above CANNOT see. That one varies the
      # CONTENT of a sample the verdict is known to parse; this one runs the
      # REAL driver against a 12-test synthetic ctest project and feeds its REAL
      # output to the REAL verdict. A field renamed on one side of that seam
      # leaves every one of those cells green and returns "0 of 3 passes" from every actual
      # campaign — at a cost of three suite runs on a runner to find out.
      #
      # Needs cmake + ctest (preinstalled on ubuntu-24.04) and REFUSES to run
      # without them rather than skipping: a skipped seam check is a silent pass.
      #
      # ⚠️ THE COST CONDITION, not a number — a figure written here would go
      # stale silently and nothing would ever re-run it. It runs the driver
      # TWICE (once per platform branch), so its wall time is dominated by
      # 2 x (2 serial + 1 parallel) ctest passes over a 12-test synthetic
      # project plus 8 machine-witness calls. Both are deliberately shrunk —
      # 0.4 s sleeps, and PARALLELISM_WITNESS_{ITERS,REPEATS} set by the harness
      # — because this job runs on EVERY push. Re-derive before assuming
      # anything about it: `time ci/test-parallelism-aba-seam.sh`. If that grows
      # past roughly a minute, shrink the sleeps rather than the cell count:
      # TWELVE tests is what makes `--parallel 4` unambiguously distinguishable
      # from 1 in the in-flight oracle, which is the assertion S2 exists for.
      - name: Regression pin — A-B-A driver/verdict seam (#267)
        run: bash ci/test-parallelism-aba-seam.sh

      # #209 — the bench gate's own harness. 61 cells over synthetic
      # Google-Benchmark JSON: every hard cell of tools/bench_compare.py and
      # ci/run-bench-suite.sh is proven RED for its own named reason, and the
      # GREEN controls pin shapes that must PASS (notably a `stddev` aggregate
      # of exactly 0.0, which an over-eager "every timing must be > 0" rule
      # would have rejected on a correct tree, and a candidate ADDING a
      # benchmark or a paired manifest row, which Article VIII §3 requires).
      #
      # It earned its place immediately: cell R-SILENT caught a real defect in
      # ci/run-bench-suite.sh — a stale results file from an earlier invocation
      # satisfied the "binary wrote no results" check, so a benchmark that
      # silently stopped emitting would have read as a clean measurement.
      #
      # Buildless and hermetic — python3 + coreutils, no compiler, no benchmark
      # binaries, no network. ~1 s.
      - name: Regression pin — bench gate dispositions (#209)
        run: bash ci/test-bench-gate.sh

      # #252's checker is the only thing standing between "the sanitizer legs are
      # green" and "the sanitizer legs can see the C half of their closure", so it
      # gets the same treatment as the instruments above: every property driven by
      # a MUTANT that violates it, each proven to redden the one case that names
      # it. Fixtures are REAL archives built with `cc -c -fsanitize=<x>` + `ar`
      # (compile-only — no sanitizer runtime needed) and read with the REAL `nm`;
      # a canned-output double would pin this harness's belief about what an
      # instrumented archive looks like, which is the belief under test.
      #
      # Placed after the python-policy pin because T2 derives its preset census
      # from tier1.yml AND tier3-libcxx.yml with PyYAML, and that step is where
      # the bootstrap lives. The guard is repeated rather than assumed: a
      # reordering that leaves this step first must not turn the census silently
      # empty — and T2 fails loud on an empty census either way.
      - name: Regression pin — sanitized-dependency checker (#252)
        run: |
          python3 -c "import yaml" 2>/dev/null || pip install --quiet pyyaml
          bash ci/test-sanitized-deps.sh

      # #289's migration census. Pins the EXACT set (not a count, and not a
      # floor) of `.run_for(...)` sites followed within six source lines by a
      # `.get(...)` across tests/**/*.{cpp,hpp} — the lexical population the
      # migration is retiring one PR at a time. A count alone would pass a
      # fixture that lost one site and gained a different one; exact-set
      # equality catches additions, removals, and substitutions alike, and
      # forces a legitimate migration PR to update the pin in the same change
      # (see ci/expected-eligible-tests.txt's exact-equality-vs-floor note for why
      # this repo already prefers exact equality to a floor). Buildless — no compiler, no
      # ctest, python3 + coreutils only.
      - name: "Regression pin — run_for/get pump census (#289)"
        run: bash ci/test-pump-census.sh

      # #289 site labels must be UNIQUE across tests/, or `FIXPP_FORCE_WINDOW_MISS`
      # fires at more than one site and no seam arm can tell. WHY, and why the
      # obvious grep for it reports clean: see ci/pump-label-uniqueness.sh's own
      # header — restating it here would be a second copy to keep true, and the
      # two copies of the first draft already carried the same wrong mechanism.
      # Buildless — python3 + coreutils only.
      - name: "Regression pin — #289 site-label uniqueness"
        run: bash ci/pump-label-uniqueness.sh

      # #289 batch 18: a fixed `run_for` staging a MOCK-CLOCK advance is a
      # wall-clock hope, and a starved runner turns it into a LOST advance --
      # unrecoverable, not slow. The sweep REPORTS candidates and exits 0; what
      # runs here as a gate is its CONTROLS, which exit 2 when the classifier
      # stops discriminating. That is the half worth having in CI: a reporter
      # whose rules have silently broken reports clean, which is this repo's
      # most recurring instrument defect. Mechanism and the LOWER-BOUND caveat:
      # ci/mock-clock-staging-sweep.sh's own header. Buildless — python3 only.
      - name: "Regression pin — #289 mock-clock staging sweep controls"
        run: bash ci/mock-clock-staging-sweep.sh

      # 089 (specs/089-quickfix-interop-conversation) T005/T006/T007 — the disk
      # gate every configure/build task in that feature depends on. Follows
      # the same ci/<name>.sh + ci/test-<name>.sh convention as every other
      # pin in this job (contracts/089-quickfix-interop-conversation/
      # disk-preflight.md § "The gate itself"). Every RED cell asserts its own
      # diagnostic token, not merely a non-zero exit — same discipline as the
      # #255 wheel-payload pin this contract names as precedent. Two cells
      # drive MUTANT copies (never the tracked script) to prove the
      # spurious-hit arms A-7/A-8 would pass WRONGLY without the D-9/D-10
      # guards, then RED the real script on the identical fixture. Buildless
      # — bash + coreutils, no real disk, no /proc reads (both overridden).
      - name: "Regression pin — 089 disk-preflight gate (D-1..D-11, spurious-hit arms)"
        run: bash ci/test-disk-preflight.sh

      # ci/cxx_blank.py is the SHARED lexer three of the gates above resolve at
      # runtime, and #289 batch 19 moved `blank_unevaluated` into it and replaced
      # ci/pump-census.sh's verbatim second copy of the whole module with an import.
      # Its self-test had never run in CI: a break in it would have surfaced only as
      # whichever caller happened to notice, and the two copies had ALREADY diverged
      # (the census copy was missing the CRLF line-splice arm) with nothing to say so.
      # Buildless — python3 only.
      - name: "Regression pin — shared C++ lexer controls (ci/cxx_blank.py)"
        run: python3 ci/cxx_blank.py

      # Gate B round 1 (#326) found this checker had NO CI-visible signal at
      # all: the gate itself only runs as a local pre-commit hook (nothing in
      # .github/ invokes pre-commit), and `gate()` explicitly skips its own
      # file (`if path == SELF: continue`). A commit that broke every regex
      # in the checker would pass its own hook and nothing in CI would ever
      # notice. This step is the fix -- it runs unconditionally on every PR,
      # unlike the checker's own gate, which is behind a hook a developer must
      # have installed. Buildless -- no compiler, no ctest, python3 only.
      - name: "Regression pin — line-citation checker self-test (#310, #326)"
        run: python3 tools/check_line_citations.py --self-test

      # ci/pump-get-sweep.sh REPORTS a candidate list and deliberately does not gate on
      # it — the list is not pinnable, because a `.get()` is only a hazard where the
      # calling thread must pump. But its CONTROLS are gateable, and until #289 batch 20
      # nothing ran them: the sweep is invoked by hand, so a regression in its classifier
      # would have surfaced only as a number nobody could tell was wrong. It exits
      # non-zero when a control fails and zero otherwise whatever the candidate count, so
      # this step gates the instrument without pinning its output. ~2.4 s, buildless.
      # ⚠️ It does NOT cover `ci/red-arms/batch20-coroutine-axis.sh`, which needs a
      # historical commit present locally and is run by hand; the synthetic controls
      # prove the scope axis can say CORO, that arm proves it on real code.
      # ⚠️ NOT `--quiet`. That flag suppresses the CONTROL lines while still printing the
      # candidate rows and still exiting non-zero — noise without evidence. Run loud so
      # the log shows each control passing; a green step that prints nothing is
      # indistinguishable from a step that ran nothing.
      - name: "Regression pin — get-sweep classifier controls (#289)"
        run: bash ci/pump-get-sweep.sh --disposition

      # ── #291: co_spawn closure lifetime ────────────────────────────────────
      #
      # A lambda coroutine reaches its captures THROUGH the closure object, so
      # passing it to co_spawn as an immediately-invoked temporary (`}(),`)
      # destroys the closure at the end of the full-expression. asio's
      # `awaitable` promise returns `suspend_always` from `initial_suspend`, so
      # the body does not begin until AFTER co_spawn returns — the first body
      # statement naming a capture therefore already reads a destroyed closure.
      # This is a use-after-free on the first execution, not a hazard some later
      # edit would arm, and no compiler diagnostic fires on it.
      #
      # ⚠️ BOTH STEPS LIVE IN THIS UNGATED JOB DELIBERATELY, and the scan did NOT
      # go beside the other source gates in `check-layers`. `check-layers` is
      # behind `gate-precheck`, so it emits nothing until BOTH gate labels land —
      # i.e. nothing during the review rounds that are the only thing standing
      # between a fresh `}(),` and merge. brain-freshness.yml's header records
      # the same reasoning for check_line_citations.py's real run.
      #
      # The self-test is what stops the scan reporting a false clean: fixtures
      # written from the C++ FORMS the scan must separate — a nested invoked
      # lambda inside a correctly-passed one, digit separators, raw strings,
      # encoding-prefixed char literals — plus arms asserting that an
      # unterminated `co_spawn(` and a `co_spawn` token the call-site pattern
      # does not match both ERROR rather than parsing clean. Run
      # `--self-test` for the current tally; it prints N/N.
      #
      # Buildless — python3 + git only.
      - name: "Regression pin — co_spawn lambda guard self-test (#291)"
        run: python3 tools/check_co_spawn_lambda.py --self-test

      - name: "co_spawn closure-lifetime gate (#291)"
        run: python3 tools/check_co_spawn_lambda.py

      # ── #353: a test compiled OUT of a sanitizer leg by its own guard ──────
      #
      # test_seqnum_drain_on_close.cpp guarded a whole TEST on
      # `!defined(__SANITIZE_ADDRESS__)`. That test held BOTH use-after-frees
      # fixed in #291, so the ASan leg — the primary detector for that class —
      # could not have reported either one, and nothing said so: a skipped test
      # and a passing test are indistinguishable in a leg's summary. The only
      # signal is a per-preset test COUNT, and nobody compares those.
      #
      # ⚠️ WHY A SOURCE SCAN AND NOT A TEST-COUNT DIFF. Comparing
      # `--gtest_list_tests` across presets is the more direct instrument, but it
      # needs two BUILT binaries, so it could only live in a gated job — i.e.
      # emit nothing until both gate labels land, which is after the review
      # rounds that are the only thing between a fresh carve-out and merge. This
      # runs buildless in the ungated job for the same reason the #291 guard
      # above does. The cost is stated rather than hidden: this checks that every
      # preset-conditional case carries a written disposition, NOT that the
      # counts actually match at runtime.
      #
      # Self-test first, for the reason the #291 pair records: the denominators
      # witness the scan's REACH, not its DETECTOR. Break the "is this test
      # inside a sanitizer-conditional region" test and every denominator still
      # passes on a tree that has the defect.
      #
      # Buildless — python3 only.
      - name: "Regression pin — preset-conditional test census self-test (#353)"
        run: python3 tools/check_sanitizer_test_carveouts.py --self-test

      - name: "Preset-conditional test carve-out gate (#353)"
        run: python3 tools/check_sanitizer_test_carveouts.py

  # ── #354: AST audit of the named-closure co_spawn form ────────────────────
  #
  # THE RULE. A lambda coroutine reaches its captures THROUGH the closure object,
  # so the closure must outlive the coroutine. #291's buildless lexer
  # (`ci-script-pins`, ungated) catches the immediately-invoked TEMPORARY form.
  # It cannot reach the NAMED form — `auto lam = ...; co_spawn(ioc, lam(), tok)` —
  # because deciding that one means comparing the closure's SCOPE against the call
  # that DRIVES the coroutine, which needs an AST and a compilation database.
  #
  # WHY THIS JOB IS CHEAP, which is the whole reason it can exist at all: the tool
  # needs `compile_commands.json` (emitted at CONFIGURE time) plus the GENERATED
  # headers — 28 TUs under tests/ include them. It does NOT need the project
  # compiled. So this configures, builds ONLY `fixpp_codegen_generate`, and parses
  # the TUs itself with libclang. `linux-clang-release` is used because it is the
  # cheapest preset to configure; the audit is about SCOPES, and no verdict here
  # depends on optimisation level or NDEBUG.
  #
  # ⚠️ GATED, and that is a real limitation rather than an oversight. It needs a
  # configured build, so it sits behind `gate-precheck` and emits NOTHING during
  # the review rounds that are the only thing between a fresh unsafe site and
  # merge. That window belongs to the buildless lexer. Do not delete the lexer on
  # the grounds that this job subsumes it — it does not.
  #
  # Measured locally at `--jobs 4`: 242 TUs, 539 s. ⚠️ An earlier figure of "~35
  # min, dominated by clang parses" was WRONG in both halves — the parse was 8 %
  # of it and a per-AST-node `realpath` was the rest. Re-measure from this job's
  # own timing rather than trusting either number; it emits a `::notice::` with
  # its own wall-clock for exactly that.
  #
  # ⚠️ NOT IN `tier1-required` YET, DELIBERATELY, and this must not be forgotten.
  # The job ASSERTS (a FLAG exits non-zero) but nothing blocks merge on it until
  # it is added to that job's `needs:`. It is held out for ONE run because the
  # runner-side pieces — the pip-bindings / apt-library pairing, the codegen-only
  # build, the wall-clock — have never executed in CI. Promote it once a real run
  # has shown its timing and that it is not flaky. A job that asserts but cannot
  # block is exactly the shape this repo has been bitten by; leaving it here
  # permanently would be that defect.
  co-spawn-closure-audit:
    name: co-spawn-closure-audit
    runs-on: ubuntu-24.04
    needs: gate-precheck
    if: needs.gate-precheck.outputs.proceed == 'true' && github.event_name != 'release'
    concurrency:
      group: tier1-${{ github.event.pull_request.number || github.ref }}-cospawn-audit
      cancel-in-progress: true
    env:
      PRESET: linux-clang-release
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ inputs.source_ref || github.sha }}

      - name: Install Clang 22 (apt.llvm.org) + Ninja
        run: |
          wget -O /tmp/llvm.sh https://apt.llvm.org/llvm.sh
          chmod +x /tmp/llvm.sh
          ci/apt-guard.sh llvm-toolchain -- sudo /tmp/llvm.sh 22 all
          CLANG_BIN=$(ls /usr/bin/clang++-* 2>/dev/null | grep -oE '/usr/bin/clang\+\+-[0-9]+$' | sort -V | tail -1)
          CLANG_VER=$(echo "$CLANG_BIN" | grep -oE '[0-9]+$')
          test -n "$CLANG_VER" || { echo "::error::No clang-* installed by llvm.sh"; exit 1; }
          sudo ln -sf /usr/bin/clang-$CLANG_VER   /usr/bin/clang
          sudo ln -sf /usr/bin/clang++-$CLANG_VER /usr/bin/clang++
          ci/apt-guard.sh apt-install -- sudo apt-get install -y ninja-build swig python3-dev
          clang++ --version

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install Conan
        run: pipx install conan

      - name: Initialize Conan default profile
        run: conan profile detect --force

      - uses: oras-project/setup-oras@v2

      - name: Restore Conan cache from GHCR
        run: ci/restore-conan-cache.sh ${{ env.PRESET }}

      - name: Conan install
        run: |
          conan export conan/recipes/civetweb --version 1.16
          conan install . -pr conan/profiles/${{ env.PRESET }} --build=missing -of build/${{ env.PRESET }}

      - name: Configure (emits compile_commands.json)
        run: cmake --preset ${{ env.PRESET }} -DFIXPP_BUILD_PYTHON=OFF

      # 28 TUs under tests/ include generated headers. Without them libclang
      # cannot parse those TUs — and this tool reports a failed parse as UNSEEN,
      # never as clean, so the job would fail loudly rather than under-report.
      - name: Generate codegen headers only (NOT a full build)
        run: cmake --build build/${{ env.PRESET }} --target fixpp_codegen_generate

      # ⚠️ THE BINDINGS AND THE LIBRARY ARE DELIBERATELY MISMATCHED, and the
      # mismatch is the correct configuration rather than an accident.
      # PyPI's `libclang` wheel tops out at 18.1.1 and BUNDLES an 18 shared
      # library. This tree is C++23 built by clang 22, so an 18 libclang cannot
      # parse it — every TU would die and the tool would report 242 UNSEEN files.
      # So: take the PYTHON BINDINGS from pip and the SHARED LIBRARY from the
      # apt-installed LLVM 22. The tool tolerates the resulting cursor-kind skew
      # by construction (it depends only on kinds stable since early LLVM, and
      # recurses through nodes the bindings cannot name) and REPORTS the unnamed
      # count so the skew is visible rather than silent.
      - name: Install libclang python bindings + locate the LLVM 22 library
        run: |
          set -euo pipefail
          pip install libclang
          CLANG_VER=$(clang++ --version | grep -oE 'clang version [0-9]+' | grep -oE '[0-9]+$')
          LIBDIR=$(llvm-config-${CLANG_VER} --libdir)
          LIBCLANG=""
          for cand in "$LIBDIR/libclang.so" "$LIBDIR/libclang.so.1" "$LIBDIR/libclang-${CLANG_VER}.so.1"; do
            [ -f "$cand" ] && { LIBCLANG="$cand"; break; }
          done
          # Fail LOUD. Falling back to pip's bundled 18 would parse nothing and
          # the tool would report every TU as UNSEEN — recoverable, but only after
          # someone reads the log. An unresolvable library is a broken job.
          test -n "$LIBCLANG" || {
            echo "::error::no libclang.so under $LIBDIR for clang $CLANG_VER"; ls -la "$LIBDIR" | head -40; exit 1; }
          echo "LIBCLANG=$LIBCLANG" >> "$GITHUB_ENV"
          echo "RESOURCE_DIR=$(clang++ -print-resource-dir)" >> "$GITHUB_ENV"
          echo "using libclang: $LIBCLANG"

          # clang-query drives the SECOND instrument. Resolve it from the LLVM we
          # just installed, and fail loud if the toolchain did not ship it: the
          # cross-check is what keeps the audit's zero from resting on one tool,
          # so silently skipping it is the failure this pair exists to prevent.
          BINDIR=$(llvm-config-${CLANG_VER} --bindir)
          CQ=""
          for cand in "$BINDIR/clang-query" "$(command -v clang-query-${CLANG_VER} || true)" "$(command -v clang-query || true)"; do
            [ -n "$cand" ] && [ -x "$cand" ] && { CQ="$cand"; break; }
          done
          test -n "$CQ" || {
            echo "::error::clang-query not found for clang $CLANG_VER (looked in $BINDIR and on PATH)."
            echo "::error::llvm.sh installs it via clang-tools-$CLANG_VER; add it if the package set changed."
            ls -la "$BINDIR" | grep -i clang | head -20; exit 1; }
          echo "CLANG_QUERY=$CQ" >> "$GITHUB_ENV"
          echo "using clang-query: $CQ"

      # ⚠️ THE PAIR, and the order matters. The sweep's own counters witness the
      # scan's REACH; only the fixtures witness DETECTION. A sweep alone is a gate
      # that cannot fail for the reason it exists — and that is not hypothetical
      # here: the argument-level lambda prune was BROKEN while every in-tree check
      # passed, because implicit AST nodes happen to wrap the argument in real
      # code. `--self-test` is what caught it.
      - name: "Detector self-test — co_spawn named-closure audit (#354)"
        run: |
          # FIXPP_REPO_ROOT (#363): the self-test now includes screen arms pinned
          # against two IN-TREE oracle files whose site counts the walker already
          # reports, so it needs the repo root. Without it the arms fall back to
          # cwd and fail loudly as "oracle unreadable" rather than silently.
          FIXPP_REPO_ROOT="$PWD" python3 tools/audit_co_spawn_named_closure.py --self-test \
            --libclang "$LIBCLANG" --resource-dir "$RESOURCE_DIR"

      - name: "co_spawn named-closure audit (#354)"
        run: |
          set -euo pipefail
          START=$(date +%s)
          FIXPP_REPO_ROOT="$PWD" python3 tools/audit_co_spawn_named_closure.py \
            --build-dir build/${{ env.PRESET }} \
            --libclang "$LIBCLANG" \
            --resource-dir "$RESOURCE_DIR" \
            --jobs 4 \
            --json-out /tmp/co-spawn-audit.json
          echo "::notice::co_spawn audit wall-clock: $(( $(date +%s) - START ))s"

      # ⚠️ THE CROSS-CHECK IS PART OF THE GATE, not an optional extra. Both the
      # tool docstring ("Usage — RUN THE PAIR, always") and brain/index.md ("Keep
      # them as a pair; a clean sweep from a single tool with this one's defect
      # history is not evidence") say so, and an earlier version of this job ran
      # the audit alone and discarded its --json-out — which made both statements
      # false and left the audit's zero resting on one instrument.
      #
      # It re-derives the SAME population through clang-query, a matcher sharing
      # no code with the libclang walker, and diffs the site SETS rather than the
      # counts: two instruments agreeing on a total while disagreeing about WHICH
      # sites those are is a finding, not a pass. It fails on any non-empty
      # difference, on any file it could not see, and on a population it cannot
      # reconcile against.
      - name: "co_spawn census cross-check (#354)"
        run: |
          set -euo pipefail
          START=$(date +%s)
          python3 tools/reconcile_co_spawn_census.py \
            --build-dir build/${{ env.PRESET }} \
            --audit-json /tmp/co-spawn-audit.json \
            --clang-query "$CLANG_QUERY" \
            --extra-arg="-resource-dir=$RESOURCE_DIR" \
            --jobs 4
          echo "::notice::co_spawn cross-check wall-clock: $(( $(date +%s) - START ))s"

      # The site table is what a reviewer needs to disposition a FLAG or a MANUAL
      # without re-running a 9-minute sweep locally. It was being written to /tmp
      # and discarded.
      - name: Upload the audit site table
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: co-spawn-audit
          path: /tmp/co-spawn-audit.json
          if-no-files-found: warn

  check-layers:
    name: check-layers
    runs-on: ubuntu-24.04
    needs: gate-precheck
    # Skipped on release (see the `linux` job note) — layer checks gated the merge commit.
    if: needs.gate-precheck.outputs.proceed == 'true' && github.event_name != 'release'
    concurrency:
      group: tier1-${{ github.event.pull_request.number || github.ref }}-check-layers
      cancel-in-progress: true

    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ inputs.source_ref || github.sha }}

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Run layer enforcement script
        run: python3 tools/check_layers.py

      # 049-c-abi-handles-errors (T024): C-ABI occupancy + reentrancy gates.
      # Pure-text checks (no build) — run alongside the layer gate.
      - name: Run C-ABI occupancy gate (FR-013)
        run: bash tools/check_capi_occupancy.sh

      - name: Run C-ABI reentrancy gate (FR-014)
        run: bash tools/check_capi_reentrancy.sh

      - name: Run dictionary snapshot exclusivity gate
        run: bash tools/check_dictionary_snapshot_exclusivity.sh

  # ── Benchmarks (HARD since #209) ─────────────────────────────────────────
  # ⚠️ THIS JOB WAS SOFT IN THREE PLACES AND RAN A BENCHMARK THAT MEASURES
  # NOTHING. It is now a blocking gate and is in `tier1-required`'s `needs:`.
  # Design + acceptance criteria: .specify/ci209-bench-gate.md. Three tiers:
  #   tier 1  execution + schema over the allowlist — HARD, needs no comparand
  #   tier 2  candidate vs its own merge-base on THIS runner, A-B-A — HARD
  #   tier 3  checked-in baselines — INFORMATIONAL, each skip named
  # The old header cited [const §VIII.2] for the soft disposition. §VIII.2 fixes
  # a ±5% review/re-baselining obligation and never prescribed a CI exit code;
  # it is not what made this job soft, and no amendment was needed to harden it.
  bench:
    name: bench
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      packages: read
      id-token: write  # BoringCache authenticates this restore-only consumer through GitHub OIDC
    # Backstop against a hung bench build; matches the `linux` job's 90
    # (previously inherited GitHub's 360-min default).
    timeout-minutes: 180
    needs: gate-precheck
    # Skipped on release (see the `linux` job note) — bench is a soft gate, not an asset gate.
    if: needs.gate-precheck.outputs.proceed == 'true' && github.event_name != 'release'
    concurrency:
      group: tier1-${{ github.event.pull_request.number || github.ref }}-bench
      cancel-in-progress: true
    # ccache the release build (mirrors the matrix `linux` job): without this the
    # bench job did a full COLD release build every run (~800 per-message TUs at
    # -O2), the slowest Tier-1 job. Content-hashed cross-run cache → warm rebuilds.
    env:
      CMAKE_CXX_COMPILER_LAUNCHER: ccache
      CMAKE_C_COMPILER_LAUNCHER: ccache
      CCACHE_COMPILERCHECK: content
      CCACHE_COMPRESSLEVEL: "5"
      # Retain the upstream path for local state and comparable statistics.
      # BoringCache's remote namespace does not derive its identity from this
      # path; ccache derives object identity from the compiler invocation.
      CCACHE_DIR: /tmp/fixpp-ccache-linux-clang-release

    steps:
      # fetch-depth: 0 — #209 tier 2 needs the MERGE-BASE commit present locally
      # to `git worktree add` and build it. The default shallow clone has only
      # HEAD, so `git rev-parse HEAD~1` and a checkout of base.sha both fail.
      - uses: actions/checkout@v6
        with:
          ref: ${{ inputs.source_ref || github.sha }}
          fetch-depth: 0

      # Heavy build on the ~14 GB free-disk ceiling; reclaim the preinstalled
      # SDKs we don't use (~30 GB). Mirrors the `linux` job's identical step.
      - name: Free up disk space
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: true
          android: true
          dotnet: true
          haskell: true
          large-packages: false
          docker-images: true
          swap-storage: false

      - name: Install Clang 22 (apt.llvm.org) + Ninja
        run: |
          wget -O /tmp/llvm.sh https://apt.llvm.org/llvm.sh
          chmod +x /tmp/llvm.sh
          ci/apt-guard.sh llvm-toolchain -- sudo /tmp/llvm.sh 22 all
          # apt.llvm.org's llvm.sh installs versioned binaries (e.g. clang-22)
          # but does NOT update the unversioned `clang`/`clang++` paths. CMake
          # presets use unversioned `clang++`, which otherwise resolves to the
          # runner image's pre-installed clang-18 (real binary at /usr/bin/clang++,
          # missing C++23 std::expected in libstdc++ headers visible to it).
          # `update-alternatives --install` does NOT overwrite that pre-existing
          # regular file — force a symlink directly. Detect the actually installed
          # major (llvm.sh silently falls back to an earlier major when the
          # requested one isn't yet published for the runner's distro).
          CLANG_BIN=$(ls /usr/bin/clang++-* 2>/dev/null | grep -oE '/usr/bin/clang\+\+-[0-9]+$' | sort -V | tail -1)
          CLANG_VER=$(echo "$CLANG_BIN" | grep -oE '[0-9]+$')
          test -n "$CLANG_VER" || { echo "::error::No clang-* installed by llvm.sh"; exit 1; }
          echo "Force-symlinking clang-$CLANG_VER as default unversioned clang/clang++"
          sudo ln -sf /usr/bin/clang-$CLANG_VER   /usr/bin/clang
          sudo ln -sf /usr/bin/clang++-$CLANG_VER /usr/bin/clang++
          clang++ --version
          ci/apt-guard.sh apt-install -- sudo apt-get install -y --no-install-recommends \
            ninja-build \
            python3-pip \
            pipx

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install Conan + pytest via pipx
        run: |
          pipx install conan
          conan --version
          # pytest is required by the decimal_compare_oracle CTest entry
          # (tests/oracle/decimal_compare_oracle_test.py) which is registered
          # in every preset. ⚠️ This used to read "not just the python-bindings
          # build" — #254 folded that job into these six legs, so the contrast
          # it drew no longer exists and the sentence inverted. pytest is now
          # needed here for TWO independent reasons: that CTest entry, and the
          # bindings' own test suite (bindings/python/tests/).
          # pyyaml is required by the interop_cell_results_schema_check CTest
          # entry (tests/interop/cell_results_schema_check_test.py — 016 T028).
          pip install pytest pyyaml

      - name: Initialize Conan default profile
        run: conan profile detect --force

      - name: Teach Conan about Clang 22
        run: |
          mkdir -p ~/.conan2
          cat > ~/.conan2/settings_user.yml <<'EOF'
          compiler:
            clang:
              version: ["19", "20", "21", "22", "23"]
          EOF

      # Reuses the matrix leg's linux-clang-release seed. Restore-only — the
      # matrix release leg owns the push/prune.
      - name: Set up oras
        uses: oras-project/setup-oras@v2
      - name: Restore Conan cache from GHCR
        run: ci/restore-conan-cache.sh linux-clang-release

      # ccache MUST install BEFORE "Conan install" so the CMAKE_*_COMPILER_LAUNCHER
      # is on PATH for any conan from-source dependency build (mirrors the `linux`
      # job's ordering + exit-127 note).
      # ⚠️ THIS JOB IS A PURE CONSUMER OF THE SHARED TIER-1 CCACHE NAMESPACE.
      # It has no writer capability, deliberately (#273).
      #
      # It used to key on `tier1-bench` and save on push:main. MEASURED on PR
      # #279's run 32146773220, where both jobs started at the same second on the
      # same runner pool and built the SAME preset:
      #
      #   linux-clang-release (matrix)   8m36s   ccache 1599/1636 hits (97.74%)
      #   bench (soft)                 2h00m37s  ccache   15/1616 hits ( 0.93%)
      #
      # `conan-cache HIT` on both, so dependencies were not the cause: the object
      # counts are near-identical (1616 vs 1636) and the wall clock differs 14x
      # purely on compiler-cache warmth. The `tier1-bench` entry had been evicted
      # from the shared 10 GB Actions pool between merges — #273 — and because
      # the save is push:main-only, no PR run could ever repopulate it. A cache
      # that is cold on every PR is not a cache; it was a 2 GB reservation in a
      # contended pool, bought with a 2-hour build.
      #
      # Nothing justified the separation: `FIXPP_BUILD_BENCH` appears ONLY as a
      # gate on `add_subdirectory(bench)` (CMakeLists.txt's `if(FIXPP_BUILD_BENCH)`,
      # ProjectOptions.cmake's option decl). It changes no compile flag, so the library/test objects here
      # are hash-identical to the matrix leg's and restore straight into them.
      #
      # `trust-policy: restore` is the load-bearing half. The benchmark job can
      # read objects written by the producer legs but cannot add or delete
      # entries. It therefore cannot alter the producer namespace.
      #
      # Cost, stated: the bench TARGETS themselves are never cached (a handful of
      # TUs against ~1600). Accepted.
      - name: Install ccache and its HTTP storage helper
        env:
          CCACHE_VERSION: "4.14"
          CCACHE_SHA256: c64760b0b85ba86068f4cd162dc42e2dc39c6f46b0cb8c1990dfccbec7a1fed0
          CCACHE_STORAGE_HTTP_VERSION: "0.9"
          CCACHE_STORAGE_HTTP_SHA256: 875dbf6d575d06e4c4492f1ba639beb68530bc23382031a6bacc767cded9f463
        run: |
          ccache_archive="ccache-${CCACHE_VERSION}-linux-x86_64-glibc.tar.gz"
          curl --fail --location --show-error --silent --retry 5 --retry-all-errors --retry-delay 5 \
            "https://github.com/ccache/ccache/releases/download/v${CCACHE_VERSION}/${ccache_archive}" \
            --output "${RUNNER_TEMP}/${ccache_archive}"
          echo "${CCACHE_SHA256}  ${RUNNER_TEMP}/${ccache_archive}" | sha256sum --check
          tar xzf "${RUNNER_TEMP}/${ccache_archive}" -C "${RUNNER_TEMP}"
          sudo install -m 0755 \
            "${RUNNER_TEMP}/ccache-${CCACHE_VERSION}-linux-x86_64-glibc/ccache" \
            /usr/local/bin/ccache

          helper_archive="ccache-storage-http-go-${CCACHE_STORAGE_HTTP_VERSION}-linux-amd64.tar.gz"
          curl --fail --location --show-error --silent --retry 5 --retry-all-errors --retry-delay 5 \
            "https://github.com/ccache/ccache-storage-http-go/releases/download/v${CCACHE_STORAGE_HTTP_VERSION}/${helper_archive}" \
            --output "${RUNNER_TEMP}/${helper_archive}"
          echo "${CCACHE_STORAGE_HTTP_SHA256}  ${RUNNER_TEMP}/${helper_archive}" | sha256sum --check
          tar xzf "${RUNNER_TEMP}/${helper_archive}" -C "${RUNNER_TEMP}"
          sudo install -m 0755 \
            "${RUNNER_TEMP}/ccache-storage-http-go-${CCACHE_STORAGE_HTTP_VERSION}-linux-amd64/ccache-storage-http" \
            /usr/local/bin/ccache-storage-http

      - name: Checkout the BoringCache validation plan
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6.0.2
        with:
          ref: boringcache-validation
          path: .boringcache-plan
          sparse-checkout: .boringcache.toml
          sparse-checkout-cone-mode: false
          persist-credentials: false

      - name: Exclude the validation plan from source status
        run: printf '%s\n' '/.boringcache-plan/' >> .git/info/exclude

      - name: Restore the shared Tier 1 compiler cache through BoringCache
        uses: boringcache/one@1039999c65011be670f5655e0e48ad556188ab12 # v1.30.4
        with:
          working-directory: .boringcache-plan
          trust-policy: restore
          mode: ccache
          diagnostics: summary
          fail-on-cache-error: true

      - name: Conan install
        run: |
          conan install . \
            -pr conan/profiles/linux-clang-release \
            --build=missing \
            -of build/linux-clang-release

      - name: Configure with benchmarks
        run: cmake --preset linux-clang-release -DFIXPP_BUILD_BENCH=ON

      - name: Build
        run: cmake --build --preset linux-clang-release

      # ── #209: the real benchmarks, and an assertion ────────────────────────
      # ⚠️ EVERY `continue-on-error` IS GONE FROM THIS JOB, and there were TWO,
      # not one. The brief for #209 named the compare step; the "Run benchmark"
      # step carried its own (`# SOFT: bench binary may not exist yet`), and
      # tools/bench_compare.py `return 0`d by construction on top of both. Three
      # independent softnesses — removing any subset would leave this job green
      # regardless of outcome and the new `tier1-required` needs: entry vacuous.
      #
      # It also ran `placeholder_bench`, which measures nothing. #263 is what
      # that cost: XmlLoader::load regressed 60-90% ON MAIN, and no CI job could
      # have reported a runtime regression of any size.

      - name: Run bench suite (candidate, measurement A1)
        run: ci/run-bench-suite.sh build/linux-clang-release bench-results/a1

      # ── Tier 2: the merge-base, built on THIS runner ───────────────────────
      # The hard timing signal is candidate-vs-its-own-merge-base on ONE VM, not
      # candidate-vs-checked-in-baseline. That is not a preference: 10 of the
      # allowlisted baselines carry no `cpu_time` field on any row (they are
      # hand-authored analysis records), 3 more are `benchmarks: []`, and every
      # remaining one was recorded on the WSL2 dev host. Re-seeding is circular
      # — from the candidate it becomes its own comparand; from main it
      # canonizes #263's known regression. Full argument:
      # .specify/ci209-bench-gate.md §2-3.
      #
      # Only the `paired` targets are built here, not the whole tree.
      - name: Build the merge-base (tier 2 comparand)
        run: |
          set -euo pipefail
          HEAD_SHA="$(git rev-parse HEAD)"
          # ⚠️ `pull_request.base.sha` is the base BRANCH TIP, not the merge base.
          # Using it attributes every unrelated main-side change since the branch
          # point to this PR. Compute the real ancestor.
          BASE_TIP='${{ github.event.pull_request.base.sha }}'
          BEFORE='${{ github.event.before }}'
          if [ -n "$BASE_TIP" ]; then
            BASE_SHA="$(git merge-base "$HEAD_SHA" "$BASE_TIP")"
          elif [ -n "$BEFORE" ] && [ "$BEFORE" != "0000000000000000000000000000000000000000" ]; then
            # push: `before` is the tip prior to the push, so a MULTI-COMMIT push
            # is covered as one range. `HEAD~1` would compare only its last
            # commit and let a regression in an earlier commit of the same push
            # through.
            BASE_SHA="$BEFORE"
          else
            # workflow_dispatch, or a push with no usable `before` (new branch).
            BASE_SHA="$(git rev-parse HEAD~1)"
          fi
          # The base must be a genuine ANCESTOR, or the delta is not "what this
          # change did".
          if ! git merge-base --is-ancestor "$BASE_SHA" "$HEAD_SHA"; then
            echo "::error::tier 2: resolved base $BASE_SHA is not an ancestor of HEAD $HEAD_SHA."
            exit 1
          fi
          # ⚠️ A base that resolves to HEAD would build the same tree twice and
          # pass FOREVER — a tier-2-shaped false green. Fail closed instead.
          if [ "$BASE_SHA" = "$HEAD_SHA" ]; then
            echo "::error::tier 2: merge-base resolved to HEAD ($HEAD_SHA) — the comparison would be a tree against itself and could never fail. Refusing to run."
            exit 1
          fi
          echo "tier 2 base=$BASE_SHA head=$HEAD_SHA"
          echo "BENCH_BASE_SHA=$BASE_SHA" >> "$GITHUB_ENV"
          git worktree add /tmp/fixpp-base "$BASE_SHA"
          cd /tmp/fixpp-base
          conan install . -pr "${GITHUB_WORKSPACE}/conan/profiles/linux-clang-release" \
            --build=missing -of build/linux-clang-release
          cmake --preset linux-clang-release -DFIXPP_BUILD_BENCH=ON
          # Only the paired targets — the base tree exists to supply a comparand,
          # not to be tested.
          #
          # ⚠️ THE TWO LISTS MUST BE ALLOWED TO DIFFER, AND ASSUMING OTHERWISE WAS
          # A P1. This block used to classify from build-log GREP after the fact:
          # an "unknown target" phrase exempted the failure as a candidate-only
          # addition. That is the wrong source of truth in both directions:
          # wording drift turns a legitimate addition into a false red, and a real
          # base-tree failure whose log happens to match is exempted. The fix is
          # smaller and deletes the heuristic: decide from the merge-base's OWN
          # bench/ci-suite.txt BEFORE building.
          #
          # If the merge-base HAS a manifest, a listed target must build and ANY
          # nonzero is fatal. An unlisted target is a candidate-only addition,
          # permitted per Article VIII §3 and excluded from the paired base legs.
          # If the merge-base predates #209 and has no manifest, NO exemption is
          # possible: every candidate paired target is built and any failure is
          # fatal. The comparator's G10/G10-RED cells pin the same asymmetry.
          #
          # ⚠️ The filtered list is for the base RUNNER ONLY. The comparator's
          # --base-manifest must stay the base's OWN bench/ci-suite.txt: the
          # filtered list is derived from the candidate's paired rows, so a row the
          # candidate DOWNGRADED is absent from it, and using it there would make
          # [T2-DOWNGRADE] undetectable. Two different things that look like one.
          BASE_RUN_MANIFEST=/tmp/fixpp-base-run-manifest.txt
          : > "$BASE_RUN_MANIFEST"
          ADDED=""
          BASE_BENCH_MANIFEST=/tmp/fixpp-base/bench/ci-suite.txt
          HAVE_BASE_BENCH_MANIFEST=0
          if [ -f "$BASE_BENCH_MANIFEST" ]; then
            HAVE_BASE_BENCH_MANIFEST=1
          fi
          awk '$3=="paired" && $1 !~ /^#/ {print $1"\t"$2"\t"$3}' \
              "${GITHUB_WORKSPACE}/bench/ci-suite.txt" > /tmp/cand-paired.tsv
          test -s /tmp/cand-paired.tsv || { echo "::error::no paired rows in the candidate manifest"; exit 1; }
          while IFS=$'\t' read -r exe c3 c4; do
            t="${exe##*/}"
            if [ "$HAVE_BASE_BENCH_MANIFEST" -eq 1 ] && ! awk -v exe="$exe" '$1 == exe { found=1 } END { exit found ? 0 : 1 }' "$BASE_BENCH_MANIFEST"; then
              ADDED="$ADDED $t"
              echo "::notice::merge-base manifest has no row for '$t' — candidate-only addition, excluded from the paired base legs (permitted per Article VIII §3)"
              continue
            fi
            if cmake --build --preset linux-clang-release --target "$t" > /tmp/bt.log 2>&1; then
              printf '%s\t%s\t%s\n' "$exe" "$c3" "$c4" >> "$BASE_RUN_MANIFEST"
            else
              echo "::error::base build FAILED for target '$t' listed by the merge-base manifest:"
              tail -40 /tmp/bt.log
              exit 1
            fi
          done < /tmp/cand-paired.tsv
          test -s "$BASE_RUN_MANIFEST" || { echo "::error::no paired target exists in the merge-base — the paired tier would compare nothing, which is a silent pass"; exit 1; }
          echo "BENCH_BASE_RUN_MANIFEST=$BASE_RUN_MANIFEST" >> "$GITHUB_ENV"
          echo "base paired set: $(wc -l < "$BASE_RUN_MANIFEST") row(s) built; candidate-only additions:${ADDED:- none}"

      # ── A-B-A-B ────────────────────────────────────────────────────────────
      # A-vs-A is a noise floor for the CANDIDATE phase, and it is NOT enough on
      # its own: a throttle transient confined to the single base measurement
      # inflates the base, shrinks the apparent delta, and lets a real
      # regression through while both A legs agree perfectly. So the base is
      # measured twice as well and B-vs-B is checked by the same rule. Worked
      # example in tools/bench_compare.py's run_paired docstring.
      - name: Run bench suite (merge-base, measurement B1)
        run: ci/run-bench-suite.sh /tmp/fixpp-base/build/linux-clang-release bench-results/b1 --only-paired "$BENCH_BASE_RUN_MANIFEST"

      - name: Run bench suite (candidate again, measurement A2)
        run: ci/run-bench-suite.sh build/linux-clang-release bench-results/a2 --only-paired

      - name: Run bench suite (merge-base again, measurement B2)
        run: ci/run-bench-suite.sh /tmp/fixpp-base/build/linux-clang-release bench-results/b2 --only-paired "$BENCH_BASE_RUN_MANIFEST"

      # ── The assertions. No continue-on-error. ──────────────────────────────
      - name: "Tier 1 + 3 — execution, schema, and baseline report"
        run: python3 tools/bench_compare.py --suite bench-results/a1

      # Exit 2 means the INSTRUMENT was noisy (same-tree spread over the noise
      # band), not that the code regressed. Re-measure once before failing: a
      # transient that spans one four-leg pass rarely spans two, and this keeps
      # a bad runner from blocking the merge queue on a first blip.
      #
      # ⚠️ The retry is bounded at ONE and a second exit-2 FAILS. It does not
      # loop, and it never degrades to exit 0 — a gate a noisy runner can
      # silently switch off while reporting green is the false-green shape this
      # whole change removes. The failure message names runner noise rather than
      # a perf regression so an operator can tell the two apart without reading
      # the log.
      - name: "Tier 2 — paired base-vs-candidate (HARD)"
        run: |
          set -uo pipefail
          # ⚠️ --base-manifest: THE MANIFEST IS PART OF THE GATE AND THE CHANGE
          # BEING GATED CAN EDIT IT. Flipping one pre-existing row from `paired`
          # to `no` removes that binary from --only-paired, from the base build's
          # target list and from run_paired's filter at once, so the regression
          # it was measuring stops existing rather than being reported missing;
          # the other paired rows keep T2-VACUOUS quiet and the gate exits 0
          # (Gate B round 2, P1, F1 — reproduced at +100%, `tier 2 PASSED`).
          # Diffing against the merge-base's own copy makes deletions and
          # downgrades [T2-DOWNGRADE] failures while still permitting additions.
          #
          # It is NOT the only defence, and deliberately so: a merge-base that
          # PREDATES #209 has no bench/ci-suite.txt at all — including this PR's
          # own — and that case is exempted BY NAME, not silently. The mandatory
          # paired floor in ci/test-bench-gate.sh (cells M-PINNED / M-PINNED-RED)
          # is what covers it, and runs unconditionally in ci-script-pins.
          cmp_paired() {
            python3 tools/bench_compare.py --paired \
              --a1 bench-results/a1 --b1 bench-results/b1 \
              --a2 bench-results/a2 --b2 bench-results/b2 \
              --base-manifest /tmp/fixpp-base/bench/ci-suite.txt \
              --base-run-manifest "$BENCH_BASE_RUN_MANIFEST"
          }
          cmp_paired && exit 0
          rc=$?
          # Exit 2 is also argparse's malformed-command-line code, so a broken
          # cmp_paired definition is misdiagnosed here as runner noise and costs
          # one full re-measurement before the real failure returns.
          if [ "$rc" -ne 2 ]; then
            echo "::error::tier 2 FAILED (exit $rc) — a regression or an integrity finding, not runner noise."
            exit "$rc"
          fi
          echo "::warning::tier 2 was UNINFORMATIVE (runner noise exceeded the noise band). Re-measuring once."
          ci/run-bench-suite.sh build/linux-clang-release bench-results/a1 --only-paired
          ci/run-bench-suite.sh /tmp/fixpp-base/build/linux-clang-release bench-results/b1 --only-paired "$BENCH_BASE_RUN_MANIFEST"
          ci/run-bench-suite.sh build/linux-clang-release bench-results/a2 --only-paired
          ci/run-bench-suite.sh /tmp/fixpp-base/build/linux-clang-release bench-results/b2 --only-paired "$BENCH_BASE_RUN_MANIFEST"
          cmp_paired && exit 0
          rc=$?
          if [ "$rc" -eq 2 ]; then
            echo "::error::tier 2 UNINFORMATIVE TWICE — this runner's own measurement noise exceeds the noise band across two independent four-leg passes. This is an INSTRUMENT failure, not a detected performance regression: do not read it as the code being slow. Re-run the job; if it recurs, the tier-2 band needs re-deriving (see .specify/ci209-bench-gate.md §6a)."
          fi
          exit "$rc"

      # Kept for the record trail AC-8 asks for: the job's own cost, separable
      # into build vs measurement, so the critical-path budget can be judged
      # from data rather than from the 180-minute timeout.
      - name: Upload bench results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: bench-results
          path: bench-results/
          retention-days: 14

  # ── Required gate: the Tier 1 matrix must ACTUALLY have run green ──────────
  # Closes the skip-into-green hole. The core build/test jobs are gated
  # `if: needs.gate-precheck.outputs.proceed == 'true'`, and GitHub scores a
  # SKIPPED job as success — so while the gates are unsatisfied the matrix skips
  # and the umbrella "Tier 1" check reports green having built and tested
  # nothing. This REQUIRED job is RED unless the core jobs ran to success, so a
  # PR cannot merge on a matrix that never ran; satisfy Gate A + Gate B (add the
  # gate-a-done / gate-b-done labels) to fire the matrix and turn it green.
  #
  # This check IS required by branch protection. Because it runs in the PR's
  # pull_request context, it satisfies the merge gate (a workflow_dispatch run
  # would NOT — only pull_request-context check-runs count).
  # Ref: feedback_ci_gate_observes_not_asserts_witness_skips_into_green.
  # tier2/tier3 share the identical gate shape (Gate A ∧ Gate B `filter` job),
  # so one gate-label event co-fires all three to green-latest.
  tier1-required:
    name: tier1-required
    runs-on: ubuntu-latest
    # #254: `python-bindings` removed — the job no longer exists. ⚠️ This entry
    # and the four assertion sites below MOVE AS ONE UNIT. The `result` of a job
    # that is not in `needs:` (or does not exist) evaluates to the EMPTY STRING,
    # and empty is neither `success` nor `skipped` — so leaving any assertion
    # behind makes this required check RED on EVERY non-release run, both
    # branches of the python_touched split, PR and push alike.
    # #209: `bench` ADDED. It was absent, so the bench job could go red while
    # this required check reported green — and it could not even do that, being
    # soft in three independent places (both of its steps carried
    # `continue-on-error: true` and `tools/bench_compare.py` returned 0 by
    # construction). All three are gone; this entry is what makes the remaining
    # `exit 1` able to block. `bench` joins the UNCONDITIONAL `== success` set
    # below, not the wheel-style `skipped` branch: its `if:` is
    # character-identical to `linux`/`check-layers`, so it has no legitimate
    # skip disposition on a non-release event, and the release early-exit
    # already covers the one event where it does not run.
    needs: [gate-precheck, linux, coverage, check-layers, ci-script-pins, bench,
            python-wheel-build, python-wheel-test]
    if: always()
    steps:
      - name: Assert the core Tier 1 matrix ran to success (no skip-into-green)
        run: |
          # A published release intentionally skips the C++ matrix (wheel-only).
          #
          # ⚠️ THE ci-script-pins ASSERTION SITS BELOW THIS EARLY EXIT, so a
          # release event never reaches it. The JOB still runs on release (it
          # has no event `if:`) — what is waived here is aggregating its
          # result into this gate, not executing it. That is deliberate and
          # matches the rest: the pins guard the ci/ cache scripts, which a
          # wheel-only release event does not run. Making one job blocking on
          # an event where every other job is waived would be an asymmetry
          # with no failure behind it.
          #
          # Same asymmetry now applies to `python-wheel-build`: its own `if:`
          # includes `release`, so it DOES run on a release event (its `if:`
          # is `release || (proceed && python_touched)`), and it is now in
          # this job's `needs:` — so `tier1-required` waits on it (~70 min
          # warm) before reaching this early exit, then discards its result
          # unread. Cosmetic (nothing gates a release on this check), but a
          # real behaviour change worth recording rather than rediscovering.
          if [ "${{ github.event_name }}" = "release" ]; then
            echo "release event — C++ matrix intentionally skipped; gate not applicable."
            exit 0
          fi
          gp='${{ needs.gate-precheck.result }}'
          lx='${{ needs.linux.result }}'
          cov='${{ needs.coverage.result }}'
          cl='${{ needs.check-layers.result }}'
          # ci-script-pins is UNGATED, so unlike the three above it is expected
          # to be `success` even on a PR that has not yet earned its gate labels
          # — it is the only member of this list that can be green before then.
          csp='${{ needs.ci-script-pins.result }}'
          # #209 — moved in as ONE UNIT with the `needs:` entry above, per this
          # job's own standing warning: a `needs:` member with no assertion is
          # silently ignored, and an assertion for a job not in `needs:`
          # evaluates to the empty string and reds every non-release run.
          bch='${{ needs.bench.result }}'
          echo "gate-precheck=$gp  linux=$lx  coverage=$cov  check-layers=$cl  ci-script-pins=$csp  bench=$bch"
          fail=0
          for kv in "gate-precheck=$gp" "linux=$lx" "coverage=$cov" "check-layers=$cl" "ci-script-pins=$csp" "bench=$bch"; do
            if [ "${kv#*=}" != "success" ]; then
              echo "::error::Tier 1 job '${kv%%=*}' did not run to success (got '${kv#*=}'). The core build/test matrix must actually run — satisfy Gate A + Gate B (gate-a-done / gate-b-done) so the matrix fires before merge."
              fail=1
            fi
          done

          # ── Python legs (gate-b/r1, F2) ─────────────────────────────────
          # `tier1-required` did not previously depend on the three python
          # jobs at all, so they could go red while this check reported
          # green — the exact skip-into-green hole this job exists to close,
          # now reopened one layer down. #251 is the PR that makes it matter:
          # its whole point is to surface NEW sanitizer findings on these
          # legs, behind a required check that could not see them.
          #
          # ⚠️ #254 — the bindings' signal is NO LONGER ASSERTED HERE, and
          # that is a STRENGTHENING, not a gap. `python-bindings` was folded
          # into `linux`, which this job asserts `== success` UNCONDITIONALLY
          # for every non-release event, with no python_touched gate of its
          # own. The old contract accepted `python-bindings == skipped` as
          # green on a C++-only PR; the new one has no such branch, because
          # nothing is left that can legitimately skip. Read the `linux`
          # assertion above as covering the bindings from here on.
          #
          # A uniform "must be success" rule is wrong for two reasons:
          #   - `python-wheel-build` gates independently on
          #     `proceed && python_touched` — a path-gated C++-only PR
          #     legitimately reports `skipped`, and that is NOT skip-into-
          #     green: `python_touched` is the discriminant that decides
          #     whether that job was relevant, not an unconditional skip.
          #   - `python-wheel-test` has no `if:` of its own — only
          #     `needs: python-wheel-build`. If that build fails,
          #     wheel-test's own disposition is `skipped`, not `failure`,
          #     so it must be checked against `python-wheel-build`'s result,
          #     not against `python_touched` directly. Otherwise a build
          #     failure would double-report AND a naive "skipped is always
          #     fine for wheel-test" would silently re-open the hole here.
          #
          # Any `failure` or `cancelled` result fails both branches below
          # (neither equals `success` nor `skipped`), so both are covered
          # without a separate check.
          #
          # Latency: this makes `tier1-required` wait on the slowest python
          # job. ⚠️ The figures that used to sit here (113.1 min for
          # `python-bindings (asan)` on run 31273945846, against
          # `linux-clang-coverage`'s 134.8) went with the job in #254. The
          # remaining pole is unchanged: coverage at ~134.8 min was already
          # longer than any python leg, which is why #254 buys runner-MINUTES
          # (~316/run, gross) and not wall-clock. Shortening tier 1's wall is
          # #241's.
          pt='${{ needs.gate-precheck.outputs.python_touched }}'
          pwb='${{ needs.python-wheel-build.result }}'
          pwt='${{ needs.python-wheel-test.result }}'
          echo "python_touched=$pt  python-wheel-build=$pwb  python-wheel-test=$pwt"
          if [ "$pt" = "true" ]; then
            if [ "$pwb" != "success" ]; then
              echo "::error::python-wheel-build did not run to success (got '$pwb') while python_touched=true."
              fail=1
            fi
          else
            if [ "$pwb" != "skipped" ]; then
              echo "::error::python-wheel-build expected exactly 'skipped' when python_touched=false, got '$pwb'."
              fail=1
            fi
          fi
          if [ "$pwb" = "success" ]; then
            if [ "$pwt" != "success" ]; then
              echo "::error::python-wheel-test did not run to success (got '$pwt') while python-wheel-build=success."
              fail=1
            fi
          else
            if [ "$pwt" != "skipped" ]; then
              echo "::error::python-wheel-test expected exactly 'skipped' when python-wheel-build!=success (got '$pwb'), got '$pwt'."
              fail=1
            fi
          fi

          [ "$fail" = 0 ] || exit 1
          echo "Tier 1 core matrix ran to success."
