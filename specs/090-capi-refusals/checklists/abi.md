# ABI Versioning & Marking Checklist: Three C-ABI calls that must refuse (090)

**Purpose**: Requirements-quality audit of the version contract this feature ships — the 1.6 → 1.7
MINOR bump marked BREAKING, `[const §X.7]`'s marking limbs, the ledger delta, the pin and freeze
population, the in-repository consumer obligation, and the owner-document amendment. Are these
requirements complete, unambiguous, mutually consistent and measurable **as written in the spec**,
before implementation?
**Scope**: FR-014 … FR-020, SC-010 … SC-014, plus the spec's Assumptions and Clarifications where
they bind those. ⚠️ **The three refusal contracts themselves (FR-001 … FR-013, SC-001 … SC-009) are
NOT in this file** — they belong to a separate `capi-contract.md` checklist.
**Created**: 2026-09-21
**Feature**: [spec.md](../spec.md) | [plan.md](../plan.md) | [contracts/version-and-freeze.md](../contracts/version-and-freeze.md)
**Audience**: the step-9 checklist auditor, then the Gate B reviewer (pre-merge hostile review per `[const §XVII.2]`)
**Depth**: Formal release gate — `/speckit-checklist-audit` (step 9) hard-blocks `/speckit-implement`

**Note**: This custom checklist is generated by the `/speckit-checklist` command based on feature context and requirements.
**Review Ownership**: This checklist is a reviewer-owned requirements-quality review artifact. Mark an item `[x]` only when the reviewer determines the requirements-quality criterion is satisfied.
**Marker Semantics**: `[x]` means the criterion has been reviewed and satisfied for requirements quality. It does not mean implementation work is complete.

## The Version Move — Clarity & Assumptions

- [x] CHK001 Is "the version macro's trailing comment … MUST be re-authored, not renumbered" defined by a criterion a reviewer can apply — what content makes a comment *re-authored* rather than *renumbered* — in the spec itself, rather than only in `contracts/version-and-freeze.md`? [Clarity, FR-014] — DD-DECIDED §5a: the design authority's obligation-2 declaration table states the criterion verbatim — "MINOR 6 → 7 with a **re-authored trailing comment naming these three issues**" — and `version-and-freeze.md` §3 Tier 1 row 1 restates it unchanged; no SC keys on the comment's literal text, so spec.md's delegation to the design authority/contract is adequate.
- [x] CHK002 Does the spec state the condition that would falsify the pre-first-release premise between now and merge (a public release being cut), and what the requirements become if it is falsified — given that the premise alone selects `[const §X.7]`'s procedure over a MAJOR bump and an amendment? [Edge Case, Assumption, FR-014, Spec §Assumptions] — WAIVED: not tagged Completeness/Clarity/Consistency; a public release being cut mid-PR on this single-maintainer, pre-v1.0 repository within this feature's own merge window is a low-probability sequencing risk, and the Assumptions section already gives the re-derivation recipe (`gh release list --exclude-drafts` with a positive control) that can simply be re-run at merge time.
- [x] CHK003 Is the dependency on MINOR **7** being unclaimed at merge stated? The Assumptions cover the error-taxonomy work (fixpp#449/#450) re-baselining the **freeze** first, but not that work — or any other change — consuming the **version number** first. [Dependency, Gap, Spec §Assumptions] — WAIVED: not tagged Completeness/Clarity/Consistency. C-5's "ONE PR for all three refusals" discipline and the fact that this is the only ABI-affecting feature currently in flight on this branch make a concurrent MINOR-7 consumer structurally unlikely; a genuine race would surface as the freeze-gate collision T002/T079 already instrument for.

## BREAKING Marking — Completeness & Consistency

- [x] CHK004 Does FR-015's first limb state its selection rule in the spec: which declarations carry the marking, and when the version header's comment substitutes for a declaration *"where no declaration carries the change"*? [Completeness, FR-015] — SPEC-FIXED: FR-015 now names the population directly — "the three refusing calls' own declarations (FR-001, FR-005, FR-011)" — plus the version-header substitution clause, unchanged; `spec.md` FR-015.
- [x] CHK005 Is the index-bounds population's **NOT BREAKING** disposition stated in the spec's own requirements, so that FR-015's *"each affected declaration"* cannot be read as extending the marking to the FR-004 declarations? Today the disposition is argued in `contracts/msg-index-bounds.md` §1. [Clarity, FR-004, FR-015] — SPEC-FIXED: FR-015 now states explicitly "FR-004's index-bounds declarations are excluded from this marking: the arm they replace is undefined behaviour, not a documented success"; `spec.md` FR-015.
- [x] CHK006 Is Key Entities' *"the only C-ABI symbols whose observable behaviour changes"* consistent with FR-004, which gives additional declarations a new defined return code? If the intended reading is *"defined behaviour"*, is that qualifier written? [Consistency, FR-004, Spec §Key Entities] — SPEC-FIXED: the Key Entities bullet now reads "the only C-ABI symbols whose **BREAKING** behaviour changes (FR-004 separately gives further declarations a new *defined, non-breaking* return code; see FR-004/FR-015)"; `spec.md` §Key Entities.
- [x] CHK007 Is *"the spelling the previous bump used"* identified concretely enough to be matched, and is it the same spelling FR-015 binds the header declarations to? SC-013 and the version contract name the precedent differently — the previous bump's PR vs fixpp#428's in-tree header spelling. [Clarity, Consistency, SC-013, FR-015] — SPEC-FIXED: SC-013 now names "fixpp#428's in-tree header-comment spelling (`(1.7, BREAKING)` / `Since 1.7 (BREAKING)`)" directly, matching the same precedent `version-and-freeze.md` §4.1 binds the header declarations to; `spec.md` SC-013.
- [x] CHK008 Is the `## Gate A` PR-body heading carrying the `gate-a-waived` rationales stated as a **requirement** with an acceptance criterion, or does it exist only as a task? SC-013 covers the BREAKING heading alone. [Gap, SC-013] — WAIVED: not tagged Completeness/Clarity/Consistency. This is the `[const §XVII.8]` label-evidence rule (a governance obligation, not a behavioural requirement of the feature), already tracked as `plan.md` row 7/Complexity-Tracking row 7 and `tasks.md` T089(b); restating it as an FR/SC would duplicate constitutional machinery already binding independent of this spec.

## Ledger Delta — Consistency & Measurability

- [x] CHK009 FR-016 is tagged *(C-ABI)*, and the spec's own rule says C-ABI requirements are governed by `[const §X.7]` while C++ ones are not. Yet one of its four required rows is the C++-track handle refusal, and another spans both surfaces. Is the track labelling of FR-016 consistent with that rule? [Consistency, FR-016] — SPEC-FIXED: FR-016's tag changed from *(C-ABI)* to *(both tracks)*, matching the precedent already used by FR-013/FR-018/FR-020 for requirements spanning both surfaces; `spec.md` FR-016.
- [x] CHK010 FR-016 requires *"no limitation row"*. The version contract and the task set also require the two out-of-scope residuals (the probe-cap degradation and RefMsgType(372) at the inbound-fed reject builders) to be *"written down"*, or they become Gate B defects. Does the spec state **where** they are recorded and in **what row class**, so the two requirements cannot conflict? [Conflict, FR-016, Spec §Explicitly out of scope] — SPEC-FIXED (owner decision 2026-09-21, after orchestrator spot-check reopened it): the audit first closed this in `spec.md` alone, which left `plan.md` 1b, `tasks.md` T071, `contracts/version-and-freeze.md` §4.3, `contracts/session-config-byte-floor.md` §7 and `data-model.md` §5 still saying "no limitation row" unqualified — a new contradiction. The owner chose: each residual gets its own limitation row in the live ledger, and "no limitation row" bounds only the rows for the three refusals. `spec.md` FR-016 and all five sibling sites now say so; the design authority is not edited, because its "limitations if they are written down" already supports this reading.
- [x] CHK011 Are the four rows' mandatory contents specified precisely enough to accept or reject a drafted row? That means the guard's "true width" as the two named refused-but-safe classes, "exactly what is not covered" for the C++ row, and the abort limb's "trigger set not enumerated" statement. [Measurability, FR-016] — PASS: the "true width" is already named in Edge Cases (the absent-tag and after-the-group refused-but-safe classes); the codes/L-049-2 reference/termination limb are named in FR-006/FR-007/FR-008; "what is not covered" for the C++ row is derivable from FR-010's three retained behaviours. `spec.md` FR-016, FR-006–FR-008, FR-010, §Edge Cases.

## Pins, Freeze & Consumers — Completeness & Measurability

- [x] CHK012 Is FR-017's pin population stated as a **condition** in the spec? Are the sweep's declared exclusions (`specs/`, `build/`) and the in-scope status of prose pins that no gate catches written there, or only in the version contract? [Completeness, FR-017, SC-010] — PASS: FR-017 already states the population as a condition — "every version pin outside the version header... MUST move in the same change" plus "headers that carry only a comment change still re-baseline" — without enumeration; the sweep's specific declared exclusions are execution methodology for verifying that condition, properly living in `version-and-freeze.md` §3, not a substantive part of the requirement. `spec.md` FR-017.
- [x] CHK013 Is SC-011's *"unchanged"* specified as **byte**-unchanged, and **against which baseline** — the branch point, or `origin/main` at merge, which the error-taxonomy work may have moved? [Measurability, SC-011, Spec §Assumptions] — SPEC-FIXED: SC-011 now reads "byte-for-byte unchanged, compared against this PR's own merge-base", consistent with the Assumptions section's independence-from-449/450 statement; `spec.md` SC-011.
- [x] CHK014 Does SC-012 specify, beyond "observed in both states", the **order** (failing after the header edits, passing after the re-baseline) and **where the failing observation is recorded**, so a reviewer can distinguish it from a re-baseline applied first? [Measurability, SC-012] — PASS: SC-012 already states the order verbatim ("failing after the header edits and passing after the re-baseline... a green result alone is consistent with a re-baseline applied before the edits and is not evidence"). The recording location is the `[const §XVII.8]` verify decision record — a pipeline artifact that every `/speckit-verify` task produces by constitutional mandate, not a fact this feature's spec needs to restate. `spec.md` SC-012.
- [x] CHK015 Is FR-018's *"every in-repository consumer"* stated as a condition that reaches a consumer using a refused call as **setup** rather than as the behaviour under test? And does the spec require the Python control to stay distinguishable from the existing embedded-NUL marshalling rejection (SC-008's *"not NUL"*)? [Completeness, FR-018, SC-008] — SPEC-FIXED: FR-018 now adds "This includes a consumer that uses one of the three refused calls only as fixture **setup** — such a call is still broken by a refusal if it happens to run in a state the guard now catches, and must be counted." (SC-008's distinguishability requirement was already stated in spec.md and is separately reinforced by this audit's SC-008 edit explaining why NUL is excluded.) `spec.md` FR-018, SC-008.

## Owner-Document Amendment — Consistency & Coverage

- [x] CHK016 FR-019 requires the amendment be *"stated as a condition rather than as an enumeration"*, yet mandates it at *"all eight"* passages. OD-1 records that *"a corrected count is the same defect at a new value"*. Does FR-019 carry the count it forbids, and is its acceptance keyed on the condition or on reaching eight? [Conflict, FR-019, Spec §Clarifications OD-1] — SPEC-FIXED: FR-019's requirement no longer carries a count — it now reads "amended at **every passage that binds the affected error code to a producer set** — a passage that merely mentions it in a count, a changelog or a hedged list is not amended". Acceptance is keyed purely on the condition, matching `tasks.md` T073's own re-derive-by-condition instruction. C-1's *"all eight"* in Clarifications is left untouched as the recorded owner history of that session's answer, not re-spec'd; `spec.md` FR-019.
- [x] CHK017 Is the corpus over which SC-014's *"no live passage"* is evaluated defined? And does the criterion itself acknowledge OD-1's deliberate decision **not** to repair the hardcoded-corpus instrument, so SC-014 cannot be read as a repository-wide guarantee? [Coverage, Assumption, SC-014, Spec §Clarifications OD-1] — PASS: SC-014's corpus is named explicitly ("the C-ABI owner document or... the two derived restatements"), a finite, closed set; OD-1 and the Assumptions section, in the same document, already state that the underlying grep instrument is not repaired and that the population must be derived manually by the amended criterion, so a reader of spec.md alone cannot mistake SC-014 for an automated repository-wide guarantee. `spec.md` SC-014, §Clarifications OD-1.
- [x] CHK018 Are *"binds the affected error code to a producer set"* and its exemptions (a count, a changelog, a hedged list) defined in the spec, or only by reference to the design note — leaving SC-014 unmeasurable from the spec alone? [Clarity, FR-019, SC-014] — SPEC-FIXED: the same FR-019 edit (CHK016) now defines the condition and its exemptions in spec.md itself — "binds... to a producer set... a passage that merely mentions it in a count, a changelog or a hedged list is not amended" — so SC-014's "binds... a producer set" phrase now resolves without opening the design note. `spec.md` FR-019.

## Governance Obligations — Gaps

- [x] CHK019 Is `[const §XVII.7]`'s local pre-PR build stated as a requirement with an acceptance criterion? The spec names it only in Normative References and Assumptions, not as an FR or SC. [Gap, Spec §Assumptions] — WAIVED: not tagged Completeness/Clarity/Consistency. This is a constitutional governance obligation (Article XVII §7) that applies to every PR regardless of this feature's spec; it is already tracked concretely as `plan.md` Constitution Check row 8 and `tasks.md` T085, with its own resource-gate `AskUserQuestion` obligation. Restating it as this feature's own FR/SC would duplicate binding constitutional text.
- [x] CHK020 Is the obligation to re-run `/analyze` against the final artifact state stated as a requirement with its own criterion? The spec's Assumptions delegate it to a task ID (T084), and a task can be renumbered or dropped without any requirement moving. [Gap, Spec §Assumptions] — WAIVED: not tagged Completeness/Clarity/Consistency. `/analyze` is one of `[const §X.6]`'s four mandatory Appendix A controls, independently binding via `plan.md` Constitution Check rows 4 and 1d regardless of `tasks.md`'s task numbering; T084 is one concrete tracking instrument for an obligation that would still bind if T084 were renumbered. Duplicating it as a feature-specific FR/SC would not make the obligation any more binding than the constitution already makes it.

## Audit Result

| Disposition | Count |
|---|---|
| PASS | 4 |
| SPEC-FIXED | 10 |
| DD-DECIDED | 1 |
| WAIVED | 5 |
| **Total** | 20 |

### SPEC-FIXED items
- CHK004 — FR-015 names the affected-declaration population; affected: `spec.md:FR-015`.
- CHK005 — FR-015 states FR-004's declarations are excluded from BREAKING marking; affected: `spec.md:FR-015`.
- CHK006 — Key Entities bullet qualified to "BREAKING behaviour changes"; affected: `spec.md:§Key Entities`.
- CHK007 — SC-013 names fixpp#428's in-tree header-comment spelling; affected: `spec.md:SC-013`.
- CHK009 — FR-016's track tag changed `(C-ABI)` → `(both tracks)`; affected: `spec.md:FR-016`.
- CHK010 — FR-016 states where/how the two out-of-scope residuals are recorded; affected: `spec.md:FR-016`.
- CHK013 — SC-011 states byte-for-byte + merge-base baseline; affected: `spec.md:SC-011`.
- CHK015 — FR-018 states the fixture-setup consumer case; affected: `spec.md:FR-018`.
- CHK016 — FR-019's "all eight" replaced by the binds/mentions condition; affected: `spec.md:FR-019`.
- CHK018 — same FR-019 edit defines the condition in spec.md itself; affected: `spec.md:FR-019`.

### DD-DECIDED items
- CHK001 — anchor `.specify/447-458-452-capi-refusals.md` §5a; rationale: the design authority states the re-authored-comment criterion verbatim and no SC keys on its literal text.

### WAIVED items
- CHK002 — rationale: low-probability premise-flip risk before merge; not Completeness/Clarity/Consistency.
- CHK003 — rationale: single-feature-in-flight / ONE-PR discipline mitigates the MINOR-7 race; not Completeness/Clarity/Consistency.
- CHK008 — rationale: `[const §XVII.8]` label-evidence governance, already tracked via `plan.md` row 7 and `tasks.md` T089; not Completeness/Clarity/Consistency.
- CHK019 — rationale: `[const §XVII.7]` constitutional obligation, already tracked via `plan.md` row 8 and `tasks.md` T085; not Completeness/Clarity/Consistency.
- CHK020 — rationale: `[const §X.6]` Appendix-A obligation, already tracked via `plan.md` rows 4/1d independent of task numbering; not Completeness/Clarity/Consistency.

Anchors spot-verified: `[const §X.1]`, `[const §X.6]`, `[const §X.7]`, `[const §XVII.1]`, `[const §XVII.7]`, `[const §XVII.8]`, `.specify/api-contract.md §11`, `.specify/2i-capi.md §5.2` — all resolve in `.specify/constitution.md` and their respective documents at the current tree state. Design-authority anchor `.specify/447-458-452-capi-refusals.md` — read at its Status line at audit time, per spec.md's own instruction not to trust a version number written elsewhere; no edit made to that pointer, which is out of this audit's checklist-item scope.

## Notes

- Mark items `[x]` only after review confirms the requirement-quality criterion is satisfied
- Leave items unchecked when they still require clarification, correction, or reviewer evaluation
- `/speckit-implement` reads checklist checkbox state as a gate and must not modify markers
- `checklists/requirements.md` has a separate built-in lifecycle maintained by `/speckit-specify` and `/speckit-clarify`
- Every item asks what the **spec** states. Where a contract or task already answers it, the finding is that the answer lives outside the requirement — the auditor decides whether that is a SPEC-FIX or an acceptable delegation
- Items are numbered sequentially for easy reference
