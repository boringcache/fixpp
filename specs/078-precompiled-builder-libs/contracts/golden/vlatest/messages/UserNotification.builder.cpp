// SPDX-License-Identifier: AGPL-3.0-or-later
// GENERATED by fixpp-codegen (078-precompiled-builder-libs). DO NOT EDIT.
// fixpp/vlatest/messages/UserNotification.builder.cpp -- external-linkage build_ definition (data-model.md Entity 4), compiled only into fixpp_builders_<ver>; no validator symbol (SC-003).
#include "UserNotification.hpp"
#include <fixpp/wire/body_builder.hpp>

namespace fixpp::vlatest {

::fixpp::core::expected_t<::std::span<::std::byte>> build_UserNotification(::std::span<::std::byte> out, UserNotificationArgs const& args) noexcept {
    ::fixpp::wire::body_builder bb{"CB"};
    if (args.text) {
        auto r = bb.field(58, *args.text);
        if (!r) return ::std::unexpected(r.error());
    }
    if (args.encoded_text) {
        auto r_len = bb.field(354, static_cast<::std::int64_t>(args.encoded_text->size()));
        if (!r_len) return ::std::unexpected(r_len.error());
        auto r_data = bb.field(355, *args.encoded_text);
        if (!r_data) return ::std::unexpected(r_data.error());
    }
    if (args.usernames) {
        auto gh0 = bb.group_begin(809, 553);
        if (!gh0) return ::std::unexpected(gh0.error());
        for (auto const& item0 : *args.usernames) {
            auto en0 = gh0->add_entry();
            if (!en0) return ::std::unexpected(en0.error());
            auto& eh0 = *en0;
    if (item0.username) {
        auto r = eh0.set_string(553, *item0.username);
        if (!r) return ::std::unexpected(r.error());
    }
        }
        auto ge0 = bb.group_end(*gh0);
        if (!ge0) return ::std::unexpected(ge0.error());
    }
    if (args.user_status) {
        auto r = bb.field(926, *args.user_status);
        if (!r) return ::std::unexpected(r.error());
    }
    if (args.throttles) {
        auto gh1 = bb.group_begin(1610, 1611);
        if (!gh1) return ::std::unexpected(gh1.error());
        for (auto const& item1 : *args.throttles) {
            auto en1 = gh1->add_entry();
            if (!en1) return ::std::unexpected(en1.error());
            auto& eh1 = *en1;
    if (item1.throttle_action) {
        auto r = eh1.set_int(1611, *item1.throttle_action);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item1.throttle_type) {
        auto r = eh1.set_int(1612, *item1.throttle_type);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item1.throttle_no_msgs) {
        auto r = eh1.set_int(1613, *item1.throttle_no_msgs);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item1.throttle_time_interval) {
        auto r = eh1.set_int(1614, *item1.throttle_time_interval);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item1.throttle_time_unit) {
        auto r = eh1.set_int(1615, *item1.throttle_time_unit);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item1.throttle_msg_type) {
        auto gh2 = eh1.group_begin(1618, 1619);
        if (!gh2) return ::std::unexpected(gh2.error());
        for (auto const& item2 : *item1.throttle_msg_type) {
            auto en2 = gh2->add_entry();
            if (!en2) return ::std::unexpected(en2.error());
            auto& eh2 = *en2;
    if (item2.throttle_msg_type) {
        auto r = eh2.set_string(1619, *item2.throttle_msg_type);
        if (!r) return ::std::unexpected(r.error());
    }
        }
        auto ge2 = bb.group_end(*gh2);
        if (!ge2) return ::std::unexpected(ge2.error());
    }
        }
        auto ge1 = bb.group_end(*gh1);
        if (!ge1) return ::std::unexpected(ge1.error());
    }
    return bb.commit(out);
}

}  // namespace fixpp::vlatest
