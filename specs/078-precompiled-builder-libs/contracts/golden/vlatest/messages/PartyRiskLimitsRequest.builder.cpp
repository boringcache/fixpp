// SPDX-License-Identifier: AGPL-3.0-or-later
// GENERATED by fixpp-codegen (078-precompiled-builder-libs). DO NOT EDIT.
// fixpp/vlatest/messages/PartyRiskLimitsRequest.builder.cpp -- external-linkage build_ definition (data-model.md Entity 4), compiled only into fixpp_builders_<ver>; no validator symbol (SC-003).
#include "PartyRiskLimitsRequest.hpp"
#include <fixpp/wire/body_builder.hpp>

namespace fixpp::vlatest {

::fixpp::core::expected_t<::std::span<::std::byte>> build_PartyRiskLimitsRequest(::std::span<::std::byte> out, PartyRiskLimitsRequestArgs const& args) noexcept {
    ::fixpp::wire::body_builder bb{"CL"};
    if (args.text) {
        auto r = bb.field(58, *args.text);
        if (!r) return ::std::unexpected(r.error());
    }
    if (args.subscription_request_type) {
        auto r = bb.field(263, *args.subscription_request_type);
        if (!r) return ::std::unexpected(r.error());
    }
    if (args.encoded_text) {
        auto r_len = bb.field(354, static_cast<::std::int64_t>(args.encoded_text->size()));
        if (!r_len) return ::std::unexpected(r_len.error());
        auto r_data = bb.field(355, *args.encoded_text);
        if (!r_data) return ::std::unexpected(r_data.error());
    }
    if (args.party_i_ds) {
        auto gh0 = bb.group_begin(453, 448);
        if (!gh0) return ::std::unexpected(gh0.error());
        for (auto const& item0 : *args.party_i_ds) {
            auto en0 = gh0->add_entry();
            if (!en0) return ::std::unexpected(en0.error());
            auto& eh0 = *en0;
    if (item0.party_id) {
        auto r = eh0.set_string(448, *item0.party_id);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item0.party_id_source) {
        auto r = eh0.set_char(447, *item0.party_id_source);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item0.party_role) {
        auto r = eh0.set_int(452, *item0.party_role);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item0.party_role_qualifier) {
        auto r = eh0.set_int(2376, *item0.party_role_qualifier);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item0.party_sub_i_ds) {
        auto gh1 = eh0.group_begin(802, 523);
        if (!gh1) return ::std::unexpected(gh1.error());
        for (auto const& item1 : *item0.party_sub_i_ds) {
            auto en1 = gh1->add_entry();
            if (!en1) return ::std::unexpected(en1.error());
            auto& eh1 = *en1;
    if (item1.party_sub_id) {
        auto r = eh1.set_string(523, *item1.party_sub_id);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item1.party_sub_id_type) {
        auto r = eh1.set_int(803, *item1.party_sub_id_type);
        if (!r) return ::std::unexpected(r.error());
    }
        }
        auto ge1 = bb.group_end(*gh1);
        if (!ge1) return ::std::unexpected(ge1.error());
    }
        }
        auto ge0 = bb.group_end(*gh0);
        if (!ge0) return ::std::unexpected(ge0.error());
    }
    if (args.requested_party_roles) {
        auto gh2 = bb.group_begin(1508, 1509);
        if (!gh2) return ::std::unexpected(gh2.error());
        for (auto const& item2 : *args.requested_party_roles) {
            auto en2 = gh2->add_entry();
            if (!en2) return ::std::unexpected(en2.error());
            auto& eh2 = *en2;
    if (item2.requested_party_role) {
        auto r = eh2.set_int(1509, *item2.requested_party_role);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item2.requested_party_role_qualifier) {
        auto r = eh2.set_int(2386, *item2.requested_party_role_qualifier);
        if (!r) return ::std::unexpected(r.error());
    }
        }
        auto ge2 = bb.group_end(*gh2);
        if (!ge2) return ::std::unexpected(ge2.error());
    }
    if (args.risk_limit_platform) {
        auto r = bb.field(1533, *args.risk_limit_platform);
        if (!r) return ::std::unexpected(r.error());
    }
    if (args.risk_instrument_scopes) {
        auto gh3 = bb.group_begin(1534, 1535);
        if (!gh3) return ::std::unexpected(gh3.error());
        for (auto const& item3 : *args.risk_instrument_scopes) {
            auto en3 = gh3->add_entry();
            if (!en3) return ::std::unexpected(en3.error());
            auto& eh3 = *en3;
    if (item3.instrument_scope_operator) {
        auto r = eh3.set_int(1535, *item3.instrument_scope_operator);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item3.instrument_scope_symbol) {
        auto r = eh3.set_string(1536, *item3.instrument_scope_symbol);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item3.instrument_scope_symbol_sfx) {
        auto r = eh3.set_string(1537, *item3.instrument_scope_symbol_sfx);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item3.instrument_scope_security_id) {
        auto r = eh3.set_string(1538, *item3.instrument_scope_security_id);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item3.instrument_scope_security_id_source) {
        auto r = eh3.set_string(1539, *item3.instrument_scope_security_id_source);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item3.instrument_scope_security_alt_id) {
        auto gh4 = eh3.group_begin(1540, 1541);
        if (!gh4) return ::std::unexpected(gh4.error());
        for (auto const& item4 : *item3.instrument_scope_security_alt_id) {
            auto en4 = gh4->add_entry();
            if (!en4) return ::std::unexpected(en4.error());
            auto& eh4 = *en4;
    if (item4.instrument_scope_security_alt_id) {
        auto r = eh4.set_string(1541, *item4.instrument_scope_security_alt_id);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item4.instrument_scope_security_alt_id_source) {
        auto r = eh4.set_string(1542, *item4.instrument_scope_security_alt_id_source);
        if (!r) return ::std::unexpected(r.error());
    }
        }
        auto ge4 = bb.group_end(*gh4);
        if (!ge4) return ::std::unexpected(ge4.error());
    }
    if (item3.instrument_scope_product) {
        auto r = eh3.set_int(1543, *item3.instrument_scope_product);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item3.instrument_scope_product_complex) {
        auto r = eh3.set_string(1544, *item3.instrument_scope_product_complex);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item3.instrument_scope_security_group) {
        auto r = eh3.set_string(1545, *item3.instrument_scope_security_group);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item3.instrument_scope_cfi_code) {
        auto r = eh3.set_string(1546, *item3.instrument_scope_cfi_code);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item3.instrument_scope_upi_code) {
        auto r = eh3.set_string(2895, *item3.instrument_scope_upi_code);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item3.instrument_scope_security_type) {
        auto r = eh3.set_string(1547, *item3.instrument_scope_security_type);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item3.instrument_scope_security_sub_type) {
        auto r = eh3.set_string(1548, *item3.instrument_scope_security_sub_type);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item3.instrument_scope_maturity_month_year) {
        auto r = eh3.set_string(1549, *item3.instrument_scope_maturity_month_year);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item3.instrument_scope_maturity_time) {
        auto r = eh3.set_string(1550, *item3.instrument_scope_maturity_time);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item3.instrument_scope_restructuring_type) {
        auto r = eh3.set_string(1551, *item3.instrument_scope_restructuring_type);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item3.instrument_scope_seniority) {
        auto r = eh3.set_string(1552, *item3.instrument_scope_seniority);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item3.instrument_scope_put_or_call) {
        auto r = eh3.set_int(1553, *item3.instrument_scope_put_or_call);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item3.instrument_scope_flexible_indicator) {
        auto r = eh3.set_char(1554, (*item3.instrument_scope_flexible_indicator) ? 'Y' : 'N');
        if (!r) return ::std::unexpected(r.error());
    }
    if (item3.instrument_scope_coupon_rate) {
        auto r = eh3.set_decimal(1555, *item3.instrument_scope_coupon_rate);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item3.instrument_scope_security_exchange) {
        auto r = eh3.set_string(1616, *item3.instrument_scope_security_exchange);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item3.instrument_scope_security_desc) {
        auto r = eh3.set_string(1556, *item3.instrument_scope_security_desc);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item3.instrument_scope_encoded_security_desc) {
        auto r_len = eh3.set_int(1620, static_cast<::std::int64_t>(item3.instrument_scope_encoded_security_desc->size()));
        if (!r_len) return ::std::unexpected(r_len.error());
        auto r_data = eh3.set_string(1621, *item3.instrument_scope_encoded_security_desc);
        if (!r_data) return ::std::unexpected(r_data.error());
    }
    if (item3.instrument_scope_settl_type) {
        auto r = eh3.set_string(1557, *item3.instrument_scope_settl_type);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item3.risk_instrument_multiplier) {
        auto r = eh3.set_decimal(1558, *item3.risk_instrument_multiplier);
        if (!r) return ::std::unexpected(r.error());
    }
        }
        auto ge3 = bb.group_end(*gh3);
        if (!ge3) return ::std::unexpected(ge3.error());
    }
    if (args.requesting_party_i_ds) {
        auto gh5 = bb.group_begin(1657, 1658);
        if (!gh5) return ::std::unexpected(gh5.error());
        for (auto const& item5 : *args.requesting_party_i_ds) {
            auto en5 = gh5->add_entry();
            if (!en5) return ::std::unexpected(en5.error());
            auto& eh5 = *en5;
    if (item5.requesting_party_id) {
        auto r = eh5.set_string(1658, *item5.requesting_party_id);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item5.requesting_party_id_source) {
        auto r = eh5.set_char(1659, *item5.requesting_party_id_source);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item5.requesting_party_role) {
        auto r = eh5.set_int(1660, *item5.requesting_party_role);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item5.requesting_party_role_qualifier) {
        auto r = eh5.set_int(2338, *item5.requesting_party_role_qualifier);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item5.requesting_party_sub_i_ds) {
        auto gh6 = eh5.group_begin(1661, 1662);
        if (!gh6) return ::std::unexpected(gh6.error());
        for (auto const& item6 : *item5.requesting_party_sub_i_ds) {
            auto en6 = gh6->add_entry();
            if (!en6) return ::std::unexpected(en6.error());
            auto& eh6 = *en6;
    if (item6.requesting_party_sub_id) {
        auto r = eh6.set_string(1662, *item6.requesting_party_sub_id);
        if (!r) return ::std::unexpected(r.error());
    }
    if (item6.requesting_party_sub_id_type) {
        auto r = eh6.set_int(1663, *item6.requesting_party_sub_id_type);
        if (!r) return ::std::unexpected(r.error());
    }
        }
        auto ge6 = bb.group_end(*gh6);
        if (!ge6) return ::std::unexpected(ge6.error());
    }
        }
        auto ge5 = bb.group_end(*gh5);
        if (!ge5) return ::std::unexpected(ge5.error());
    }
    if (args.risk_limit_request_id) {
        auto r = bb.field(1666, *args.risk_limit_request_id);
        if (!r) return ::std::unexpected(r.error());
    }
    if (args.requested_risk_limit_type) {
        auto gh7 = bb.group_begin(1668, 1530);
        if (!gh7) return ::std::unexpected(gh7.error());
        for (auto const& item7 : *args.requested_risk_limit_type) {
            auto en7 = gh7->add_entry();
            if (!en7) return ::std::unexpected(en7.error());
            auto& eh7 = *en7;
    if (item7.risk_limit_type) {
        auto r = eh7.set_int(1530, *item7.risk_limit_type);
        if (!r) return ::std::unexpected(r.error());
    }
        }
        auto ge7 = bb.group_end(*gh7);
        if (!ge7) return ::std::unexpected(ge7.error());
    }
    if (args.risk_limit_request_type) {
        auto r = bb.field(1760, *args.risk_limit_request_type);
        if (!r) return ::std::unexpected(r.error());
    }
    return bb.commit(out);
}

}  // namespace fixpp::vlatest
